# 全專案文件矛盾與錯誤稽核 —— 2026-09-01

**性質**:一次性的全庫文件對帳,不是任何 skill 的產出,不屬於任何審查輪次。
**觸發**:管理者裁決「先不管專案進度,把文件裡的矛盾與錯誤抓出來修正,否則後續產出品質會很糟」。

## 範圍與方法

| | |
|---|---|
| 範圍 | 全庫 ~189 檔 / ~58,000 行,**含**英文框架檔(`.claude/skills`、`.claude/agents`、`WORKFLOW-GUIDE.md`、`UPGRADING.md`)。**不含** `production/session-logs/`(73 萬行稽核流水帳) |
| 比對基準 | **文件 vs 文件**。管理者明文裁決本輪**不做文件↔程式碼對帳** —— 凡只能靠讀 `src/` 判定的宣稱,一律標為「本輪範圍外・未查證」,不予推測 |
| 執行方式 | 12 名 specialist 平行查核 + 主流程自查開場載入檔與 `.claude/rules/` |
| 抽驗 | 主流程對每一批最重的 2~4 筆獨立重跑查證,**共抽驗 26 筆,零誤報** |
| 總計 | **136 筆** |

### 🔴 可信度說明(必讀)

1. **本報告的每一筆都附出處行號。** 讀者不必相信作者,可自行打開核對。
2. **行號會漂移。** 本報告刻意在正文中以「檔案 + 內容特徵」描述,行號僅供定位起點。
   本次稽核抓到 **7 處**因行號漂移而指錯地方的引用,是專案 `.claude/rules/design-docs.md:34`
   明文禁止的寫法 —— 本報告不重蹈覆轍。
3. **subagent 的結論已被抽驗但未逐筆重跑。** 未經抽驗的筆數,其可信度等同「一名查核員的單方宣告」。
4. **本輪未查到 ≠ 沒問題。** 第五節逐項列出誠實的涵蓋缺口。

---

## 一、本輪修正的(約 50 筆)

### 1.1 四組高風險數字 —— 改為單一來源

病根不是個別寫錯,而是**同一個事實被手抄 4~7 份,無一份被指定為唯一來源**。
逐處改對只會製造下一次漂移 —— 專案自己的 `docs/consistency-failures.md` 記載「修法本身引入
新缺陷」已發生 **8 場**。

| 事實 | 複本數 | 現況 | 處置 |
|---|---|---|---|
| 架構立場總項數 | 4 處 / 3 個值 | `adr-revision-history.md` 寫 **81**、`architecture.yaml` 檔頭寫 **85**、`technical-preferences.md` 寫 **87**。**實測 87**(10 state / 13 interface / 29 api / 35 forbidden) | 只留登記表檔頭,其餘改為「查登記表」 |
| 需求涵蓋率 | 6 處 | 皆為 `64 ✅ / 34 ⚠️ / 32 ❌` | 只留 `traceability-index.md`,其餘指過去 |
| `tactical-combat-system.md` 行數 | 7 處 | 皆寫 **1038**,實際 **1077**。git 可追:登記豁免當下(`b001c46`)確為 1038,**同日再兩次提交增至 1077** | **不再寫數字**,改為「該文件為明文例外」 |
| ADR 核准狀態 | 多處 | `traceability-index.md` 仍宣稱「5 份 ADR 皆仍為 `Proposed`」,實際 0002/0003 已於 2026-08-25 `Accepted` | 只留 ADR 本體,其餘指過去 |

> ⚠️ **「架構立場項數」這一組的方向與直覺相反**:被指定為權威來源的 `architecture.yaml`
> 檔頭是**錯的**(85),而下游摘要 `technical-preferences.md` 是**對的**(87)。
> 而 `technical-preferences.md` 同時寫著「數字有疑義時一律以登記表為準」——
> **照那句話做的人會拿到錯的數字。**

### 1.2 開場自動載入的 7 個檔(全部修,含低嚴重度)

這 7 個檔的內容進入**每一次對話**,錯誤會被複製進之後寫的每一份文件。

| 檔案 | 問題 | 嚴重度 |
|---|---|---|
| `technical-preferences.md` | 禁令逐節表合計寫 **31**、散文寫「其餘 **27** 項」,實測 **35 / 31**。ADR-0003 那列寫 1,實際 5。**同檔第 251 行寫 87 是對的** —— 同一份文件兩個互斥數字,兩處都自稱「逐節實測」 | 高 |
| `technical-preferences.md` | 「C7 仍開」,而 `consistency-failures.md` 記載已於 2026-08-21 由兩側各自關閉 | 高 |
| `technical-preferences.md` | 「架構總監覆核從未跑過,**五份 ADR 皆然**」為假。三個獨立來源記載 2026-08-24 對 ADR-0003 執行過(CONCERNS → 窄範圍複驗 APPROVE)。**該句 2026-08-21 為真,未更新** | 高 |
| `technical-preferences.md` | ADR-0001「6 項待驗證**全部需碰真引擎**」不精確 —— 第 (5) 是要寫的測試、第 (6) 是程式碼審查項(且 `src/` 尚無對應程式碼,現在無從做)。**真正需跑引擎探針的是 4 項** | 中 |
| `technical-preferences.md` | `addons/gdUnit4/`「272 檔,約 484 KB」。實測:全部檔案 516 / 2128 KB;僅 `.gd` 236 / 1356 KB;git 追蹤 512 / 1901 KB。**三種算法皆不符** | 中 |
| `coding-standards.md:4` | 「**Every system must have** a corresponding ADR」與劑量規則 3「ADR 只在跨系統契約時才寫」正面衝突,**兩者皆開場載入** | 高 |
| `directory-structure.md` | 目錄樹 **13 個子目錄不存在**;**4 個實際存在的頂層未畫**(含 `addons/` —— 專案唯一相依套件且明文進版控);且畫著 `src/networking/`,而 `networking_features` 是**專案級禁令** | 高 |
| `coordination-rules.md:25` | model tier 表列 `/story-readiness` 為 haiku,檔案實為 sonnet。反方向零落差 | 中 |

> **裁決依據(2026-09-01,管理者)**:`coding-standards.md:4` 依 2026-08-25 劑量裁決改為
> 「只在跨系統契約時才寫」。連帶修正 `WORKFLOW-GUIDE.md` 的「最少 3 份 ADR」下限敘述。

### 1.3 會讓人做錯事的高嚴重度

#### (a) 兩顆會直接變成程式碼的地雷 —— 在唯二已 `Accepted` 的 ADR 裡

| 出處 | 問題 | 後果 |
|---|---|---|
| `adr-0002` `to_dict()` 範例 | `"source": "combat_card"` 小寫,而列舉是 `COMBAT_CARD`、檢查函式只收大寫名。**全檔七處只有這一處小寫**,同一行另一欄位卻是大寫 → 漏改 | **存檔寫得出來、雜湊也通過**(該層不驗語意),讀回時**每一筆**記錄被判非法 → 整個存檔讀不回來。**失敗點離寫入點很遠** |
| `adr-0003` Key Interfaces | 一處要求「回傳 `MIGRATION_FAILED`」,**同節往上 50 行**的列舉定義明文寫「該值由下一份 ADR 擴充,本 ADR 只定義兩種」 | 照著寫**直接編譯失敗** |

#### (b) 會擋住開工、或誤導排程的假狀態

| 出處 | 問題 |
|---|---|
| `traceability-index.md` | 仍宣稱 5 份 ADR 皆為草案 → 照它判斷,**剛核准、唯二能開工的兩個系統會被判定不能動** |
| `traceability-index.md` | `TR-concept-012` / `-014` 標為「❌ 缺口 —— 尚未登記到 `architecture.yaml`」。三條 pattern(`rng_in_combat_settlement` / `networking_features` / `procedural_terrain_generation`)**2026-08-18 即已登記**。**這個假缺口從第二輪起為假,活過第三~七輪** |
| `active.md:11` / `PROJECT-STATUS.md:57` | 「只剩一件事擋著全部正式開發:ADR-0001」**不成立** —— ADR-0005 亦為草案,且管轄四個已核准系統之一。**同一份 `active.md` 的後段自己就寫著「棋盤畫面同時踩到兩份草案」** |
| `one-year-plan.md:219` | 「一行遊戲程式碼都還沒寫過」。實測 **3,791 行 / 241 條測試**,專案檔 2026-08-27 建立。**該計畫現在說專案還在第 0 個月** |
| `one-year-plan.md:210, :222` | 「ADR-0001 是五份裡唯一從未被檢查過的」。該說法已於 2026-08-31 在三份文件更正,**唯獨此檔兩處沒改到** |
| `PROJECT-STATUS.md:122` | 「目前程式約 **1,650** 行」。實測 3,791。查核員試過五種算法**無一對得上**。該句作用是向管理者傳達「投入還很淺」→ **低報 2.3 倍會影響轉向決策** |
| `PROJECT-STATUS.md:122` | 「停損點在第 8 個月」寫成肯定句,而 `one-year-plan.md:90` 明文「**③ 仍未決,需要管理者明文同意,否則那一節只是一段建議**」。且該頁「之後會來問你的」清單**不含它** —— 全專案唯一明文等待裁決的事,是唯一沒被端上去的 |
| `active.md:969-975` | 標題為「**現況**」的整區已全面過期(五份 ADR 全草案 / `src/` 為空 / 登記表 81 項 / 戰棋未核准)。**同檔其他歷史區塊都有「本節為舊清單」標記,唯獨這一區沒有** |
| `adr-0004` Validation Criteria #7 | 判準句要求「涵蓋**無缺口**」,而同一份文件另外三處明文承認有 1 項永久缺口(`TR-save-030` 雲端同步,歸屬平台策略)。**這條驗收條件結構上永遠不可能通過** |
| `cursor-highlight-state.md` Open Questions 末列 | 標 **✅ 已關閉**、宣稱「鎖死問題已修法」。該修法(規則文字 + 旋鈕 + AC-56/57)**於次日第十一輪全套撤回**,同檔另處寫「撤回後**無任何暫行防線**,缺陷維持 **100% 可重現、未緩解**」。**這是該表唯一標 ✅ 的一列** → 掃表找待辦的人會跳過它 |
| `cursor-highlight-state.md` Open Questions | 宣稱 `design/ux/accessibility-requirements.md`「**實際不存在**」「已是孤兒義務」。該檔 **2026-08-07 建立、89 行、2026-08-19 定 Tier**,且其建立背景欄自陳就是為關閉這一列而建 |
| `entities.yaml` | 登記 `affinity_links_per_character_max: 4`,`source:` 欄指向 `affinity-position-chain.md` —— **而該檔已於 2026-08-31 裁決「不設上限」並明文標舊推導為錯、已移除**。同一錯誤同日已從 `architecture.yaml` 刪除過一次,又在姊妹登記表復發 |

#### (c) 行號指路違規 —— 7 處,全部已漂移失真

`.claude/rules/design-docs.md:34` 明文禁止用行號指向另一份設計文件、`systems-index.md` 或
review log,理由是行號會漂移。**規則預言的失效已全數發生:**

| 出處 | 指向 | 實際 |
|---|---|---|
| `tactical-combat-system.md:326` | `systems-index.md` 第 26 列 | 該處是章節標題 `## Systems Enumeration` |
| `cursor-highlight-state.md:101` | `systems-index.md` 第 166 列 | 實際在第 203 列 |
| `cursor-highlight-state.md:103` | `systems-index.md` 第 165 列 | 同上 |
| `cursor-highlight-state.md:340` | `systems-index.md` 第 166 列 | 同上 |
| `cursor-highlight-state.md:371` | `game-concept.md` 第 172 行 | 該處是好感度卡牌裁決;目標在第 205 列(**漂移 33 列**) |
| `save-system.md:337` | `systems-index.md` 第 165 列 | 該處是空行 |
| `save-system.md:455` | 同檔 Core Rules #10 第 88 行 | 該處是空行,落在 Core Rules #5 段內(**漂移 48 列**) |

#### (d) 工具層 —— 會讓自動化靜默失效

| 出處 | 問題 |
|---|---|
| `.claude/skills/test-setup/` | 產出的測試啟動檔載入 `addons/gdunit4/GdUnitRunner.gd`(**任何大小寫皆不存在**)並呼叫框架沒有的方法,結尾**無條件回報成功**。`coding-standards.md` 已記載這個事故,但**產生它的工具沒改** → 誰再跑一次就把修好的覆寫回壞的 |
| `.claude/skills/smoke-check/` | 兩條指令路徑:一條需引擎在 PATH(**本機沒有**),一條指向上述不存在的檔。而規則明文「無法確認是否執行 → 視為通過,不要判失敗」,且**全文未提檢查引擎結束代碼** → 該關卡在本機**結構性地不可能失敗** |
| `create-control-manifest` / `propagate-design-change` | 指名使用 `TD-MANIFEST` / `TD-CHANGE-IMPACT` 兩個關卡,**`director-gates.md` 的 26 個定義中沒有它們** → agent 只能自行編標準,並產出看似正常的審查報告 |
| `.claude/agents/` | `creative-director`、`producer`、`technical-director` **三份缺「May I write this to [filepath]?」條款**,而三者皆有 Write/Edit。**其餘 46 份全部都有** —— 缺的正好是權限最大、負責寫 ADR 與衝刺計畫的三位 |
| `log-agent.sh` / `log-agent-stop.sh` | grep 備援少 `-m1`,`agent-audit.log` **811/1761 行(46%)為壞行**;`invoked` 505 vs `completed` 445 → **60 個 agent 未回報完成,而該檔是唯一能回答「哪個 agent 死在半路」的紀錄** |

---

## 二、登記待辦、本輪不修(約 86 筆)

依管理者裁決,本輪只修高嚴重度 + 開場載入檔。以下**全部確有其事**,只是不在本輪範圍。

### 2.1 引擎參考庫

- `modules/` 底下 **8 份**子系統參考檔 `Last verified` 停在 **2026-02-12 / Godot 4.6**,而專案釘 **4.7.1**。專案自列三大高風險(輸入裝置 ID 重編號、Control 版面、shader 前處理器)中,**輸入與介面各有對應子檔,而兩份對這兩項零字**。頂層四份已更新到 4.7.1
- `scripting-typing.md` 自稱 327 行,實測 340
- `VERSION.md` 模組索引稱兩份「各補兩節」,其一實際三節

### 2.2 流程手冊與框架(`WORKFLOW-GUIDE.md` 1703 行為主)

根因:**該檔第 10-11 行自己宣告 `workflow-catalog.yaml` 為權威,其後 1690 行與該 catalog、與 `gate-check` skill 各自漂移。**

- 垂直切片:手冊稱「硬閘門、自動 FAIL」,skill 與 catalog 皆為「建議、不阻擋」
- `/art-bible` 是必要產出,**1703 行手冊僅附錄表格提及一次**,流程正文從未教
- Phase 3 閘門手冊列 5 項,實際 12 項(漏測試目錄、CI workflow、範例測試檔等 7 項)
- `/test-setup` 同樣只在附錄;且 catalog 把它放 pre-production,skill 自述 technical-setup
- Phase 1 閘門:漏列「視覺識別錨點」(會擋)、誤列 `systems-index.md`(屬下一道)
- `skill-flow-diagrams.md:35` 把主架構文件寫成 `master.md`,**四個獨立來源一致為 `architecture.md`** → 照圖建檔過不了兩道閘門
- 同檔內部矛盾:`/vertical-slice` 同時排在「寫 epic 之前」(4.2)與「排完 sprint 之後」(4.7)
- Appendix C Workflow 2 整個跳過 Phase 4,照做會被 Pre-Production 閘門擋下
- 計數:hooks 稱 12 實際 **13**(漏的正是 `validate-doc-consistency.sh`);`COLLABORATIVE-DESIGN-PRINCIPLE.md` 稱 48 agent 實際 **49**、稱 `AskUserQuestion` 整合進 16 skill 實際 **48**;README 稱 41 範本實際 **40**;README 稱列出全部 73 skill **實際列 72**(漏 `vertical-slice`)
- 「All skills — Updated to require approval before writing」有 **2 個真反例**(`balance-check`、`consistency-check` 具 Write/Edit 但無請示條款)
- 「Each team skill coordinates a 6-phase workflow」9 個裡僅 3 個是 6 階段
- GDD 範本第 3 節標題為 `Detailed Design`,三處規範皆為 `Detailed Rules` → `/design-review` 逐字比對會誤判
- 「Research from 155 game postmortems confirms…5-10x」**全庫無出處**
- `UPGRADING.md` 目錄漏一節且排序斷裂;README 稱開發環境 Windows 10(實為 11)

### 2.3 工具與 agent 設定

- `agent-roster.md` 兩列 Model 欄與 frontmatter 不符;領導層三位實為 opus 但名冊**無 Model 欄**
- `director-gates.md` 第二個檔內矛盾(**尚未被 `consistency-failures.md` 記錄**):第 31 行說 `review-mode.txt` 於 `/start` 設定,第 827 行的 2026-08-21 裁決說**絕不要建立它**;而 `/start`、`/adopt`、`/sprint-plan` **三支 skill 仍會建立**,且明文「不需另外請示」→ **下次跑 `/start`,該裁決被靜默推翻**
- `/reverse-document` 三條範本路徑缺 `.claude/docs/` 前綴 → 找不到範本
- `/test-setup` CI 範本仍用已遷移的 `MikeSchulze/gdUnit4-action@v1`
- 六個 skill 的 `model:` 低於其指定 `agent:` 一階(含 `create-architecture`)
- `/setup-engine` 仍建議 GUT(專案已裁定 GdUnit4);`creative-director.md` 引用不存在的 `CD-NARRATIVE-FIT`
- `.claude/settings.json` 允許清單含 3 項 Python 指令,專案無 Python
- `.claude/rules/network-code.md` 全份是多人連線規範(伺服器權威、客戶端預測、回滾、頻寬預算),掛在一個**明文禁止連線**的專案裡
- `post-compact.sh` 只有 `echo`、不留痕 → 「它從未跑過」與「每次都跑」**結構上不可區分**

### 2.4 設計與架構文件

- `unit-stats-provisional.md`「假設與依據」仍寫 Φ 總和上限 **±6**,同檔中段已推翻為 **+10**
- 「輸入設定/重新綁定」2026-08-19 判定為**必要**,兩份文件登記「下次盤點必須納入」,2026-08-25 與 08-31 兩次盤點**皆未納入**,系統總數仍 14
- `tactical-combat-system.md` 檔頭「真正擋著實作的三件事」漏列 OQ-10,而同檔末尾自陳 OQ-10 亦為阻擋項
- `systems-index.md:33` 未同步 OQ-16(b) 的降級(**同一天**的兩份文件不一致)
- `adr-0003-deferred-to-implementation.md`:編號項 41,**另有 3 項未編號**(總待辦 44,其中兩項為安全性裁決);**至少 6 項**指涉的構造已改名或已解決而清單未標註 —— 其中排在「實作第一天就會撞到」第一位的那項,**指名的函式在現行文件零命中**
- `adr-0002`:「本次修訂**唯一**改變公開簽章回傳型別的一處」,同日實際兩處;兩處交叉引用指向**不存在的「機制七之二」**
- `adr-0003` 機制五把禁止的形狀寫成慣例本身(漏存在性守衛);`validate_semantics()` 契約「只做值域」的舊措辭殘留在**程式碼註解與需求對照表**兩處,且 ADR-0003 照抄舊版;Key Interfaces 的 `SaveReader` 仍回傳裸 `Variant`,與**同節往上 30 行**的 2026-08-24 修正抵觸
- `adr-0004`:近一半未讀(見第五節);Key Interfaces 的 `write()` 回傳 `ReadResult`,而寫入失敗型別是 `WriteFailure`,**`ReadResult` 無欄位可承載它**;`ReadResult` 宣告六種互斥結局卻只提供**五個**建構子;`TR-save-028` 被算進「7 項部分涵蓋」的分子,**但需求對照表 19 列中不存在**(編號 027 直接跳 029)→ 兩種修法會讓 22/7/1 往**相反方向**動
- `adr-0005`(**依裁決本輪不動**):Key Interfaces 一行仍寫已刪除的做法(**正下方第三行就是刪除說明**);兩個公開方法全文無定義,其一結構上做不到宣稱的事;R6-8 散文改了但**正下方程式碼範例沒改**;「涵蓋結論」表停在第五輪 15/4,而狀態區已載明第七輪重推為 **12/7**(較差);讀取側介面標「2 個查詢」實列 6 個;`## Date` 欄漏第四次修訂;稱登記表 41 項(實際 87);稱模組檔「全部 8 份」(現為 10 份);兩項 forbidden pattern 正文寫「候選」而登記表已 `active`;正文 4 次引用一個**不存在的「Registry 更新提案」節**
- `adr-0001`:`Dictionary[Vector2i, unit_id]` 三處寫成非法型別字面,**僅 Key Interfaces 一處就地加註更正**,未回改另三處 —— 與 2026-08-21「錯誤範例擴散造成 13 處編譯期錯誤」同形;「本 ADR 明確排除以 `PhysicsServer`/`RayCast` 實作視線」為**指向自己的空引用**(本文從未排除,視線不歸它管);`board_version`「唯一遞增時機」以「或任一其他……」收尾為**開放列舉**,而驗收條件假設它封閉
- `cursor-highlight-state.md`:AC-48 引用一個已改名的段落(且「六組」在數學上亦錯,應為三組);一條「數值必須嚴格大於 0」的硬性約束掛到完全無關的 AC,**該約束實際零驗收覆蓋**;Open Questions 末列脫離表格結構(缺開頭 `|`)→ 渲染後掉出表格,**可能正是 F1 那列長期沒被注意到的原因**
- `save-system.md`:AC-12 的斷言與 Core Rules #14 的六步序列直接矛盾(**對一個完全正確的實作也會判失敗**);AC-6 對耐久性做無條件宣稱,而第三輪已收斂範圍且明文點名 Edge Cases 同步、**未點名 AC-6**;「分三級告知,見 AC-69」但 AC-69 只回傳兩類
- `consistency-failures.md` 自身:稱「每一輪都建議建檔,共 **7** 次」,實測第一輪零命中、自第三輪起 **5** 次,且最後一輪自稱「第五次」。**同時犯了它自己記錄的全稱字眼與未數之數兩種模式**;掛「⚠️ 仍開」的兩筆中,`director-gates.md` 那筆**已被調和**(第 797-834 行 25 行警示區塊);「沒有任何 skill 會主動建立它」有反例(`/consistency-check` 會建立)

---

## 三、需要管理者裁決,本輪不動(6 項)

| # | 事項 | 為什麼不能自行決定 |
|---|---|---|
| 1 | **引擎 LLM 知識截止日**:`godot/VERSION.md` 寫 **January 2026**,`unity/`、`unreal/` 的 `VERSION.md` 與 `engine-reference/README.md` 三處寫 **May 2025** | 這是 `breaking-changes.md` 與 `VERSION.md` 對 **4.4 / 4.5 / 4.6 三個版本**風險分級矛盾的**根因**(4.7 兩邊一致,掩蓋了其餘三處)。改哪一邊會連動風險分級,屬引擎專家的技術判斷 |
| 2 | **`adr-0003` 錯誤代碼衝突**:同一輸入,ADR-0003 說回傳 `MIGRATION_FAILED`、ADR-0002 路徑判為 `SEMANTIC_VALIDATION_FAILED` | 兩者對玩家後果不同(觸發遷移 vs 判存檔語意損毀)。收斂需讀 ADR-0004 與 GDD Core Rules #10 |
| 3 | **五項 `project.godot` 設定標 (A) 級但無證據檔路徑** | 依 2026-08-31 新規則「沒附檔案路徑的 (A) 級一律降為 (C)」,五項應全部降級。**反諷**:未標 (A) 的三項「會反噬的實測事實」反而附了引擎原始碼位置與逐字警告,證據強度更高 |
| 4 | **ADR-0005「單一根 Viewport 假設」** | ✅ 已裁決(2026-09-01):**不凍結**,把該假設重新裁決列為解凍前置。本輪不動該檔 1609 行 |
| 5 | **第 8 個月停損點** | 全專案唯一明文標為「仍未決、需管理者明文同意」的事項,而管理者狀態頁已將其寫成既定事實 |
| 6 | **核心幻想反轉** | `game-concept.md` 仍為「運籌帷幄的軍師、算無遺策」,`gameplay-flow-decisions.md` D1 已裁決改為克蘇魯恐怖基調。落差報告列為**第一級「改了就是另一款遊戲」第 1-1 項**。**這是已裁決未執行的設計工作,不是事實對齊** |

---

## 四、本次抽驗紀錄

主流程對每批最重的發現獨立重跑查證,**共 26 筆,零誤報**。抽驗涵蓋:禁令逐節計數、
版本風險分級、知識截止日分裂、關係線上限矛盾、核心幻想反轉、ADR 狀態、假缺口、
程式碼行數、停損點狀態、目錄樹子目錄、skill 計數、架構主文件檔名、請示條款缺漏、
runner 路徑、稽核 log 壞行率、enum 大小寫、未定義的 enum 成員、單一 Viewport 假設、
永遠達不到的驗收條件、`TR-save-028` 缺列、標 ✅ 的撤回項、宣稱不存在的檔案。

### 查核員的自我修正(值得記錄)

- 架構文件查核員在停頓後重數,**自行撤回**一條線索:`adr-0005:1514` 的 8 與 round6 的 10 **不是矛盾**(8 個核心 `class_name` + 2 個輔助 = 10,同一括號內兩個計數各自成立)。
- **兩名查核員訂正了主流程的簡報錯誤**:①主流程稱「8 份 `architecture-review`」,實際 **7 份**(第八輪未執行 —— 這正是本專案的失效模式:「該做但沒做」被記成已存在);②主流程稱 `adr-0003-revision/` 有 4 份檔,實際 **18 份**。
- 主流程亦訂正自己:第一批曾稱「目錄結構圖與實際相符」,**該檢查只比對了頂層**,子目錄有 13 處不符。

---

## 五、誠實的涵蓋缺口

**「本輪未查到」不等於「沒問題」。** 以下為查核員明確回報的未讀區,任何後續判斷不得
把這些區塊當成已驗證。

| 區塊 | 缺口 |
|---|---|
| `adr-0004`(526 行) | **約 250 行未讀** —— 機制四(逐槽重入)、機制五(分步遷移狀態機)、機制六(權杖四條終止路徑)、機制七(兩階段回寫)整段。**建議下一輪自第 161 行接手** |
| `adr-0005`(1609 行) | 第 1-11 行(前三次修訂的自陳總結)未逐行讀。**該 ADR 的頭號慣犯模式正是「自陳與正文不符」** —— 此為對稱缺口:查了正文,沒查它對自己的敘述 |
| `adr-0002`(1203 行) | 第 1060-1092 行(Alternative 4/5/6 內文)僅讀部分 |
| `save-system.md` 表一/表二(549-699) | 97 列 + 79 列**逐列逐欄但截斷**讀取;**每列註記尾段未讀**。AC 對應與算術核對皆乾淨 |
| `.claude/agents/` 49 份 | **僅 6 份逐行讀**,其餘全部機械掃描。散文層的內部矛盾未涵蓋 |
| `.claude/skills/` 73 份 | **僅 8 份逐行讀**。「description 與正文步驟是否相符」這一類**涵蓋率不到兩成** |
| `.claude/docs/director-gates.md` | 中段 26 個關卡定義本體(約 130-780 行)**未逐項讀**。`TD-MANIFEST` 缺失一項顯示那裡是 agent 行為的唯一依據 |
| `.claude/docs/templates/` 40 份 | **內容完全未讀**,僅做路徑與角色名掃描 |
| `UPGRADING.md`(861 行) | 六個版本章節內文未讀,遷移步驟正確性未查 |
| Unity / Unreal 參考庫 21 份子檔 | 僅檔頭日期,內文未讀。專案不用該兩引擎,優先度低 |
| 七份 `architecture-review-*.md` | **無一完整讀過**。四個彙總量(自陳膨脹 / 修法引入新缺陷 / 結構化欄位未同步 / 只修觸發處)**未獨立重算** —— 主流程刻意不派,理由:回頭清算歷史帳不影響未來文件品質,而本專案的問題正是流程劑量 |
| `production/session-state/active.md` | 約 2,000 行歷史區僅關鍵字定向掃描,未逐段比對 |
| `production/qa/evidence/` 17 張 PNG | 檔案與版控狀態已核,**未開圖檢視** |
| 全庫 | **未做文件↔程式碼對帳**(管理者裁決)。所有「照它寫會編譯失敗 / 會壞」的判斷,依據是**文件彼此的定義,不是編譯器** |
