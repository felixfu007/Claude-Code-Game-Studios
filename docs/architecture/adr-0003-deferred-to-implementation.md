# 存檔格式文件 —— 延後到實作階段處理的項目

**建立日期**:2026-08-21
**裁決**:專案管理者於 2026-08-21 決定 ——
「先只修**會讓程式跑不起來**的那幾項,其餘登記待辦。只要不會阻擋當前文件討論進度的都先往後擺,
等之後進入小規模開發再說。」

**對應文件**:`docs/architecture/adr-0003-save-system-serialization-format-and-type-safety.md`
**依據門檻**:`docs/architecture/adr-acceptance-criteria.md` 第四節
(誠實登記、且不影響本文件決策的項目,**不阻擋核准**)

---

## 這份清單怎麼來的

2026-08-21 對存檔格式做了四件事,累積出 40 多項發現:

1. **四支引擎探針**(驗證那份文件依賴的引擎行為)—— 推翻 18 處寫法與三項說法
2. **一次修訂草案** —— 被兩道覆核判定不可寫入,6 項必修、約 15 項次要
3. **一個能真的跑起來的設計骨架** —— 7 項設計層問題、16 項「文件沒講但實作時非決定不可」
4. **一次跨兩個行程的實測** —— 關掉一項至今無人驗證的最壞情況

其中**會讓程式跑不起來或安靜寫壞存檔的**已納入本次修訂。**其餘即本清單。**

> **給未來讀者的提醒**:本清單裡沒有一項是「不重要」。它們是「**現在不修也不會讓文件無法被討論**」。
> 進入實作時請逐項處理,不要因為它們被歸為延後就當成已解決。

---

## 一、實作第一天就會撞到的(優先處理)

| # | 白話問題 | 技術細節 | 為何能延後 |
|---|---|---|---|
| D-1 | 驗證器如果綁在一個沒人保管的物件上,那個物件被回收後,**所有存檔都會被判定損毀** | 驗證器 `Callable` 不會讓它綁定的 `RefCounted` 續命;擁有者離開作用域後 `is_valid()` 為 `false`、`get_object()` 為 null,**但 `get_method()` 仍回傳方法名**(看起來還活著)。fail-closed 把「有人忘了留參照」放大成全面失敗 | 這是**誰保管驗證器實例**的問題,屬於程式結構決定,文件層只需說「呼叫前先檢查有效性」(本次已納入) |
| D-2 | 檢查一個訊號是否為空,不能用直覺的那個方法 | `Signal.is_null()` 在跨行程情況下回 `false`,但 `get_object()` 是 null、`connect()` 回 `ERR_UNCONFIGURED` | 寫入側閘門已拒絕此型別(本次已納入),所以只有處理**舊有損壞檔案**時才會遇到 |
| D-3 | 型別檢查的寫法讓讀檔慢了三成,而其中大部分不是必需的 | 500 筆記錄下:規格形狀 18–26 毫秒;精簡版(只回代碼、不組路徑字串)2.4–4.2 毫秒;實際編碼只佔 1.4–1.8 毫秒。**85% 的成本來自寫法,不是語意** | 純效能。建議實作:成功路徑走精簡版,偵測到失敗後再跑一次帶診斷版本(失敗是罕見路徑) |
| D-4 | 型別檢查對外公開時,**只能回報「有毒」,無法回報「毒在哪」** | `validate_payload_types()` 的簽章帶不出 `offending_path`,而結果物件規定要有這個欄位。遷移鏈重用它時只知有問題、不知位置 | 診斷品質問題,不影響正確性 |
| D-5 | 深度上限設 64 夠不夠,沒有依據 | 實測合法巢狀深度 256 可正常往返;真實資料約 4 層;引擎遞迴上限約 1024 框 | 64 遠高於實務需求。等有真實資料形狀再校準 |

---

## 二、文件沒講、實作時必須自己決定的(11 項)

**每一項都是那份文件的一個洞。** 骨架當下的決定記在括號裡,供實作參考,但**那不是文件的裁決**。

| # | 文件沒回答的問題 | 骨架當時怎麼決定 |
|---|---|---|
| D-6 | 編碼結果長度為零時,該回哪一種拒絕代碼? | 視為與「型別違規」不同的獨立情況 |
| D-7 | 目錄裡出現**重複的區塊名稱**怎麼辦? | 拒絕整份檔案 |
| D-8 | 語意驗證的結果型別只有名字、**沒有欄位定義** | 自行定義了最小欄位 |
| D-9 | 逐區塊檢查是「一個一個驗完就用」還是「全部先驗完才開始用」?**副作用:讀取因此不是全有全無** | 採交錯(驗一個用一個) |
| D-10 | 同一個區塊名稱被**登記兩次**驗證器怎麼辦? | 後者覆蓋前者 |
| D-11 | 取驗證器的函式回傳可能是空值,**呼叫端的義務沒寫** | 呼叫端一律先檢查 |
| D-12 | 每個區塊自己的版本號,由誰負責比對? | 骨架未實作,留給遷移文件 |
| D-13 | 外層信封出現**未知的額外欄位**怎麼辦?**副作用:存檔可夾帶任意資料而不被發現** | 忽略 |
| D-14 | 排序函式可不可以改動傳入的資料? | 不改動,回傳新陣列 |
| D-15 | 用 `StringName` 當鍵安不安全? | 實測 `has("k")` 與 `has(&"k")` 四種組合全部互通,故安全 |
| D-16 | 區塊清單的鍵集合**不被任何檢查碼涵蓋**,而且**不能**放進形狀檢查 —— 否則會提前攔下一個規格要求由檢查碼攔的情況。**兩者無法同時滿足,這是設計的真實張力** | 依規格,不放進形狀檢查 |

---

## 三、文件內部一致性(不影響程式行為)

| # | 內容 |
|---|---|
| D-17 | 「型別白名單不是靠維護清單實現,而是格式選擇的副產品」這句話與新增的檢查機制矛盾 —— 須拆成兩句(類別實例化仍是副產品;資料型別白名單是新增的維護義務) |
| D-18 | 消解某個舊有疑問的那段推導鏈斷了(它的前提是「不存在型別這個維度」,而現在存在) |
| D-19 | 某條需求的涵蓋理由部分為假(保護來自新增的檢查,不來自格式選擇) |
| D-20 | 效能章節完全沒提新增檢查的成本,而該節現行寫「可忽略」 |
| D-21 | 驗收條件沒有任何一條驗證新增的檢查機制 |
| D-22 | 驗證器契約只寫「值域」漏了「型別」,而唯一的實作早已是兩者都做 —— **定義通用契約的文件比它的實作弱** |
| D-23 | 讀取側的失敗回報方式與寫入側不對稱:三種不同失敗塌成同一個空值。而**同日另一份文件剛把這個形狀判定為必修並全數移除** |
| D-24 | 風險等級是否維持、以及「這次的錯不是引擎版本造成的、而是過期的舊版記憶」這件事,都該寫進文件 |
| D-25 | 幾處措辭與已死的交叉引用(某個括號裡的例子指向一個已關閉的項目) |

---

## 四、實作層陷阱(寫程式時要知道,文件不必寫)

| # | 內容 |
|---|---|
| D-26 | 列舉型別的欄位**不擋超出範圍的整數**。寫字面量 `1.7` 會被編譯器擋;寫 `7` 編譯通過並存進去;經過無型別變數傳入的小數會**靜默截斷**。「編譯期會擋」只對字面量成立 |
| D-27 | 結果物件的預設值等於「成功」—— 忘記賦值會被讀成成功。既有文件有同性質先例,故不改慣例,但須有呼叫端義務與測試 |
| D-28 | 結果物件的型別無法用一般方式辨識(它回報的是父類別名稱),診斷訊息不能依賴它 |
| D-29 | 結果物件本身**永遠不可放進存檔內容**(會被自家檢查拒絕) |
| D-30 | 檢查碼算失敗時若回傳空陣列,而比較用直接相等 → **兩邊都失敗時比較成立,檢查被靜默繞過**(本次已納入修正,此處記錄以免未來重犯) |

---

## 五、跨文件事項(範圍外,但不可靜默省略)

| # | 內容 |
|---|---|
| D-31 | **好感度那份文件內部自相矛盾**:它一處主張三個結果類別是獨立的全域名稱(並計入七個全域名稱),另一處把它們的欄位型別寫成巢狀在別的類別裡。**前一種讀法實測不編譯。** 兩者只能選一,現行文件同時主張兩者 |
| D-32 | 同一份文件裡有一個型別**全文沒有任何宣告**,只作為回傳型別出現六次,而存檔格式文件消費它 |

---

## 六、仍未查證的引擎行為(9 項)

| # | 內容 | 為何不阻擋 |
|---|---|---|
| D-33 | **發行版本(release build)的行為** —— 全部量測都在除錯版本下 | 與另兩份文件的同一項是同一個洞,**可一次關三份** |
| D-34 | 跨行程時,那個資源編號是否**永遠**不會命中舊物件 | 已測 2000 次配置皆未命中,但**未讀引擎原始碼,故不宣稱是保證** |
| D-35 | 還原出來的有效資源編號拿去對伺服器下指令會怎樣 | **刻意未測**(可能造成不可預期的副作用) |
| D-36 | 其他伺服器的資源編號是否同樣可預測 | 已知一個可預測即足以支撐「寫入側必須拒絕」 |
| D-37 | 訊號的來源物件在同一行程內已死亡的情形 | 寫入側已拒絕該型別 |
| D-38 | 原子置換行為 | 屬另一份文件範圍 |
| D-39 | 遷移鏈是否真的重用了型別檢查 | 屬另一份文件範圍 |
| D-40 | 深度上限 64 是否夠用 | 見 D-5 |
| D-41 | 編碼函式對字典是否還有其他非決定性來源 | 已知的那一個已處理(見本次修訂) |

---

## 七、組合後果分析:兩塊未受完整性標記涵蓋的區域

> **搬遷記錄(2026-08-24)**:本節原擬併入 ADR-0003 Consequences → Negative,依覆核
> 裁定「本身已被裁定為延後項、不阻擋修訂」,改直接併入本延後清單;ADR-0003 保留一段
> 精簡摘要並指向本節。以下為原文,除 **9 處**調整外內容逐字保留 —— 其中 7 處為跨文件指稱重定位、
> 2 處為配合新位置的標題與引導句改寫(內容不變)。其餘 103 行由工具搬移,經 `sha256sum` 與逐行 `diff` 獨立驗證未經改寫。

#### 兩塊未受完整性標記涵蓋的區域(組合後果)

**執行者**:security-engineer。**擬併入位置**:ADR-0003「Consequences → Negative」。
**性質**:組合後果分析,結合兩項已各自存在、但從未合併檢視的既有決策/行為。
**建議處置**:登記為延後項,**不阻擋本次修訂**——等實作階段再決定是否擴大頂層雜湊
的涵蓋範圍。

##### 背景:兩項各自獨立、各自有理由的決定

1. **信封層未知額外鍵——2026-08-24 管理者裁決:忽略,不拒絕。**
   裁決理由:
   「與骨架 c-14 的既有行為一致,與延後清單 D-13 記錄的決定一致……換到的是:未來加
   欄位不必升版本號、新舊版存檔仍可互通」。
2. **`blocks` 字典的額外條目——骨架既有行為 c-4。**
   `prototypes/save-format-skeleton-2026-08-21/README.md`「(c) 設計沒講到」表 c-4:
   「`blocks` 的鍵集合不被任何雜湊涵蓋。manifest 少一條 → 頂層雜湊抓到;但 `blocks`
   少一條 → 頂層雜湊完全看不到」,決定為「在 S4 加明確守衛……刻意不把它提前到 S1B」,
   其副作用逐字記載:「**`blocks` 裡多出來的條目會被靜默忽略(從未讀取、從未檢查)**」。

兩項單獨看都有各自的工程理由(前者換取格式演進彈性,後者是「manifest↔blocks
交叉檢查該放哪一關」這個設計張力下的必要取捨,c-4 原文明說「兩者不能同時滿足」)。

##### 頂層雜湊的實際涵蓋範圍(用於界定「未受涵蓋」的精確意思)

頂層雜湊的輸入是 `(ruleset_version, block_manifest 依 source_id 字典序排列的 tuple
清單)`——**不包含** `blocks` 字典本身的鍵集合,也不包含信封層任何未列在
必要鍵清單中的欄位。**證據**(ADR-0003 內部):
機制二的 manifest 結構定義(「manifest 頂層雜湊
(涵蓋規則集版本號 + 逐區塊 tuple 清單……)」)、機制四的重算公式、以及 Key Interfaces 的
`compute_top_level_hash(ruleset_version, block_manifest)` 簽章——參數只有這兩個。

##### 組合後果:兩塊區域都不受頂層雜湊涵蓋,但風險輪廓不同——不可等量齊觀

合起來看,存檔裡確實存在**兩塊不受頂層雜湊涵蓋的區域**。但這兩塊區域被下一層
防線(型別閘門)涵蓋的程度**並不相同**,必須分開講清楚,否則會把兩者的風險
誤判為同一等級。

###### 區域一:信封層未知額外鍵——型別閘門仍會掃到

依 ADR-0003 機制一之二的讀取側接入點,`deserialize_manifest()` 的順序是
`bytes_to_var()` → **`SaveTypeGate.scan_envelope(整個信封)`** → `check_shape()`。
型別閘門跑在形狀檢查**之前**,而且掃描對象是**整個信封 Dictionary**(遞迴掃描
所有鍵與值)。因此即使信封層出現一個未列在必要鍵清單中的額外鍵,它的**內容**仍會
被這道閘門掃過。

| 未知欄位能夾帶什麼 | 是否可行 |
|---|---|
| `RID`/`Object`/`Callable`/`Signal`(危險型別,見 `docs/engine-reference/godot/modules/core-serialization.md` 第 2、4 節的四象限表格) | ❌ **擋得住**——型別閘門涵蓋整個信封,那四個危險型別無論放在信封的哪個位置都會被掃到並拒絕 |
| 一般資料(字串、數字、陣列等白名單型別) | ✅ 可夾帶,且**不受頂層雜湊保護**——放進去之後,即使被外部竄改,`top_level_hash` 比對仍會通過(因為它本來就沒涵蓋這個鍵) |

**⚠️ 界線務必寫準,不要誇大**:管理者的裁決只開放了「任意資料」這一面,
**沒有**開放「危險型別」那一面。這一點在原提問時沒有明講,此處補記以免日後
誤解裁決範圍。**依據**:ADR-0003 機制一之二的讀取側接入點順序(型別閘門先於形狀檢查、且掃整個信封),
與此處的判定一致。

###### 區域二:`blocks` 的額外條目——型別閘門**不會**掃到內容,是更深的盲區

`blocks` 字典本身是信封的一部分,所以它的**外層形狀**(每個值的外層型別是否為
`PackedByteArray`)會被信封層型別閘門掃到。但 `blocks` 的值是**已經序列化過的
巢狀 `PackedByteArray`**——依 ADR-0003 機制一之二的既定設計(已序列化的巢狀
`PackedByteArray` 不打開來掃、視為葉節點;ADR-0003 機制一之二已明文記載此決策與其代價),
信封層型別閘門**不會
解碼**這個 `PackedByteArray` 的內容,只把它當成一個不透明的葉節點通過。這個設計
本身已由 F4'-a 驗證為安全前提的一部分(manifest 層讀取不遞迴解讀區塊內容,
見 `docs/engine-reference/godot/modules/core-serialization.md` 第 2 節)。

而**具體到 `blocks` 額外條目**——即 `blocks` 字典裡存在一個 `source_id` 不在
`block_manifest` 中的條目——c-4 已明文記載這種條目「從未讀取、從未檢查」,也就是
它**永遠不會被送進 `deserialize_block()`**(該函式才是真正對區塊內容做
「讀取側對稱型別閘門」的地方,見骨架驗證 C 讀取側毒藥向量表)。

**結論(推論,綜合上述兩項已測事實推導,非單獨一次量測)**:`blocks` 額外條目的
**位元組內容**,目前的設計下**不會被任何一道型別閘門檢查**——既不是信封層閘門
(視為葉節點,不開封)、也不是逐區塊閘門(從未被選中解碼)。它比區域一更深一層:
區域一至少保證「危險型別擋得住,只有一般資料能夾帶」;區域二連這個保證都沒有——
一段完整編碼的危險型別位元組理論上可以原封不動地放在一個孤兒 `blocks` 條目裡,
不觸發雜湊不符,也不觸發任何一道型別閘門的拒絕。**但需同時說清楚它不是一個可
即時利用的攻擊面**:因為這段位元組從未被 `bytes_to_var()` 解碼,危險型別本身
不會被「解出」成活體 `Signal`/`RID` 控代碼或空殼 `Callable`——它就是一段永遠
不會被讀取的死位元組,除非未來實作變更成「遍歷 `blocks` 全部鍵」而非「只依
`block_manifest` 選讀」,那時才需要重新評估是否要在該路徑上補一道閘門(這是
針對假設中的未來變更的推論,不是對現行程式碼的宣稱)。

###### 兩塊區域的風險輪廓對照

| | 區域一:信封層未知額外鍵 | 區域二:`blocks` 額外條目 |
|---|---|---|
| 受頂層雜湊涵蓋 | ❌ 否 | ❌ 否 |
| 受型別閘門涵蓋(危險型別會被擋) | ✅ 是 | ❌ 否(內容從未被開封檢查) |
| 可夾帶什麼而不觸發任何檢查 | 一般資料(字串/數字/陣列) | 任意位元組,理論上包含危險型別的完整編碼 |
| 該危險型別是否會被「解出」成活體控代碼 | 不適用(已被閘門擋下,不會寫入/讀出) | 不會——除非未來變更讀取路徑改為遍歷全部 `blocks` 鍵 |
| 目前性質 | 未受完整性保護的「合法資料」旁路 | 未受完整性保護**且**未受型別檢查的「不透明位元組」盲區,目前不可觸發解碼 |

##### 尚待實作階段裁決的問題(已併入本清單)

以下三項登記為本清單新項目,待實作階段裁決:

- 是否要把頂層雜湊的涵蓋範圍擴大到包含 `blocks` 的鍵集合(可偵測「多了/少了
  一個區塊」而不必依賴 S4 的個別守衛)?
- 若日後任何程式碼路徑改為遍歷 `blocks` 全部鍵(而非只依 `block_manifest` 選讀),
  是否需要在該路徑上額外掛一道型別閘門(現行 `deserialize_block()` 那一道只在
  被 manifest 選中時才會執行)?
- 信封層是否需要一個「已知但未使用」的稽核記錄(例如記錄下有哪些未知鍵/孤兒
  區塊被忽略),供人工排查存檔異常時使用?這不是安全性要求,是可觀測性要求。

**威脅模型範圍註記**:以上分析全部在單機、無連線威脅模型下進行——攻擊面是本機
存檔檔案被使用者本人或本機其他程式修改,不涉及網路傳輸或多人連線。所有引用的
量測結果均取自 debug/headless 建置,release build 下是否維持一致**未查證**
(`core-serialization.md`「未查證」第 1 項;`save-format-skeleton-2026-08-21/README.md`
「(d) 未查證」第 1 項),本文不宣稱這些行為在 release build 下同樣成立。


---

## 修訂記錄

| 日期 | 變更 |
|---|---|
| 2026-08-24 | 新增第七節。來源:ADR-0003 Consequences → Negative 的組合後果分析(security-engineer 執行,經兩道覆核裁定為延後項) |
| 2026-08-21 | 建立。來源:四支引擎探針 + 一次被駁回的修訂草案 + 一個可執行的設計骨架 + 一次跨行程實測 |
