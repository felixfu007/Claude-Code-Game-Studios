# 架構追溯索引

最後更新:2026-08-19(第四輪 `/architecture-review` 於 ADR-0005 修訂後重推 19 列游標需求;其餘 111 列自第三輪起零改動,依構造沿用)
引擎:Godot 4.7.1

## 涵蓋率總覽

| 狀態 | 首輪(ADR-0001) | 第二輪(ADR-0001~0004) | 第三輪(ADR-0001~0005) | **第四輪(ADR-0005 修訂後)** |
|---|---|---|---|---|
| ✅ 已涵蓋 | 5(4%) | 50(38%) | 61(47%) | **65(50%)** |
| ⚠️ 部分涵蓋 | 16(12%) | 24(18%) | 34(26%) | **33(25%)** |
| ❌ 缺口 | 109(84%) | 56(43%) | 35(27%) | **32(25%)** |
| 需求總數 | 130 | 130 | 130 | **130** |

| GDD | 層級 | GDD 狀態 | 需求數 | ✅ | ⚠️ | ❌ |
|---|---|---|---|---|---|---|
| affinity-data-pool.md | Foundation | Approved | 24 | 22 | 2 | 0 |
| save-system.md | Foundation | Approved | 30 | 22 | 7 | 1 |
| cursor-highlight-state.md | Foundation | Approved | 19 | **13** | **6** | **0** |
| tactical-combat-system.md | Core | **未 Approved** | 43 | 5 | **13** | **25** |
| game-concept.md(跨系統) | — | — | 14 | **3** | **5** | **6** |

> 逐列標記由 `/architecture-review` **獨立於 ADR 撰寫脈絡**重新推導,不採信 ADR 自身「Requirements Addressed」表的宣稱。
>
> - **第二輪**(2026-08-18):7 項 `TR-save-*` 降為 ⚠️、`TR-save-030` 判為 ❌ —— 推翻三份文件「30 項全覆蓋」的宣稱。
> - **第三輪**(2026-08-19):19 項 `TR-cursor-*` 全部由 ❌ 轉出。**ADR-0005 自陳 16 完整 / 3 部分,獨立重推為 11 完整 / 8 部分** —— `-001`/`-008`/`-015`/`-017`/`-019` 五項由 ✅ 降為 ⚠️,其中 `-008`(違反 AC-52 的定序缺口)與 `-010`(失焦當幀緩衝事件仍被裁定)為 **BLOCKING** 級。`TR-tactical-024`/`-025` 因 ADR-0005 機制十定案介面側而由 ❌ 升為 ⚠️。5 份 GDD 自第二輪以來零修改,130 項基線未動。詳見 `architecture-review-2026-08-19.md`。
> - **第四輪**(2026-08-19,ADR-0005 修訂後):`-010`/`-019` 由 ⚠️ 升 ✅ → **13 完整 / 6 部分 / 0 缺口**。修訂關閉第三輪 9 項中的 6 項(F5 兩項 BLOCKING 之一徹底解決),但 **F1 只關一半**(步驟二/三仍與 GDD 四步序列相反,R4-1),且**修法本身引入 3 項新缺陷**(R4-2 `diagnostic_seed_position()` 讀取未宣告識別符、R4-3 呈現層平滑器對上升方向也限速、R4-4 重入閘門可能鎖死 `arbitrate_frame()` 自己)。`TR-cursor-015` 的兩項落差因未被第三輪編號而完全未處理。5 份 GDD 與 ADR-0001~0004 自第三輪以來 git 改動數皆為 0,其餘 108 列依構造不動。**另修正第三輪的傳播遺漏**:`TR-concept-005`/`-006`/`-007` 三列在 ADR-0005 已存在的情況下仍留著第二輪的「架構層 ADR 未見」判定,本輪重推為 ✅/⚠️/✅。詳見 `architecture-review-2026-08-19-round4.md`。

本檔案是 `docs/architecture/architecture-review-2026-08-19.md`(及前兩輪)背後的逐條細節。供 story 引用的穩定 TR-ID 在 `docs/architecture/tr-registry.yaml`(該檔案的需求文字較精簡,完整文字與涵蓋理由以本檔案為準)。
---

## 完整矩陣 — 戰棋移動與交戰系統(tactical-combat-system.md)— Core 層

| TR-ID | 需求 | 領域 | 涵蓋情形 |
|---|---|---|---|
| TR-tactical-001 | 棋盤必須是有限邊界的方格網,座標為整數——`Grid \ B` 列舉(AC-13)需要邊界表示法 | 資料 | ⚠️ 部分——ADR-0001 的稀疏佔位表避開了對棋盤尺寸的依賴,但沒有定義邊界結構本身 |
| TR-tactical-002 | 地形須有兩個正交的逐格屬性(`terrain_cost` ≥1、遮蔽布林值),須各自可查詢/可渲染,並有載入時驗證拒絕 `terrain_cost < 1` | 資料 | ❌ 缺口 |
| TR-tactical-003 | 逐單位 schema:HP/ATK/DEF/MP/陣營/武器分層/兩個行動旗標/存活狀態 | 資料 | ❌ 缺口 |
| TR-tactical-004 | `player_baseline_stat`(公式二輸入)全專案無擁有系統(OQ-2) | 資料 | ❌ 缺口 |
| TR-tactical-005 | 武器資料表需要載入時的結構性 + 組成性驗證(AC-3、AC-23) | 資料 | ❌ 缺口 |
| TR-tactical-006 | `reachable_set(u, ignore_occupancy)` 須為單一函式;移動合法性只能消費集合 A,不得用 B(AC-14) | 資料 | ❌ 缺口 |
| TR-tactical-007 | 移動展開須為考慮成本的 Dijkstra 類演算法,不可用 BFS(地形成本非均一) | 資料/演算法 | ❌ 缺口 |
| TR-tactical-008 | `reachable_set` 須為有界前緣展開、在 MP 耗盡時提前終止,不得先展開全盤再過濾 | 效能 | ❌ 缺口 |
| TR-tactical-009 | 雙 Dijkstra 掃描的移動範圍刷新須壓進 16.6ms 影格預算(尚未驗證的假設) | 效能 | ❌ 缺口 |
| TR-tactical-010 | `threat_range_all(E)` 成本為 N 倍單位成本;敵方單位數上限無擁有者;效能測試須以格數×敵數兩軸參數化(OQ-16) | 效能 | ❌ 缺口 |
| TR-tactical-011 | 游標即檢視使查詢成為高頻路徑(逐輸入事件,而非逐回合) | 效能 | ⚠️ 部分——ADR-0001 以此作為拒絕深拷貝的理由,但未設定預算 |
| TR-tactical-012 | 跨幀攤分計算須合法且安全,靠版本比對 + 協程宿主生命週期防衛 | 並發 | ✅ 已涵蓋(ADR-0001 機制一) |
| TR-tactical-013 | `occupied(tile)` 須為邏輯資料結構,絕不可由節點樹或動畫狀態導出 | 資料 | ✅ 已涵蓋(ADR-0001 機制三) |
| TR-tactical-014 | 查詢輸出須等於從頭重算;過期輸出絕不可用於接受下一個玩家輸入 | 資料 | ✅ 已涵蓋(ADR-0001 board_version) |
| TR-tactical-015 | 合成查詢原子性——並存疊加圖與 N 敵聯集須共用同一份快照 | 並發 | ✅ 已涵蓋(ADR-0001 assert_same_version) |
| TR-tactical-016 | 結算步不可重入、單幀、禁 `await`/`call_deferred()`、含卡死防衛 | 並發 | ✅ 已涵蓋(ADR-0001 機制二) |
| TR-tactical-017 | 結算步內部子結構①→②a→②b(唯讀)→②c→③→④須被保留;③永遠覆蓋②b 的預測 | 並發 | ⚠️ 部分——ADR-0001 保證序列不被中斷,但未定案內部子結構/覆蓋規則本身 |
| TR-tactical-018 | `Φ` 須在①快照並由③/②b 重用;若為參照型別回傳須明確複製或用 `duplicate_deep()` | 資料 | ⚠️ 部分——ADR-0001 一般性地禁止回傳內部參照,但未涵蓋此處特定的入向複製義務 |
| TR-tactical-019 | 視線須為純格狀幾何 Bresenham 類演算法,並有完整定義的跨角規則 | 資料/演算法 | ❌ 缺口 |
| TR-tactical-020 | 曼哈頓距離用於射程/移動(專案通用),Bresenham 幾何用於視線——絕不混用 | 資料 | ❌ 缺口 |
| TR-tactical-021 | 決定性三層 tie-break 選路;移動動畫須精確走過預覽路徑 | 並發 | ❌ 缺口 |
| TR-tactical-022 | 全部公式與選路須決定性,含跨引擎 tick;集合型輸出須用集合相等而非序列相等 | 並發 | ⚠️ 部分——ADR-0001 涵蓋自身機制的決定性,不涵蓋 OQ-16 留待日後的演算法層 |
| TR-tactical-023 | 陣亡處理須保證對好感度數值池的通知恰好一次、陣營閘控、非「已接受」回傳時重試 | 資料/跨系統 | ⚠️ 部分 — ADR-0002 機制三提供 notify_death() 冪等拒絕與 5 主角鍵域(型別層陣營閘控),但「恰好一次」呼叫保證與非接受回傳時的重試邏輯屬戰棋側,仍無 ADR |
| TR-tactical-024 | 游標目標失效重驗:回傳值須被檢查,過期目標須保留持久化的延後重驗狀態 | 資料/跨系統 | ⚠️ 部分——ADR-0005 機制十已定案介面側(`mark_pending_reresolve()` 回傳結構化 `MarkResult`,絕不靜默)。**戰棋側的消費仍無 ADR**:回傳值須被檢查、過期目標須保留持久化的延後重驗狀態 |
| TR-tactical-025 | 輸入閘控委派給游標系統的有效性旗標,須在面板更新/確認鍵處理前查詢 | 輸入 | ⚠️ 部分——ADR-0005 機制十已定案查詢側(`is_current_target_valid()` + `get_device_authority()`,兩者拒絕回饋可區分)。**戰棋側「須在面板更新/確認鍵處理前查詢」仍無 ADR**;且受 F1 的 AC-52 定序缺口直接影響 |
| TR-tactical-026 | 全手把平權為每個操作皆須滿足的原則;與 Godot 4.7 裝置 ID 重新編號有交集 | 輸入/平台 | ❌ 缺口 |
| TR-tactical-027 | 需要回饋事件契約/列舉,以區分拒絕、靜默、與合法的 0 傷害結算(OQ-17) | 輸入/資料 | ❌ 缺口 |
| TR-tactical-028 | 疊加圖架構須支援同時合成(移動+威脅),每個狀態須有非色彩通道,繪製呼叫數 <1000 | 渲染 | ❌ 缺口 |
| TR-tactical-029 | 疊加圖實作須提供可查詢的計算後格集合 getter,供測試用,而非依賴 `_draw()` 輸出 | 渲染/測試 | ❌ 缺口 |
| TR-tactical-030 | 傷害路徑須回傳完整拆解(ATK/DEF/Φ/結果/預計剩餘 HP),須能容忍不設上限的 Φ | 資料/渲染 | ❌ 缺口 |
| TR-tactical-031 | 預判模式須可證明零寫入、絕不快取、每次游標進入重算、為物理上獨立的元件 | 資料/渲染 | ❌ 缺口 |
| TR-tactical-032 | 呈現層與邏輯層須以有序視覺事件佇列解耦;邏輯絕不等待動畫 | 渲染/並發 | ⚠️ 部分——ADR-0001 禁止動畫驅動邏輯,但未定案呈現層事件排序機制本身 |
| TR-tactical-033 | 回合邊界的行動旗標重置須為獨立事件,不得是狀態轉換的副作用 | 資料 | ❌ 缺口 |
| TR-tactical-034 | **沒有任何系統擁有全域回合結構**(階段轉換、勝負判定時機),儘管 Core Rules #9 消費它 | 資料/架構 | ❌ 缺口 |
| TR-tactical-035 | 需要回合層級的名冊查詢介面供 #10 渲染;持久化 vs. 可切換面板尚未定案(OQ-21) | 資料/輸入 | ⚠️ 部分——版本戳記機制一般性地涵蓋其活性,但介面形狀未定案 |
| TR-tactical-036 | 威脅範圍介面須在一次呼叫內同時回傳扁平聯集與逐敵拆分兩個切面 | 資料 | ⚠️ 部分——ADR-0001 把聯集視為一次原子查詢,但未定案雙切面回傳形狀 |
| TR-tactical-037 | `enemy_stat_scaling` 須用 `ceil()` 絕不四捨五入;驗證須拒絕 `enemy_advantage_pct ≥ 1.0` | 資料 | ❌ 缺口 |
| TR-tactical-038 | `max_occlusion_ratio ≤ 0.25` 需要一個關卡資料驗證閘門(工具鏈決策) | 資料/工具 | ❌ 缺口 |
| TR-tactical-039 | 地形假設在單一結算步內穩定;缺乏戰鬥中地形變更的重算觸發規則(OQ-3);無不可通行地形層級(OQ-10) | 資料 | ❌ 缺口 |
| TR-tactical-040 | 戰鬥內狀態的存讀檔持久化(board_version、佔位表、行動旗標、結算進度)完全未定義 | 資料/持久化 | ❌ 缺口 |
| TR-tactical-041 | **專案中不存在任何 AI/遭遇系統**,儘管 Core Rules #9 要求敵方回合須有決策邏輯 | 資料/AI | ❌ 缺口 |
| TR-tactical-042 | 供給系統 #5 的站位/鄰接介面無資料形狀定案;`Φ_max` 交叉校準義務未定義 | 資料/跨系統 | ⚠️ 部分——ADR-0001 的機制在形狀定案後可自動適用,但介面本身未設計 |
| TR-tactical-043 | 系統 #6(技能卡牌)的效果掛鉤須滿足同步契約;可行性尚未確認(OQ-4) | 並發/跨系統 | ⚠️ 部分——ADR-0001 依賴此契約成立,但未設計掛鉤介面本身 |

---

## 完整矩陣 — 好感度數值池(affinity-data-pool.md)— Foundation 層

| TR-ID | 需求 | 領域 | 涵蓋情形 |
|---|---|---|---|
| TR-affinity-001 | Delta Log schema 固定為 5 個具型別欄位(`pair`、`m_i`、`t_i`、`c_i`、`source_i`),append-only,須用具型別類別而非 `Array[Dictionary]` | 資料 | ✅ 已涵蓋 — ADR-0002 機制二(AffinityRecord 具型別 RefCounted,5 欄) |
| TR-affinity-002 | 須提供依配對索引的儲存,查詢複雜度 `O(n_p + m)`——這是 GDD 層級已定案的契約,非架構師可自由裁量 | 資料/效能 | ✅ 已涵蓋 — ADR-0002 機制二(per-pair 索引 Dictionary + c_now 對刻度標記線性計數) |
| TR-affinity-003 | 所有 Dictionary 鍵須為值型別,絕不可為 Object/Resource 參照(GDScript 參照相等陷阱) | 資料/引擎能力 | ✅ 已涵蓋 — ADR-0002 機制二/七(enum 值型別鍵;權杖為單調遞增 int,非物件身分) |
| TR-affinity-004 | 兩個彼此獨立的全域單調計數器(`t_now`、戰役刻度),皆為衍生值,不得另存 | 資料/狀態擁有權 | ✅ 已涵蓋 — ADR-0002 機制二(_t_now 為執行期快取,不獨立持久化) |
| TR-affinity-005 | 獨立的戰役刻度標記列表;`c_now(t_query)` 不得由 Delta Log 的最大 `c_i` 推導 | 資料/持久化 | ✅ 已涵蓋 — ADR-0002 機制二(_campaign_tick_marks 獨立附加,不由 _records 反推) |
| TR-affinity-006 | 獨立的陣亡標記表,讀寫皆須 O(1),不得影響 TR-affinity-002 的複雜度保證 | 資料 | ✅ 已涵蓋 — ADR-0002 機制三(_death_marks 獨立 Dictionary,t_death 至多 2 次鍵查找) |
| TR-affinity-007 | 陣亡通知介面:單一方法、陣營閘控、Foundation→Gameplay 的反向依賴,不透過系統 #5 轉接 | 跨系統/介面 | ✅ 已涵蓋 — ADR-0002 機制三(notify_death 單一方法、Character 鍵域即型別層陣營閘控、不經系統 #5 轉接);呼叫端義務見 TR-tactical-023 |
| TR-affinity-008 | 同結算步內陣亡通知與卡牌寫入的相對呼叫順序會翻轉寫入合法性——順序須決定性、不可任意 | 跨系統/決定性 | ⚠️ 部分 — ADR-0002 機制三保證介面不排隊/不緩衝(必要條件),但明文「呼叫順序不由本系統裁決」,順序決定性歸戰棋系統,而 TR-tactical-023 仍無 ADR |
| TR-affinity-009 | 「前進戰役刻度」為獨立介面,呼叫方恰有一處,於戰鬥/章節開始時呼叫 | 跨系統/介面 | ✅ 已涵蓋 — ADR-0002 機制二(advance_campaign_tick 獨立介面) |
| TR-affinity-010 | 「附加記錄」介面恰有 3 個呼叫點;公開方法中僅有 2 個會改變狀態,不存在刪除/修改/清空/重排 | 介面/封裝 | ✅ 已涵蓋 — ADR-0002 機制四(唯二改變 Delta Log/刻度狀態的公開方法) |
| TR-affinity-011 | 寫入端驗證須 fail-loud、不自動糾正,涵蓋 7 類明文列出的拒絕情境 | 介面/錯誤處理 | ✅ 已涵蓋 — ADR-0002 機制四(7 類 WriteRejection 窮盡列舉,驗證順序明訂) |
| TR-affinity-012 | 三個讀取函數 + 公式四皆須為純函數/零副作用、決定性,並有 `0^0 := 1` 慣例 | 決定性/數值 | ✅ 已涵蓋 — ADR-0002 機制五 + Validation Criteria 第 5 項(明訂不依賴 pow() 引擎預設,顯式保證 0^0:=1) |
| TR-affinity-013 | 讀取回傳簽章須攜帶 `t_query`/`n(p, t_query)`,另附僅供 QA 用、業務邏輯不得依賴的診斷輸出 | 介面/可觀測性 | ✅ 已涵蓋 — ADR-0002 機制六(t_query/n_pair/c_now 為正式欄位;diagnostic_visited_count 標記 QA-only) |
| TR-affinity-014 | 序列化生命週期介面須為權杖式(非布林),支援並行重疊視窗 | 並發/介面 | ✅ 已涵蓋 — ADR-0002 機制七(權杖式,支援任意數量並行重疊視窗) |
| TR-affinity-015 | 權杖集合需要逐權杖的逾時/存活偵測(非整批清空),且權杖 ID 永不重新發放 | 並發/資源生命週期 | ✅ 已涵蓋 — ADR-0002 機制七(惰性逐權杖逾時清除,_next_token_id 只增不減) |
| TR-affinity-016 | **若架構階段選擇背景執行緒序列化,權杖集合本身須執行緒安全**——全專案唯一已宣告的執行緒安全義務 | 執行緒/並發 | ✅ 已涵蓋 — ADR-0002 機制七(無條件 Mutex);但條件前提已被 ADR-0004 判為否,見本輪衝突 C3 |
| TR-affinity-017 | 存檔系統須無損往返 3 份結構(Delta Log、刻度標記、陣亡標記表);儲存型別與增量持久化是兩個正交決策 | 狀態持久化 | ✅ 已涵蓋 — ADR-0002 機制八(export_state/import_state 涵蓋三份結構) |
| TR-affinity-018 | `Pair` 鍵與 `source_i` 一律以字串名稱持久化,非 enum 序數;退役名稱永久保留、CI 可檢查 | 狀態持久化/schema 演進 | ✅ 已涵蓋 — ADR-0002 機制八(轉換原語)+ ADR-0003 機制五(SaveEnumRegistry 承接退役名稱治理與 CI 檢查義務) |
| TR-affinity-019 | 本系統是反序列化語意驗證規則的唯一權威(欄位值域 + 5 條跨結構不變量) | 狀態持久化/驗證 | ✅ 已涵蓋 — ADR-0002 機制八 validate_semantics() 純函式(ADR-0003 回填修訂) |
| TR-affinity-020 | **記憶化是硬性介面契約(非優化建議)**,供每回合假設性 AI 站位評估使用;兩種快取模式不得混用 | 效能/快取 | ⚠️ 部分 — ADR-0002 Key Interfaces 明訂兩種快取鍵形狀且不得混用,但明文「呼叫方義務,本 ADR 不實作快取」;擁有者(好感度—位置連鎖系統 #5)無 GDD 亦無 ADR,硬性契約無人承接 |
| TR-affinity-021 | 讀取進入點須有依函數性質分流的條件式預設查詢時點(`t_death(p)` vs `t_now`) | 資料/介面 | ✅ 已涵蓋 — ADR-0002 機制五(條件式預設查詢時點依函數性質分流) |
| TR-affinity-022 | 公式四假設性記錄契約:嚴格遞增的虛擬 `t_new`,是 `t_query ≤ t_now` 規則的唯一合法豁免 | 介面/決定性 | ✅ 已涵蓋 — ADR-0002 機制五(speculative_read 依序指派遞增 t_new) |
| TR-affinity-023 | 「全作用域封鎖成因登記處」目前僅為文件層表格、無執行期查詢 API——架構階段須決定是否升級 | 資料/流程邊界 | ✅ 已涵蓋 — ADR-0002 機制九(裁決:維持文件層 + 窄範圍 can_write(),不建立跨系統登記處) |
| TR-affinity-024 | 選配的寫入事件通知信號(`entry_appended`)明文為實作慣例決策,非已承諾的契約 | 跨系統/引擎能力 | ✅ 已涵蓋 — ADR-0002 機制七(明文下游不得假設 entry_appended 被保留) |

---

## 完整矩陣 — 存檔系統(save-system.md)— Foundation 層

> *(2026-08-18 第二輪:本節逐列標記已重建。ADR-0003/0004 使 30 項中 22 項達 ✅,7 項 ⚠️,1 項(`TR-save-030`)仍為 ❌——本輪獨立推導的結果與 ADR-0004 自身「全部 30 項皆有覆蓋」的宣稱不符,理由逐列見下方「涵蓋情形」欄。)*

| TR-ID | 需求 | 領域 | 涵蓋情形 |
|---|---|---|---|
| TR-save-001 | **序列化格式未定案**(Resource/.tres vs 自訂二進位 vs JSON)——連鎖影響型別白名單可強制性、浮點往返、表示法決定論 | 引擎能力/資料 | ✅ 已涵蓋 — ADR-0003 機制一(var_to_bytes/bytes_to_var 二進位 Variant;拒絕 JSON 之理由即涵蓋浮點位元級往返) |
| TR-save-002 | 型別白名單須在任何實例化之前完成閘控;範圍(扁平聯集 vs. 依版本分域)未定案 | 引擎能力/安全 | ✅ 已涵蓋 — ADR-0003 機制一/七(allow_objects=false 使白名單結構性不存在,並消解 Open Question 4) |
| TR-save-003 | manifest + 版本化區塊結構,每區塊各自版本化,manifest 攜帶單一單調遞增的規則集版本號 | 資料 | ✅ 已涵蓋 — ADR-0003 機制二(ruleset_version + 逐區塊 format_version 分層結構) |
| TR-save-004 | 每槽多檔案佈局:`.dat`/`.bak`/`.prev.bak`/`.pre_migration.bak`/`.tmp`,槽與槽完全隔離 | 持久化/資料 | ✅ 已涵蓋 — ADR-0004 機制二/四(五檔案命名慣例 + 逐槽獨立鎖) |
| TR-save-005 | **一般寫入路徑為同步阻塞單執行緒——狀態為 `provisional`,且已逾越自訂解決期限(主機平台早已定案)** | 並發 | ⚠️ 部分 — ADR-0004 機制一維持同步阻塞並包一層 SaveIOBackend 抽象,但同節明文「此決策不解決 GDD Open Question 9」,主機 SDK 驗證仍待 /create-architecture |
| TR-save-006 | 遷移模型獨立決定:分步執行,每次一個版本躍遷,仍為單執行緒 | 並發 | ✅ 已涵蓋 — ADR-0004 機制五(每步一次版本躍遷,單執行緒) |
| TR-save-007 | 同槽重入不變量:拒絕同槽第二個並行呼叫並回傳「處理中」;逐槽進行中標記須在每條退出路徑無條件釋放 | 並發/狀態 | ✅ 已涵蓋 — ADR-0004 機制四(單一進入/單一釋放結構性保證) |
| TR-save-008 | 跨系統權杖式序列化生命週期介面(消費自好感度數值池),涵蓋 4 條終止路徑各自的保證 | 跨系統/並發 | ✅ 已涵蓋 — ADR-0004 機制六(路徑一/二/四結構性保證,路徑三為 GDD 自陳並接受的殘留風險) |
| TR-save-009 | 路徑四的強保證僅在 I/O 失敗以回傳碼(而非未分類例外)回報時成立 | 引擎能力/跨系統 | ✅ 已涵蓋 — ADR-0004 機制六/七(RewriteOutcome.IO_FAILED 依 SaveIOBackend 回傳值) |
| TR-save-010 | 結構化、機器可讀的結果須能區分:成功、4 類拒絕代碼、無資料、槽處理中、遷移語意成功但回寫失敗 | 跨系統/資料 | ✅ 已涵蓋 — ADR-0003 機制三定義 2 碼 + ADR-0004 機制八擴充至 4 碼,ReadResult 六種互斥結局 |
| TR-save-011 | 完整讀取路徑檢查順序已鎖定:完整性 → 版本 → 型別白名單 → 語意驗證/遷移(`VERSION_TOO_NEW` 須先短路) | 資料/正確性 | ✅ 已涵蓋 — ADR-0003 機制三步驟 1-6(順序由分層格式在位元組層面保證) |
| TR-save-012 | 雙讀取路徑,成本輪廓不同:manifest-only(不觸發遷移,豁免重入拒絕)vs. 完整讀取(觸發遷移,僅限非互動情境) | 資料/效能 | ✅ 已涵蓋 — ADR-0003 機制二(分層緩衝區)+ ADR-0004 機制十(明訂 manifest-only 豁免重入拒絕) |
| TR-save-013 | 兩層加密雜湊鏈(逐區塊 + 頂層),固定的規範順序,與寫入當下容器順序無關 | 資料/安全 | ✅ 已涵蓋 — ADR-0003 機制四(逐區塊 + 頂層 SHA-256,依 source_id 字典序) |
| TR-save-014 | 雜湊輸入須為寫入前、同一次序列化操作產生的記憶體資料,絕不可是寫入後讀回的位元組 | 資料/正確性 | ✅ 已涵蓋 — ADR-0003 機制四(雜湊輸入即寫入磁碟的同一個 PackedByteArray 物件) |
| TR-save-015 | 每一次寫入/flush/rename/刪除呼叫的成功與否須被實際檢查;`flush()` 的可檢查性在 4.7.1 未經確認 | 引擎能力/持久化 | ⚠️ 部分 — ADR-0004 機制三要求逐呼叫檢查 SaveIOBackend 回傳值(架構層已定案),但 flush() 本身可檢查性在 4.7.1 仍未驗證,降級方案 get_error() 為粗粒度累積狀態 |
| TR-save-016 | 跨平台原子置換是已鎖定的六步驟行為序列(步驟 0/1/2/3/4a/4),不留架構階段自由裁量 | 持久化/引擎能力 | ✅ 已涵蓋 — ADR-0004 機制三(含 Step 0 分支 A/B 的六步驟逐項對應) |
| TR-save-017 | 讀取端回復規則須採精確檔名比對,絕不可用萬用字元/前綴比對(三種 `.bak` 家族檔案並存) | 持久化 | ✅ 已涵蓋 — ADR-0004 機制三「讀取端回復規則」(精確檔名比對) |
| TR-save-018 | 遭拒存檔的唯讀存取入口,依命中的備份層級分三級告知誠實程度 | 跨系統/持久化 | ✅ 已涵蓋 — ADR-0004 機制九(ReadOnlyHitSource 三態 + 來源優先序) |
| TR-save-019 | 耐久性邊界:「flush」僅指引擎緩衝區沖刷至 OS 層級,非硬體層級落盤;真正的斷電耐久性可能需要 GDExtension | 引擎能力/持久化 | ⚠️ 部分 — ADR-0004 忠實記錄耐久性邊界且不誇大,但「是否採 GDExtension 取得硬體層落盤」本身未做決策(Verification Required 4) |
| TR-save-020 | 存檔位置須在 `user://`,`.tmp` 與目標檔須位於同一檔案系統(rename 原子性前提) | 引擎能力/持久化 | ✅ 已涵蓋 — ADR-0004 機制二(user:// + 同檔案系統定案為硬性設定要求) |
| TR-save-021 | 遷移函數須為純函數、`O(該區塊自身筆數)`;後置條件檢查須逐步驟執行,非僅驗最終輸出 | 資料/效能 | ⚠️ 部分 — ADR-0004 機制五涵蓋逐步驟後置條件檢查,但遷移函數的純函數性與 O(該區塊筆數) 委派給各擁有系統提供,無架構層強制機制 |
| TR-save-022 | 語意驗證器為 fail-closed:manifest 列出無對應驗證器宣告的區塊,本身即視為 `DATA_CORRUPTED` | 資料 | ✅ 已涵蓋 — ADR-0003 機制六(get_validator 回傳 null 即 DATA_CORRUPTED,fail-closed) |
| TR-save-023 | 持久化的 enum 一律以字串名稱 + 對照表表示;退役名稱永久保留,須有 CI 可強制的重用檢查 | 資料/工具 | ✅ 已涵蓋 — ADR-0003 機制五(SaveEnumRegistry 介面形狀 + CI 檢查必須存在;檢查實作留 /create-architecture) |
| TR-save-024 | 遷移完成標記的寫入責任分散於 3 種寫入路徑類型;須被涵蓋於頂層雜湊輸入 | 資料/持久化 | ✅ 已涵蓋 — ADR-0003 機制二/四(migration_completion_marker 位於 block_manifest tuple,已查證涵蓋於頂層雜湊輸入)+ ADR-0004 機制七 |
| TR-save-025 | 可觀測性介面分兩類:診斷用(僅 QA,含主執行緒斷言)vs. 生產用(進度查詢,豁免重入拒絕) | 跨系統/工具 | ✅ 已涵蓋 — ADR-0004 機制十(診斷/生產分類表 + 四項診斷介面) |
| TR-save-026 | 效能量測儀器化:三桶一般寫入耗時、六桶遷移讀取耗時、加性下限成本模型、無界重跑鏈風險、3×/4× 容量 | 效能 | ⚠️ 部分 — ADR-0004 機制十提供遷移呼叫計數器與順序記錄,但未涵蓋三桶一般寫入耗時/六桶遷移讀取耗時的儀器化、加性下限成本模型、無界重跑鏈風險判準 |
| TR-save-027 | 「重複回寫失敗」計數逐槽獨立、不持久化,於成功回寫或槽被覆寫時歸零;唯讀存取不影響此計數 | 狀態 | ✅ 已涵蓋 — ADR-0004 機制十一(RepeatedFailureTracker 逐槽、不持久化、唯讀存取不影響計數) |
| TR-save-028 | 巢狀 Resource 的深層複製紀律,共享地形子資源需要 `duplicate_deep(Resource.DEEP_DUPLICATE_ALL)`——確切 enum 名稱未經查證 | 引擎能力/資料 | ⚠️ 部分 — ADR-0003 機制一拒絕 Resource 使存檔側曝險消失,但地形側深拷貝紀律明文「降級為未來系統自身責任」,隨 TR-concept-001/003 未決 |
| TR-save-029 | 維運層級重跑(自動痊癒路徑二)須以創世存底重跑 + append-only 拼接方式和解,重用標準寫入管線、不走特權路徑 | 持久化/工具 | ⚠️ 部分 — ADR-0004 機制七路徑二保證資料前提(創世存底、完成標記)成立,但觸發機制與 append-only 拼接程序明文留給 /create-architecture |
| TR-save-030 | 雲端存檔同步(Steam Cloud)與多檔案原子性的互動未定義 | 持久化/基礎設施 | ❌ 缺口 — ADR-0004 雖列於 Requirements Addressed 表,同格說明文字明文「本 ADR 不解決此問題」;無任何 ADR 涵蓋 |

---

## 完整矩陣 — 單一游標/高亮狀態系統(cursor-highlight-state.md)— Foundation 層(**第四輪:13 ✅ / 6 ⚠️ / 0 ❌**;「全部需求皆為缺口」是第二輪的標題,自第三輪起已不成立)

| TR-ID | 需求 | 領域 | 涵蓋情形 |
|---|---|---|---|
| TR-cursor-001 | 全域狀態恰為 3 個已認定的頂層欄位;擁有節點的生命週期須涵蓋所有使用本系統的畫面(Autoload 類機制) | 資料/架構 | ⚠️ 部分——機制一 Autoload 薄殼 + DI 核心。**第四輪**:第三輪的條件式待決已由修訂補上 `diagnostic_seed_position()` 關閉,方向正確;但該方法寫在抽象基底 `MouseReclaimPolicy` 內卻 `return _seed`,而 `_seed` 只宣告於子類別 `ThresholdMouseReclaimPolicy` → **編譯期錯誤**(R4-2,BLOCKING,修法一行)。關閉本項的機制本身不成立,故維持 ⚠️ |
| TR-cursor-002 | 「表面類型」標籤須為單一集中定義的共用 enum;實作位置與擁有者未定案 | 資料 | ✅ 已涵蓋——機制二 `CursorTypes` 包裝類別,沿用 ADR-0002 已查證的 `AffinityTypes` 先例。**關掉自 GDD 第四輪起懸置的問題** |
| TR-cursor-003 | 同一標籤在任一時刻至多一個掛載實例;需要一個註冊/發現機制 | 資料/UI 架構 | ✅ 已涵蓋——機制三 `register()` 對已占用標籤回傳 `DUPLICATE_TAG_REJECTED` 不覆寫;迭代經 `registered_surfaces_sorted()` |
| TR-cursor-004 | 裝置權威分類須依原始 `InputEvent` 子類別,絕不可讀取 `.device`/裝置 ID | 輸入/引擎 | ✅ 已涵蓋——機制四 `classify()` 純子類別 match,結構性不觸及 `.device`。引擎專家 **CONFIRMED** 與 4.7 裝置 ID 重編號無交集 |
| TR-cursor-005 | Input Map 約束:除非語意上為懸停/游標移動,`ui_*` action 不得綁定滑鼠輸入;須於載入時驗證 | 輸入/設定驗證 | ✅ 已涵蓋——機制七(a) `CursorStartupValidator`;執行期重新綁定明文排除(專案尚無該系統) |
| TR-cursor-006 | 須緩衝整幀的 `InputEvent` 並在收集完成後才裁決;須掛在 `_input()`,絕不可用 `_unhandled_input()` | 輸入/引擎架構 | ✅ 已涵蓋——機制五 `_input()` 只 append、`_process()` 統一裁決。引擎專家 **CONFIRMED** `_input()` 為正確做法 |
| TR-cursor-007 | 專案層級「Agile Event Flushing」必須保持關閉——整個一幀一批次的假設依賴於此 | 引擎/建置設定 | ✅ 已涵蓋——機制七(b) 載入期驗證 + `has_setting()` 防衛(鍵不存在回報 UNKNOWN 而非通過)。**參考庫對鍵字串零命中,防衛設計因此正確** |
| TR-cursor-008 | 四個行為者的決定性同幀執行順序;需要具體的 `process_priority` 數值 | 時序/引擎架構 | **⚠️ 部分**——**第四輪**:F1 只關一半。機制六已補第②行為者(呼叫方主動改標,−50)與對下游的明文約束,AC-52 的 THEN 面成立;但步驟三(緩衝內導覽寫入)仍融在 −100,實際定序為 **1&3 → 2 → 4**,與 GDD 明文的 1→2→3→4 相反,且 GDD 稱該定序方向為硬性行為要求(R4-1,視同 BLOCKING;②→③ 這組無任何測試涵蓋)。N1/N4 已關閉,但衍生 R4-4(重入閘門可能鎖死 `arbitrate_frame()` 自己)、R4-5(`ActionClass` 白名單靜默降級)、R4-6(`call_deferred()` 路線不等價) |
| TR-cursor-009 | 滑鼠奪權門檻數學:逐表面類型的像素常數、淨位移非路徑總和、根視窗座標空間 | 輸入/資料驗證 | ⚠️ 部分,**判定與 ADR 一致**——**第四輪**:F2 已關閉(`evaluate()` 改收目前滑鼠座標、策略內部持 `_seed` 相減,結構性杜絕路徑總和;單一根 Viewport 假設已明文寫入 Constraints)。門檻數學本身仍在使用者裁決凍結中,不因介面修好而變完整。引擎專家更正:機制八的淨位移全程停留在 viewport 座標系,**不受 `CanvasLayer` 變換影響**,VR #11 應拆項 |
| TR-cursor-010 | 累積器須依裝置權威 + OS 焦點閘控;須掛 `NOTIFICATION_APPLICATION_FOCUS_*`;暫停/彈窗讓出機制留待架構階段 | 時序/引擎 | **✅ 已涵蓋(第四輪由 ⚠️ 升)**——F5 完整關閉:機制五 `_process()` 補上 `_arbitration_suspended` 檢查,機制九四個進出點(`suspend_arbitration()`/`resume_arbitration()`/FOCUS_OUT/FOCUS_IN)全數 `_frame_events.clear()`。修訂另自行補上一項相鄰缺口(`resume_arbitration()` 原未比照 FOCUS_IN 重新播種)。N2 已登記為 Verification Required #10 並排入 Day-1 spike |
| TR-cursor-011 | **已知確認、尚未修復的永久鎖死缺陷(持續按住方向輸入)**;已降級為建議項但架構層面仍未解決 | 輸入/架構風險 | ⚠️ 部分,**刻意如此,判定與 ADR 一致**——機制八隔離在單一檔案,明文不宣稱已緩解/不宣稱降低嚴重度/不宣稱構成圍堵。使用者第十二輪裁決:重新設計暫停、待手把硬體 |
| TR-cursor-012 | 寫入介面「設定新目標」:雙輸入簽章,不含碰撞箱幾何,自動清除有效性旗標 | 跨系統 API | ✅ 已涵蓋——機制十 `set_target(target, from_ui_action) -> SetTargetResult`;幾何查詢自 GDD 第九輪起已不存在 |
| TR-cursor-013 | 寫入介面「標記待重新解析」:須回傳結構化的已套用/已過期結果,絕不靜默 | 跨系統 API | ✅ 已涵蓋——機制十 `mark_pending_reresolve(expected) -> MarkResult`,`STALE_NOT_APPLIED` 為明確回傳值 |
| TR-cursor-014 | 讀取介面:有效性旗標查詢(閘控確認動作)+ 裝置權威查詢(閘控滑鼠點擊確認),兩者拒絕回饋須可區分 | 跨系統 API/UI | ✅ 已涵蓋——機制十刻意分兩個獨立查詢而非合併布林。論證正確:兩種拒絕的正確補救動作相反 |
| TR-cursor-015 | 卸載前目標交接義務,涵蓋存檔讀取整批替換的甲/乙/丙分支 | 跨系統/生命週期 | ⚠️ 部分——機制十一涵蓋甲/乙/丙三分支與 `from_ui_action=false`,但**漏掉 GDD Core Rules #7 明訂的「累積位移量於甲/乙兩分支皆重置為 0」**;丙分支寫成無條件重算,GDD 允許原目標仍有效時直接沿用。**第四輪:兩項完全未處理**——第三輪未把它們編為 F/N 項,修訂 session 依 9 項清單作業因而漏掉。丙分支的收窄牴觸本 ADR 自己 Ordering Note 的單向修訂約束 |
| TR-cursor-016 | **全域每裝置待機指示元件須存在於每個畫面**;現有候選擁有者皆為畫面範圍,無一符合 | UI/擁有權缺口 | ✅ 已涵蓋——機制十二 Autoload 持有的全域 `CanvasLayer`。**結構性關掉擁有權缺口**:成因是需求本身排除了任何畫面範圍擁有者。視覺樣式仍留 `/art-bible` |
| TR-cursor-017 | 原生游標須在權威≠滑鼠時隱藏,唯一例外是連續漸變的奪權回饋——**連續透明度動畫在 4.7.1 可能不受原生游標支援** | 引擎能力/UI | ⚠️ 部分——原生指標二元隱藏已涵蓋。**第四輪**:F3 已加呈現層平滑器 + `reset_triggered` 訊號(方向正確,觸發點 (d) 正確 snap),N3 已由機制十三之二處理且正確地把設計裁決退回 GDD;但 `move_toward()` **對上升方向同樣限速**,而 GDD 要求「達到門檻的當下透明度達 100%」且收斂上限的管轄範圍原文只涵蓋重置造成的下降 → 常見門檻(50–100px)與常見滑鼠速度下**結構上必然違反**(R4-3,視同 BLOCKING) |
| TR-cursor-018 | 全鍵盤/手把平權;已註冊表面不得使用原生 Control 焦點/懸停主題(4.6 雙焦點分離) | 平台/輸入 | ✅ 已涵蓋——機制十四**兩項**條件(`FOCUS_NONE` **加上**根 Control 不得帶內建 hover 主題)。引擎專家覆核同意兩條管線獨立、同意「不移除只降級」。**仍為專案級風險,不限本系統** |
| TR-cursor-019 | 交接視覺延遲硬性上限(最多 1 幀)與奪權收斂上限,皆需要幀精準的量測機制 | 時序/UI | **✅ 已涵蓋(第四輪由 ⚠️ 升)**——交接延遲 ≤1 幀的儀器(`diagnostic_last_authority_change_frame`)原本即正確;F4 已關閉,`diagnostic_reclaim_progress_history` 改為取樣自繪節點的 `_presented_alpha`(呈現值),與 `reclaim_visual_convergence_max_frames` 所約束的對象一致 |

---

## 完整矩陣 — game-concept.md(跨系統,第 224–456 行)

| TR-ID | 需求 | 領域 | 涵蓋情形 |
|---|---|---|---|
| TR-concept-001 | 活棋盤場景架構模型未定案:原地替換規則資料 vs. 整場切換場景——影響存檔相容性與 UI 圖層耦合 | 引擎能力/場景架構 | ❌ 缺口 |
| TR-concept-002 | 存檔格式須從初始設計就內建跨規則集版本遷移能力 | 狀態持久化/資料遷移 | ✅ 已涵蓋 — ADR-0003 機制二(ruleset_version + 逐區塊 format_version 自初始設計即內建)+ ADR-0004 機制五(分步遷移鏈) |
| TR-concept-003 | 若地形資料採 Resource 子類別,深層複製紀律(`duplicate_deep()`)須與存檔格式一併設計,不得事後補救 | 資料/引擎能力 | ⚠️ 部分 — ADR-0003 機制一拒絕 Resource 作為存檔格式,存檔側不再曝險;地形資料本身是否採 Resource 子類別隨 TR-concept-001 未決 |
| TR-concept-004 | 即時預覽重算範圍已鎖定:逐格候選預覽僅為 O(n)(移動單位涉及的配對),全盤 O(n²) 重算僅於落子確定時執行一次 | 效能/跨系統 | ⚠️ 部分——ADR-0001 的結算邊界機制可作為「落子時重算一次」的觸發依據,但 O(n) 預覽範圍界定本身未定案 |
| TR-concept-005 | 棋盤格高亮不得依賴原生 Control 焦點/懸停主題;單一游標格狀態源須一般化至所有具懸停目標的 UI 表面 | 輸入/平台平權 | ✅ 已涵蓋(**第四輪修正第三輪的傳播遺漏**)——ADR-0005 機制十四(`FOCUS_NONE` + 根 Control 不得帶內建 hover 主題,兩項條件)封住原生 focus/hover 兩條管線;一般化由機制二 `CursorTypes.SurfaceType` 列舉 + 機制三登記制承載。第三輪已引入 ADR-0005 卻未回頭重推本列(理由欄仍留第二輪的「架構層 ADR 未見」) |
| TR-concept-006 | 裝置權威為兩層機制:`ui_*` action 為權威閘門,原始 InputEvent 子類別做裝置分類;Input Map 絕不可讓 `ui_*` 綁滑鼠 | 輸入/平台需求 | ⚠️ 部分(**第四輪修正第三輪的傳播遺漏**)——兩層機制由 ADR-0005 機制四(裝置分類,結構性不讀 `.device`)+ 機制四之二(`ActionClass` 語意分類)承載,Input Map 約束由機制七(a) 載入期驗證;但語意分類是硬編碼六項白名單,新增導覽類 action 會靜默降級為 `OTHER` 且無任何驗證攔截(R4-5) |
| TR-concept-007 | 初始游標狀態(純手把、首次輸入前)需要呼叫方注入初始落點的介面,不由游標系統自行決定 | 跨系統/輸入 | ✅ 已涵蓋(**第四輪修正第三輪的傳播遺漏**)——ADR-0005 機制十 `set_target(target, from_ui_action)` 即為呼叫方注入介面(`from_ui_action=false` 時裝置權威不變),機制十一乙分支明文複用 Core Rules #6 的初始目標設定流程 |
| TR-concept-008 | 4.7 shader 前處理器限制收緊,可能影響好感度連線發光效果 | 引擎能力(渲染)/效能 | ❌ 缺口——引擎專家已收窄範圍,若無共用 shader include 則機率低 |
| TR-concept-009 | MVP 驗收協議 1 需要正式的預判標記狀態 + 遙測管道,比對標記與實際結算結果 | 跨系統/遙測/狀態 | ⚠️ 部分——「實際結算」的比較基準須錨定 ADR-0001 的已提交結算邊界事件,但遙測管道本身是缺口 |
| TR-concept-010 | MVP 驗收協議 2/5 需要逐回合可序列化的具名存活單位鄰接圖快照 | 資料結構/狀態持久化/遙測 | ❌ 缺口 |
| TR-concept-011 | 5 張手工 MVP 關卡需要一套工具鏈量測 4 項空間指標(含路徑搜尋 + 視線查詢),且須能在地形演變後重跑 | 工具/資料結構/效能 | ❌ 缺口 |
| TR-concept-012 | 專案級 forbidden pattern:禁止連線功能、禁止程序化地形生成(須手工編寫、可版本化、由劇情觸發) | 平台/資料結構 | ❌ 缺口——尚未登記到 `docs/registry/architecture.yaml` |
| TR-concept-013 | 戰鬥結算需要可查詢的「淨聯動總分」聚合值,要求決定性、可重現的結算以支援 A/B 對照 | 決定性/跨系統 | ⚠️ 部分——ADR-0001 保證結算邊界原子性,但聚合查詢介面與可重現對照機制是缺口 |
| TR-concept-014 | 專案級 forbidden pattern:戰鬥結算路徑絕不可使用 RNG | 決定性 | ❌ 缺口——尚未登記到 `docs/registry/architecture.yaml` |

---

## 需要 ADR 的已知缺口(優先順序清單見 `architecture-review-2026-08-19.md` 的「建議的 ADR 清單」)

*(2026-08-18 第二輪更新:第 1–3 項的 ADR 已撰寫且**已經該輪獨立驗證**——ADR-0002 對 24 項 `TR-affinity-*` 無缺口;ADR-0003/0004 對 30 項 `TR-save-*` 為 22 ✅ / 7 ⚠️ / 1 ❌,故第 3 項改為部分完成並新增第 3a 項。)*

*(**2026-08-19 第三輪更新**:第 4 項的 ADR-0005 已撰寫且已本輪獨立驗證 —— 11 ✅ / 8 ⚠️ / 0 ❌,**零缺口,第二輪 FAIL 的唯一成因解除**;但含 2 項 BLOCKING 級缺陷,見下方。**5 份 ADR 皆仍為 `Proposed`,未 `Accepted` 前引用它們的 story 會被自動阻擋。** 第 5–6 項仍尚未動筆,是目前投入產出比最高的動作 —— 第 5 項一次涵蓋 25 項 ❌ 中的大部分。)*

1. ~~好感度數值池資料結構與並發契約(TR-affinity-001 至 -024)~~ → **已撰寫**:`adr-0002-affinity-data-pool-data-structure-and-concurrency-contract.md`(Proposed)
2. ~~存檔系統序列化格式與型別安全(TR-save-001、-002 及其下游)~~ → **已撰寫**:`adr-0003-save-system-serialization-format-and-type-safety.md`(Proposed)
3. ~~存檔系統原子寫入與遷移執行模型(其餘 TR-save-*)~~ → **已撰寫**:`adr-0004-save-system-atomic-write-and-migration-execution-model.md`(Proposed)——但**未達無缺口**,見第 3a 項
3a. **存檔系統殘留缺口**(本輪新增):`TR-save-030`(雲端存檔同步 × 多檔案原子性)為 ❌,無任何 ADR 涵蓋;`TR-save-005/-015/-019/-021/-026/-028/-029` 為 ⚠️,其中 `-026`(耗時儀器化三桶/六桶 + 加性下限成本模型)缺口最實質。多數可於 `/create-architecture` 或 ADR-0004 修訂時收攏,`-030` 依 GDD 明文須待平台策略定案
4. ~~單一游標裝置權威輸入架構(TR-cursor-001 至 -019)~~ → **已撰寫**:`adr-0005-cursor-device-authority-input-architecture.md`(Proposed)——第三輪獨立重推 11 ✅ / 8 ⚠️ / **0 ❌**(見第 4a 項);**2026-08-19 修訂後第四輪重推為 13 ✅ / 6 ⚠️ / 0 ❌,新增 7 項待修訂,見第 4b 項**
4a. **ADR-0005 待修訂項**(第三輪新增,共 9 項):**F1(BLOCKING)** 機制六的四行為者非 GDD 的「四方完整定序」,呼叫方主動改標被排到確認讀取之後,違反 AC-52,且修法需新增對下游系統的掛載紀律約束;**F5(BLOCKING)** `_process()` 未檢查 `_arbitration_suspended` + 四個進出點未清空 `_frame_events`(不依賴任何引擎假設的確定性漏洞);**F2** `MouseReclaimPolicy` 對累積起點的擁有權自相矛盾、參數命名邀請 GDD 禁止的路徑總和;**F3** 呈現層平滑器缺失,對重置觸發點 (a)(b)(c) 必然違反 AC-41(修法會擴充「三方法契約」);**F4** 收斂上限量測儀器量錯對象(隨 F3 解決);**N1** `ActionClass` 語意分類機制缺失且該 `InputMap` 依賴漏列於涵蓋率表;**N2** `_notification()` 時序未列 Verification Required;**N3** `Input.mouse_mode` 全域性與 AC-60 未登記表面 carve-out 未調和;**N4** 輪詢 vs 訊號推送未定案且無重入閘門。**F1/F5 修好前不得進 `Accepted`**

4b. **ADR-0005 第四輪待修訂項**(2026-08-19 修訂後重推,共 7 項 + `-015` 兩項落差):**R4-1** F1 只關一半——步驟三仍融在 −100,實際定序 1&3 → 2 → 4,與 GDD 明文的四步序列相反,且 ②→③ 這組無任何測試涵蓋(視同 BLOCKING);**R4-2** `diagnostic_seed_position()` 寫在抽象基底卻 `return _seed`,而 `_seed` 只宣告於子類別 → 編譯期錯誤(**BLOCKING**,一行修法);**R4-3** F3 平滑器對上升方向也限速,結構上無法滿足「達到門檻的當下透明度達 100%」(視同 BLOCKING);**R4-4** N4 重入閘門可能鎖死 `arbitrate_frame()` 內部的導覽寫入(引擎專家額外發現);**R4-5** `ActionClass` 硬編碼白名單無完整性驗證,新增導覽類 action 會靜默降級;**R4-6** `call_deferred()` 路線與旗標路線不等價,可重開 F1 剛關的洞;**R4-7** ②/⑤ 角色重疊未討論(可用單一 `_process()` 內陳述順序解決,屬文件缺口)。另 **`TR-cursor-015` 的兩項落差**(甲/乙分支累積位移量未重置為 0、丙分支收窄 GDD 允許的沿用路徑)因第三輪未編號而完全未處理。詳見 `architecture-review-2026-08-19-round4.md`
5. 戰棋盤面演算法層(可達格/威脅範圍/視線)(TR-tactical-002 至 -010、-019 至 -021、-037 至 -039)—— **尚未動筆**
6. 回合結構擁有權 + 缺席的 AI/遭遇系統(TR-tactical-034、-041)—— **尚未動筆**

## 已被取代的需求

尚無。

- **首輪 → 第二輪**:4 份系統 GDD 皆未變動(已以 `git diff 890046b..HEAD -- design/gdd/` 查證,僅 `systems-index.md` 有非需求性註記變更)。
- **第二輪 → 第三輪**:5 份 GDD **全部未變動**(已查證:最新 GDD 提交為 `08bf6ff`,2026-08-18 10:16,早於第二輪審查提交 `1c3d5d0` 的 15:43;之後三個提交 `a56dd10`/`c282803`/`d54543e` 皆未觸及 `design/gdd/`)。

故 130 項需求基線與全部 TR-ID 三輪維持不變,無取代、無棄用、無改寫。
