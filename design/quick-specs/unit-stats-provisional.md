# 單位數值表(暫定)—— 第二版

> **這些是暫定值,目的是讓垂直切片跑起來以產生校準數據,不是定案。**

---

## 🔴 本檔是 `player_baseline_stat` 的正式擁有者(2026-08-31 管理者指派)

`tactical-combat-system.md` 公式二以 `player_baseline_stat` 為輸入,而該文件的 **OQ-2**
自 2026-08-17 起記載:「**全專案沒有任何 GDD 擁有它**……這不是『待別人定案』,
是**沒有人被指派**」。

🔴 **那句話是錯的,而且錯了兩週。** 這張表一直存在 —— 就是本檔第 1 節,
連同第 2、3 節的敵方推導。它沒被算成擁有者,只因為它放在 `design/quick-specs/`
底下、標題寫著「暫定」,歷次審查都把它讀成草稿而非資料的家。
**2026-08-31 管理者裁決:就地登記本檔為正式擁有者,不搬家、不複製。**

**「正式擁有者」在這裡是什麼意思**:

- ✅ **「誰負責這張表」有答案了** —— 要改主角或敵人的血/攻/防,改本檔,不改別處。
- ✅ **`/create-architecture` 對 OQ-2 的阻擋解除** —— 公式二有輸入來源了。
- ❌ **數值本身仍是暫定,這一點沒有改變。** 校準時機見
  `tactical-combat-system.md` OQ-14(首次可玩建置後的平衡校準)。
  **「有人負責」與「數字是對的」是兩件事,本次只解決前者。**
- ❌ **本檔仍不是 GDD,不進 `systems-index.md` 的系統清單。** 它是一張資料表的家,
  不是一個系統。

### ⚠️ 一項因上游核准而產生的待檢項(2026-08-31 登記)

本檔「假設與依據」節寫著 **`Φ` 總和上限 `±6`**,而 `affinity-position-chain.md`
已於 2026-08-31 核准,正式夾限為 **`−4` 到 `+12`**。

**這兩個數字不矛盾** —— `affinity-position-provisional.md` 早已明文說明 `±6` 是
「單一效果 ±3、切片只有 2 對非中立配對」的**現況後果**,並把正式 `Φ_max` 留給
好感度系統完整設計時決定。那份設計現在到了,答案是 `+12`。

**但第 5 節的對局長度推導(無 Φ 5 回合 / 有 Φ 4 回合)是建立在每回合最多 `+6` 上的。**
🔴 **那段推導在 `+12` 之下還成不成立,已初步覆算 —— 見下方「覆算結果」小節
(2026-08-31,systems-designer)。**

⚠️ **本段一度由協調者斷言「今日結論不變」(理由:切片只有 2 條關係線,實際到不了 `+12`),
該斷言已撤回。** 撤回理由:那是心算,不是量測,而且是對平衡數值下結論 ——
與同日另一次「憑心算推斷敵方數值只有兩隻照公式算」的錯誤同一種動作,那一次的結論是錯的
(實際五隻全都照公式算,只是係數分三級)。**看起來合理不等於對。**
判準見 `.claude/docs/technical-preferences.md`「(A) 的精確定義」節。

🔴 **關係資料一旦增加,本節推導必須重算,不得沿用。**
此義務已登記於 `docs/registry/architecture.yaml` 的
`phi_curve_enemy_advantage_cross_calibration` 條目。

### 覆算結果(2026-08-31,systems-designer;同日由 godot-gdscript-specialist 實機驗證覆核)

**⚠️ 證據等級聲明,先於結論**:本次覆算原計畫寫一個 headless GDScript spike,
讓引擎實際呼叫 `AffinityRules.bonus_for_at()` 跑真實資料(`assets/data/affinity/vs01_affinity_links.txt`、
`assets/data/units/vs01_roster.txt`),取得 (A) 級證據。spike 檔已寫好,放在
本次工作階段的 scratchpad,**但本次工作階段沒有可用的指令執行工具,spike 從未實際跑過**。
以下數字改為**直接讀取 `src/gameplay/affinity/affinity_rules.gd` 的具名常數與
`delta()` 函式本體**(3 段 if/elif 分支,窮舉,非取樣)算出,屬 **(B) 級**——
讀自專案權威原始碼與真實資料檔逐字內容,但未經引擎實際執行驗證。
**在有人以 (A) 級(實際跑 spike 或等效 GdUnit4 測試)覆核前,以下仍應視為
高信心推導,不是量測結果。**

> 🔴 **spike 沒跑成是派工錯誤,不是專家的錯,記在這裡以免重犯**:協調者要求
> `systems-designer` 執行 headless 引擎 spike,但**該 agent 的工具集不含指令執行能力**
> (Read/Glob/Grep/Write/Edit)。要跑引擎必須派給具備該能力的角色 ——
> 依 `.claude/docs/technical-preferences.md` 的檔案路由表,`.gd` 檔屬
> `godot-gdscript-specialist`。**派工前要看對方有沒有那雙手。**
>
> 🔴 **本節下方 (c) 段原含一條虛構的登記表引用,已刪除並就地說明**(見該段)。
> 這提高了整段的複核優先度:**同一份報告裡出現過一次憑空引用,其餘未經引擎驗證的
> 推導也應被當成待查,不是待採納。**

✅ **2026-08-31 補測(godot-gdscript-specialist,派工修正後執行)**:上面留存的 spike
檔已由具備指令執行能力的角色實際跑過。指令:
`"C:/Users/felixfu007/Downloads/Godot_v4.7.1-stable_win64.exe/Godot_v4.7.1-stable_win64_console.exe"
--headless --path . -s [scratchpad 路徑]/phi_ceiling_spike.gd`,**引擎結束碼 0**。
spike 直接呼叫專案自身的 `AffinityLink.links_from_text()`、`Unit.roster_from_text()`、
`AffinityRules.bonus_for()` / `bonus_for_at()` / `manhattan_distance()`,對真實資料檔
`vs01_affinity_links.txt`、`vs01_roster.txt` 求值——**未重新實作距離曲線、加總或夾限
任何一條規則**,符合本檔「(A) 的精確定義」的判準。spike 只保留在出具本次驗證的工作階段
scratchpad,依規定不進版控。**下方 (a) 的兩個核心數字(團隊上限 +10、t=0 團隊總和 +6)
由 (B) 升級為 (A) 級;(b) 新增的子集合量測同為 (A) 級;(c) 因討論的是現有 2 條線之外的
假設情境,無真實資料可測,維持 (B)/(C) 級推導,見該段說明。**

**(a) 真實資料下,每回合實際可達的 Φ 總和上限是多少?** ✅ **(A) 級,已實機驗證**

`vs01_affinity_links.txt` 目前只有兩條線:`1,2,POSITIVE,1`(甲乙)、`3,4,NEGATIVE,1`
(丙丁)。戊(5)不在任何一條線上。逐一讀 `delta()` 本體(窮舉三段:距離1、距離2、
距離≥3,其餘為 0):

| 單位 | 關係線 | 距離1 | 距離2 | 距離≥3 |
|---|---|---|---|---|
| 甲、乙(正) | 1-2 | **+3** | 0 | −1 |
| 丙、丁(負) | 3-4 | −1 | 0 | **+2** |
| 戊 | 無 | 0 | 0 | 0 |

甲/乙、丙/丁各自只有一條線,`clampi(x,-4,12)` 對範圍 `[-1,+3]` 與 `[-1,+2]` 都是
恆等操作(窮舉可證,不需夾限介入)——**本切片目前完全用不到 R7 的夾限**,不論
上游把夾限訂在 `±6` 還是 `−4~+12`,都不影響這張切片。

**每回合團隊總 Φ 的真實上限 = 3+3+2+2+0 = +12 嗎?不是,是 +10**——甲乙那條線
兩端最好情況各 +3(相鄰同時觸發等於雙倍計入,+6),丙丁那條線兩端最好情況各
+2(遠離同時觸發,+4),兩條線互不相關可同時達成,合計 **+10**,**確實高於文件
假設的 `+6`,「可達更高」成立**。原始假設把「單一角色最多兩條關係線同時觸發」
(角色層級的說法)誤用成「團隊每回合總和上限」(團隊層級的量),這是兩個不同
的量,`±6` 的推導方式本身就不適用於§5 想問的問題。

✅ **實機驗證(2026-08-31,godot-gdscript-specialist)**:上述「兩條線互不相關可同時
達成」不是心算推論,已用**單一** `positions` 快照同時令甲乙距離=1(相鄰)、丙丁
距離=10(遠大於死區門檻 3),對這同一份快照逐一呼叫 `AffinityRules.bonus_for()`:
`Φ(甲)=+3、Φ(乙)=+3、Φ(丙)=+2、Φ(丁)=+2、Φ(戊)=0`,引擎加總 **+10**,與心算結果
一致。另以「兩條線各自取最不利位置」的反向快照(甲乙拉開、丙丁貼近)量得團隊總和 **−4**。

> 🔴 **原文在此處寫「與 `PHI_MIN` 一致——證明這條夾限從上界到下界都構造得出真實觸發它的
> 合法佈局」。該句已刪除,因為它是錯的**(2026-08-31 協調者查證 `affinity_rules.gd`
> `bonus_for_at()` 本體後更正)。
>
> **夾限是逐一單位套用的**,不是對團隊總和套用 —— `clampi(total, PHI_MIN, PHI_MAX)`
> 位於 `bonus_for_at()` 函式內,其 `total` 是**單一單位**所有關係線的和;
> 該函式的文件註解自己寫著「clamps the total exactly once, **here**」。
> 反向快照的逐人數值是 `−1 / −1 / −1 / −1 / 0`,**每一個都離 `PHI_MIN = −4` 差得遠,
> 夾限一次都沒有動作**。團隊總和剛好等於 −4 是五個數字相加的巧合,不是夾限被觸發。
>
> ⚠️ **這個錯誤的類型,正是本節自己剛抓出來的那一個** —— 把**逐單位的量**
> (被夾限的 `Φ`)與**團隊層級的和**混為一談,和原文件把「單一角色最多兩條線」
> 誤用成「團隊每回合總和上限」是同一種混淆。**它在同一份報告裡,發生在指出它的段落下方
> 三行處。**
>
> ✅ **本節稍早那句「本切片目前完全用不到 R7 的夾限」才是正確的**,兩句衝突時以它為準。
> 量測數字(+10 / +6 / −4)本身無誤且仍為 (A) 級,錯的只是對 −4 的**詮釋**。

**但用真實起始座標(`vs01_roster.txt`)算,t=0 的實際數字是**:✅ **(A) 級,已實機
驗證**——引擎直接讀取 `vs01_roster.txt` 的真實起始座標,算出
`distance(甲,乙) = 1`(已相鄰!)、`distance(丙,丁) = 2`(死區),`bonus_for()` 回傳
Φ_甲=Φ_乙=+3、Φ_丙=Φ_丁=0、Φ_戊=0,團隊總和 **+6**——
與文件假設的數字巧合地相同,但理由完全不同(文件說的理由是「最多兩個同時生效」,
真正的理由是「甲乙兩人一開局就站得夠近」)。**+10 的上限只有在丙丁刻意分開到
距離≥3 才拿得到,不是白拿的。**

**(b) 用正確數字重算「有 Φ」— 結論是否改變?**

先指出一個獨立於上限數字的既有問題:§5「有 Φ」目前的逐回合敘述把 `+6` 整包
記在戊的攻擊上(例:「甲9+丁7+戊9=25+Φ6=31」)。**戊沒有任何關係線,任何位置
下 Φ_戊 恆為 0**——這不是取整或近似的問題,是把加成記到一個結構上不可能拿到
加成的角色身上。這一點無論本節結論是否改變都成立,留待下次修訂本檔時一併訂正
(本檔目前是第二版,修訂配額未用盡),**本次不因此另外去重寫§5 的逐回合文字**,
理由見下段結論。

⚠️ **精確化(2026-08-31,godot-gdscript-specialist):上一句「整包記在戊的攻擊上」
對字面文字而言不完全準確,已修正如下。**逐字讀 §5 原文「甲9+丁7+戊9=25+Φ6=31」,
`+Φ6` 是加在**三人總和之後的一個群組層級單一補項**,寫法上沒有指名任何一人
(不是「戊9+Φ6=15」那種逐人歸屬的寫法)。**所以「記在戊身上」這個講法本身不準確**——
原文沒有把 +6 掛在戊名下,是掛在整組的合計數字上。

但底層的算術問題是真的,而且已用引擎精確量出落差有多少,不必再靠語意爭論:
✅ **(A) 級,已實機驗證** ——對這一輪實際出場的三人子集合 {甲,丁,戊},用與 (a) 相同的
單一 `positions` 快照(甲乙相鄰、丙丁遠離),對 `bonus_for()` 逐一求值:
`Φ(甲)=+3`(與乙相鄰的真實上限)、`Φ(丁)=+2`(與丙距離≥3 的真實上限)、`Φ(戊)=0`
(無論站哪,已窮舉多個探測點驗證恆為 0)。**三人合計的真實上限是 +5,不是文件寫的
+6**——不論這 +6 打算怎麼分配在這三人身上都湊不出來,因為戊那一份永遠是 0、
丁那一份的真實上限只有 +2,兩者都不是可以互相彌補的餘裕。

✅ **但這不影響本回合「E2 同回合陣亡」的結論**:即使用修正後的 +5(而非文件寫的 +6),
`25+5=30` 仍 `≥` E2 血量 28,E2 仍在第 1 回合陣亡。**這是敘述精度問題(群組補項比
三人真實上限多算 1 點、且未指名到人),不是回合數結論的問題**——§5 標題結論
(無 Φ 5 回合、有 Φ 4 回合)不因此動搖。

**結論精確版**:原判讀「把加成記到一個結構上不可能拿到加成的角色身上」在方向上
正確(戊確實不可能貢獻任何 Φ),但描述成「整包記在戊身上」不是原文字面的意思——
原文是**群組層級**的單一補項,沒有指名任何人。精確的講法是:**這個群組層級的 +6
比這三人理論上能湊出的 +5 多了 1 點,湊不齊的那一點裡,戊永遠貢獻不了是結構性的
(0 是恆值,不是「這次沒湊到」),但差額本身不足以推翻本回合的擊殺判定**。

用「Φ 只加在該單位自己的攻擊上」的正確規則(甲/乙各自 +3 或依距離變動、
丙/丁各自 0 或依距離變動、戊恆 0),搭配與「無 Φ」版本相同的分兵技巧
(單回合內把溢出傷害轉打下一個目標),手算兩種情境:

- **情境一(樂觀,甲乙全程相鄰 +3、丙丁全程拉開至距離≥3 而各 +2)**:
  第1輪殺 E3+E2、第2輪殺 E4、第3輪殺 E1、第4輪殺 E5 剩餘量有餘裕——**4 回合**,
  與文件結論一致,margin 比文件寫的更寬鬆(因為真實上限是 10 不是 6)。
- **情境二(保守,採用真實起始距離、全程不刻意調整站位:甲乙 +3、丙丁維持 0)**:
  逐輪手算下來,第4回合對 E5 的傷害在「丙丁全程不分開」的假設下**差 1~2 點打不死**
  ——**這個保守情境卡在 4 回合和 5 回合的邊界上**,取決於丙丁在戰鬥後段(第3~4輪)
  是否曾經拉開到距離≥3。考慮到丙是遠程(2~4 格)通常留在後排、丁是中程(1~2格)
  通常往前壓,兩人的理想交戰位置本來就不同,拉開到距離≥3 是很低成本的自然結果,
  不需要玩家刻意犧牲戰術效益去湊這件事。

**結論:「無 Φ 5 回合、有 Φ 4 回合,差 1 回合」這個標題結論存活**,但比文件現在
呈現的更脆弱、更依賴站位——不是文件所寫的「每回合固定 +6、要用在哪就用在哪」
那樣穩賺不賠。§5 的頭號數字(5 回合 / 4 回合)**不改動**;上面提到的「戊被記錯
加成來源」的敘述性錯誤留待下次修訂處理。

**(c) 什麼時候夾限 `-4~+12` 才會真的咬到這張切片?** ⚠️ **(B)/(C) 級,非本輪實機驗證範圍**
——本段推理的是「假如關係資料量增加到 4~5 條線」這種**現時不存在的假設情境**,
真實資料現況每人最多 1 條線,無法用引擎對不存在的資料求值,故維持文字推導層級,
不因 (a)/(b) 的補測而升級。

單線最大貢獻是 `+3`(正向相鄰);要讓某一角色的加總「摸到」上界 `+12`,
至少需要 `⌈12/3⌉ = 4` 條線,且全部同時相鄰。

> 🔴 **本段原引用「`docs/registry/architecture.yaml` 已登記
> `affinity_links_per_character_max = 4`」—— 該條目不存在,引用是虛構的,已刪除。**
> (2026-08-31 協調者以 grep 查證:登記表全文無此鍵,亦無任何關係線數量上限條目。)
> **而且它與已核准文件正面衝突**:`affinity-position-chain.md` R7 明寫
> 「單一角色的關係線數量**不設上限**(管理者裁決:不限制敘事)」——
> 那正是第一輪審查建議四段上限、而管理者推翻的那一項。
> ⚠️ **「棋盤只有 4 個正交相鄰格」這件事本身是對的,但它約束的是「幾個人能同時相鄰」,
> 不是「一個角色能有幾條線」** —— 這正是 R7 修訂記錄裡點名的同一個混淆
> (拓樸約束被誤當成資料基數約束)。**同一個錯誤在同一天以另一種形式復發。**

不設上限之下,「摸到 `+12`」需要 4 條同時相鄰的正向線 —— 但摸到邊界不等於夾限「動手夾」:
4×3=12 本身沒有超過上界,
真正被夾限修正結果的情況要等到**第 5 條線**(例如再加一條負向、夥伴距離≥3
的線,貢獻 +2,把原始總和推到 +14,夾限才把它壓回 12)。下界 `-4` 對稱:
4 條負向相鄰線剛好摸到 `-4`,第 5 條線才會被真的夾住。
**本切片現況**:甲/乙/丙/丁各自只有 1 條線,離「4 條線摸到邊界」還差 3 條,
離「5 條線夾限動手」還差 4 條。**唯一會讓這個問題重新變得有意義的事件是
`characters.md` 第三節的關係資料量大幅增加**(單一角色新增至少 3 條關係線)
或未來對話卡牌加乘系統(amp > 1,OQ-3 尚未決定)上線——兩者都已由
`phi_curve_enemy_advantage_cross_calibration` 條目登記為重算義務,不需要
本檔另開新項。

---

## 修訂說明(第二版)

第一版把 `player_baseline` 取成「五名主角算術平均」,導致 E1~E5 的 HP/ATK/DEF **完全相同**
(33/20/8),敵人沒有強弱之分。本版兩處修改,**皆在公式二既有的參數空間內操作,未變更公式形狀**:

1. **`player_baseline` 改為「對應同武器分層的主角本人」**,不再取平均。
2. **`enemy_advantage_pct` 改為依敵人分級變動**(弱化 0.05 / 標準 0.20 / 強化 0.35),
   單一敵人的 HP/ATK/DEF 仍共用同一個係數,公式二明文要求的是這一條,不是「五隻敵人共用同一係數」。

> ⚠️ **撤回說明**:本版中途曾短暫改成「安全 ATK 上限」分級(含 E3 用負係數),
> 已**整份作廢**,不計入修訂次數(仍是第二版,不是第三版)。作廢理由三項:
> (a) 負值/零值牴觸 `tactical-combat-system.md` 公式二值域 `enemy_stat ≥ player_baseline_stat`、
> 係數值域 `[0,1.0)`,以及 Core Rules #7「敵方數值系統性高於我方」,不是空白地帶而是牴觸明文;
> (b) 把敵方總 HP/ATK 壓到低於我方,抹掉了「以弱勝強」的核心幻想承諾;
> (c) 該版無 Φ 與有 Φ 都是 4 回合,切片量不到 Φ 的效果。
> **2026-08-26 管理者裁決**:`Φ` 只作用於我方發起的攻擊,敵人攻擊我方不套用 `Φ`
> (已記錄於 `affinity-position-provisional.md` 第93行起)。這條裁決讓「丙一擊致死」風險
> 自動消失,不需要靠壓低敵方係數換安全邊際,故分級係數退回本表原始的 0.05/0.20/0.35。

## 假設與依據

- 傷害公式:`damage = max(0, ATK − DEF + Φ)`,零隨機(專案級禁令)。
- `Φ = ±3`(單一好感度關係觸發),最多兩個同時生效,總和上限 `±6`。
  **`Φ` 只在我方為攻擊方時套用;敵人攻擊我方時 `Φ = 0`**(2026-08-26 管理者裁決)。
- 敵方縮放:`enemy_stat = ceil(player_baseline × (1 + enemy_advantage_pct))`,單一敵人的
  HP/ATK/DEF 三者共用同一個 `enemy_advantage_pct`(公式二明文,不可破壞)。
- 棋盤 13(欄)×6(列)。地形移動成本:開闊地 1、灌木叢 2、倒木堆 3(不可穿越、遮蔽視線)。
- 我方站位第 0~1 欄,敵方站位第 7~11 欄。
- 失敗條件:五名主角任一人 HP 歸零即失敗(非全滅)。

## 1. 主角數值表(不可改動,已寫入資料檔)

| 代號 | 武器分層 | HP | ATK | DEF | MP |
|---|---|---|---|---|---|
| 甲 | 近戰 (1,1) | 30 | 16 | 8 | 6 |
| 乙 | 中程 (1,2) | 26 | 14 | 6 | 5 |
| 丙 | 遠程 (2,4) | 22 | 20 | 4 | 3 |
| 丁 | 中程 (1,2) | 26 | 14 | 6 | 5 |
| 戊 | 近戰 (1,1) | 30 | 16 | 8 | 6 |

## 2. 敵人強度分級

| 代號 | 對應主角(baseline) | 位置 | 分級 | 理由 |
|---|---|---|---|---|
| E1 | 甲 | (10,0) | 標準 0.20 | 近戰側翼,普通前排 |
| E2 | 乙 | (11,1) | 弱化 0.05 | 中程雜兵,優先被清 |
| E3 | 丙 | (9,4) | 弱化 0.05 | 丙自己的基準 ATK(20)已是全隊最高,維持低分級,威脅來源交給既有的「近身盲區」機制,不靠堆數值 |
| E4 | 丁 | (7,5) | 標準 0.20 | 中程雜兵,對稱 E2 |
| E5 | 戊 | (11,5) | 強化 0.35 | 本場遭遇的「硬點」——另一側近戰,刻意比 E1 難打,製造明確的強弱落差 |

## 3. 敵方數值表(E1~E5)

| 代號 | HP 算式 | HP | ATK 算式 | ATK | DEF 算式 | DEF | MP |
|---|---|---|---|---|---|---|---|
| E1 | ceil(30×1.20)=36.0 | **36** | ceil(16×1.20)=19.2 | **20** | ceil(8×1.20)=9.6 | **10** | 6 |
| E2 | ceil(26×1.05)=27.3 | **28** | ceil(14×1.05)=14.7 | **15** | ceil(6×1.05)=6.3 | **7** | 5 |
| E3 | ceil(22×1.05)=23.1 | **24** | ceil(20×1.05)=21.0 | **21** | ceil(4×1.05)=4.2 | **5** | 3 |
| E4 | ceil(26×1.20)=31.2 | **32** | ceil(14×1.20)=16.8 | **17** | ceil(6×1.20)=7.2 | **8** | 5 |
| E5 | ceil(30×1.35)=40.5 | **41** | ceil(16×1.35)=21.6 | **22** | ceil(8×1.35)=10.8 | **11** | 6 |

MP 沿用對應武器分層的第6節推導值(pct 不套用於 MP)。五隻敵人 HP/ATK/DEF **全部互不相同**。

## 4. 丙一擊致死檢查(硬約束第3項,敵人攻擊不套用 Φ)

**依 2026-08-26 裁決,敵人攻擊我方時 `Φ=0`**,單擊傷害 `= ATK − DEF`。全場最高 ATK 為 E5(22):

| 主角 | DEF | HP | 最壞單擊傷害 `= 22 − DEF` | 安全邊際 | 一擊致死? |
|---|---|---|---|---|---|
| 甲 | 8 | 30 | 14 | 16 | 否 |
| 乙 | 6 | 26 | 16 | 10 | 否 |
| 丙 | 4 | 22 | 18 | **4** | 否 |
| 丁 | 6 | 26 | 16 | 10 | 否 |
| 戊 | 8 | 30 | 14 | 16 | 否 |

**全數通過,最緊的仍是丙,安全邊際 4 點。**

**兩擊致死仍然成立,不因一擊致死解除而淡化**:E5 對丙連續兩擊 = 18×2 = 36 ≥ 22(丙 HP)。
丙若連續兩回合被同一隻敵人鎖定攻擊、且未獲支援或撤離,第二擊會致死。這是既有的量測項
(見 `active.md` 規格問題第5項),不是本節解決的問題,本節只確認**沒有一擊死**這條硬底線。

## 5. 對局長度推導(用鎖定數值重算,無 Φ / 有 Φ 各一次)

敵方總 HP = 36+28+24+32+41 = **161**。集火順序取 DEF 低者優先:E3(5)→E2(7)→E4(8)→E1(10)→E5(11)。

**無 Φ**(我方單擊傷害 `= ATK − DEF`,無加成):
- 第1回合:丙(15)+乙(9)=24 打死 E3(24)。餘甲(9)+丁(7)+戊(9)=25 打在 E2(28)上,餘3。
- 第2回合:乙補死 E2 餘3(用乙不用高輸出者,省戰力)。餘甲(8)+丙(12)+丁(6)+戊(8)=34 打死 E4(32)。
- 第3回合:五人攻 E1(36):甲6+乙4+丙10+丁4+戊6=30,餘6。
- 第4回合:丙(10)補死 E1 餘6。餘甲(5)+乙(3)+丁(3)+戊(5)=16 打在 E5(41)上,餘25。
- 第5回合:五人攻 E5(25 剩):甲5+乙3+丙9+丁3+戊5=25,剛好打死。**戰鬥結束。**

**無 Φ:5 回合**,落在 4~6 目標窗口正中間。用「總傷害/總HP」交叉驗證:平均每回合 39 點
(80 − 5×8.2 敵均 DEF),161÷39≈4.13→ceil 5,與逐回合模擬一致,5 回合是可證的下限,不是次優解。

**有 Φ**(我方攻擊 `+Φ`,每回合最多 +6,只加在我方攻擊上,敵方攻擊不受影響):
- 第1回合:丙15+乙9=24 打死 E3;甲9+丁7+戊9=25+Φ6=31≥28,**E2 同回合一併打死。**
- 第2回合:五人攻 E4(32):丙12+甲8+乙6+丁6=32(4人打死),餘戊 8+Φ6=14 轉打 E1(36),餘22。
- 第3回合:攻 E1 餘22:丙10+甲6+乙4+丁4=24(4人打死,餘2省下),餘戊 6+Φ6=12 轉打 E5(41),餘29。
- 第4回合:五人攻 E5 餘29:甲5+乙3+丙9+丁3+戊5=25+Φ6=31≥29。**打死,戰鬥結束。**

**有 Φ:4 回合。** 同樣用「總傷害/總HP」交叉驗證:45/回合,161÷45≈3.58→ceil 4,一致。

**無 Φ 5 回合、有 Φ 4 回合,差 1 回合,是可觀察的真實差距**(兩者皆為可證下限,不是分配巧合)。
比上一版「無 Φ 與有 Φ 都是 4 回合」有意義:那一版的問題是敵方總量被壓得太低,5 回合的空間已經
被壓縮到 Φ 省不出一整回合;這一版敵方總量回到 161,Φ 才有東西可以省。兩者皆落在 4~6 目標窗口內。

## 6. MP 推導(未改動)

| 武器分層 | 接戰欄位 | 純開闊地距離(格) | +1灌木叢緩衝 | MP |
|---|---|---|---|---|
| 近戰 (1,1) | 第6欄 | 5 | +1 | 6 |
| 中程 (1,2) | 第5欄 | 4 | +1 | 5 |
| 遠程 (2,4) | 第3欄 | 2 | +1 | 3 |

規則:`MP = 純開闊地所需格數 + 1`。倒木堆(不可穿越)未計入此緩衝 —— 若地形擺放包含倒木堆,
單位需繞路,實際抵達回合數可能超出本表估算(見規格問題,已在 `active.md` 列為量測項)。
