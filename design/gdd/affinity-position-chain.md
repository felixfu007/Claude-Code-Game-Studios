# 好感度—位置連鎖系統(含陣亡處理)

> **Status**: In Design
> **Author**: 管理者 + design-system(協調者) + 專家群(lean 模式)
> **Last Updated**: 2026-08-31
> **系統索引編號**: #5 ｜ **Priority**: MVP ｜ **Category**: Gameplay
> **⚠️ 特殊情況**: 本系統的**暫定版實作已經存在並在垂直切片中運作**
> (`src/gameplay/affinity/`)。本文件對照既有實作撰寫,並逐項標示
> 【既有行為】(照抄原型、未經裁決)與【本次裁決】(本文件新定案)。

## Overview

好感度—位置連鎖系統把「角色之間的關係」變成「棋盤上的空間戰術」。

**玩家面**:每位主角與特定同伴之間存在正向或負向關係,以綠線/紅線畫在棋盤上。
把互相信任的兩人排在一起,雙方變強;把彼此對立的兩人擠在一起,雙方變弱。
玩家移動游標時就看得到這個變化,**不必先下手才知道結果**。

**系統面**:本系統對「某個單位站在某一格」這個提問,回覆一個**帶號整數 `Φ`**。
`Φ` 直接進入戰棋移動與交戰系統的傷害公式 `max(0, ATK − DEF + Φ)`,
是該公式**唯一與站位有關的項**。本系統無狀態、無隨機、不寫入任何資料 ——
它只把「誰站在哪裡」翻譯成一個數字。

**為什麼必須存在**:本作的戰鬥結算沒有任何隨機性,敵方數值又固定高於我方
(`enemy_advantage_pct`,目前 0.20)。因此**好感度佈局是玩家用來抵銷數值劣勢的主要手段**。
少了這個系統,戰鬥退化成純粹比數值大小,而且我方必敗。

> ⚠️ **「唯一」二字已於 2026-08-31 移除,因為本文件自己推翻了它**(兩處,皆非假想):
> ①**對戊完全不適用** —— 他沒有任何關係線,`Φ` 恆為 0(見 Edge Cases);
> ②**只覆蓋輸出通道** —— 敵方 ATK 虛高造成的承傷劣勢(最高 6 點)`Φ` 完全不覆蓋
> (見「交叉校準結果」)。
> 上游 `tactical-combat-system.md` 有**字面相同**的一句,須同批限定範圍(見 OQ-5)。

**範圍涵蓋陣亡處理**:單位陣亡或離場後,連到它的關係線如何失效、
其餘單位的 `Φ` 如何隨之改變,屬本系統職責。

⚠️ **本文件標示慣例**:【既有行為】= 垂直切片中已在運作、照抄原型 v5 且**未經裁決**的行為;
【本次裁決】= 本文件新定案,實作需據此調整。每條規則都必須標示其一。

## Player Fantasy

**目標感受:「我不是靠運氣贏的,是靠我把人擺對位置。」**

這個系統服務的是一種**理解帶來的力量感**。玩家一開始只看到棋盤上幾條彩色的線,
像是裝飾;某一場戰鬥中他發現把甲挪到乙旁邊,傷害預覽從 4 跳到 7,
那一擊剛好夠殺掉原本殺不掉的敵人。**那個瞬間,關係線從裝飾變成資源。**

**關鍵時刻**:游標移動到某一格,畫面同時告訴他「走到這裡,你會多打 3 點,
但丙會因為靠近丁而少 1 點」。他在**下手之前**就知道全部後果 ——
這是本作零隨機承諾在好感度層的兌現。**玩家的失敗永遠是判斷失誤,不是運氣不好。**

**反面感受(必須避免)**:

- **「我不知道為什麼變強了」** —— 數字變了卻看不出是哪條關係造成的。
  這會讓本系統退化成隱形的難度調節器,支柱二(好感度是可操作的空間戰術要素)在回饋層失效。
- 🔴 **「線是假的」** —— 畫面顯示的關係狀態與實際生效的不一致。**一次都不能發生**:
  玩家一旦發現畫面會騙人,零隨機承諾建立起來的全部信任會一起失效。
  (此非假想風險 —— 舊實作的抑制邏輯確實會讓同一條線在甲、丙兩人帳上不一致;R4 取消抑制後已根除,R8 將該性質明文化為契約)

**與敘事的連結**:這些關係不是抽象的加成數字,是角色之間真實的信任與嫌隙。
玩家為了戰術把兩個人排在一起,同時也是在**選擇讓誰陪著誰**。
戰術上的最佳解與情感上的偏好可以衝突 —— 那個衝突正是本作想讓玩家感覺到的重量。

## Detailed Design

### Core Rules

**R1. 關係表是資料,不是規則。**【既有行為 —— 🔴 **資料來源部分已過時,見 Interactions ①**】
誰與誰有關係、正向或負向、強度多少,**不由本節定義**。本節只定義**曲線與疊加**。

> 🔴 **原寫「全部來自關係表檔案」是垂直切片現況,不是設計。** 權威來源為上游即時查詢
> (Interactions ①),靜態檔僅為暫代品。**與 ① 衝突時一律以 ① 為準。**

**R2. 距離一律用曼哈頓距離**,與戰鬥射程判定**採用相同定義**。
【既有行為 —— 🔴 **「同一個函式」的說法不成立,見下**】

> 🔴 **實測(2026-08-31):這是兩份獨立實作**(`affinity_rules.gd` 的 `manhattan_distance`
> 與 `combat_rules.gd` 的 `_manhattan_distance`),只是今天答案一致。原稱「已有交叉測試釘死」
> 是誇大:黑箱比對輸出**永遠無法區分「共用同一函式」與「兩份碰巧一致的實作」**。
> **【本次裁決】允許兩份實作,但必須測到一致**(合併會讓 Gameplay 橫向耦合到戰鬥模組,
> 收益低於成本)。見 AC-R2。

**R3. 距離—數值曲線**【既有行為,本次確認採用】

| 距離 | 正向關係 | 負向關係 |
|---|---|---|
| 1(相鄰) | **+3** | **−1** |
| 2 | 0 | 0 |
| ≥3 | **−1** | **+2** |

距離 2 是**刻意的死區**:靠近與遠離都必須是明確的選擇,不存在「稍微靠一點」的漸進收益。
距離 0 不會發生(棋盤禁止兩單位同格,已有落地機制而非僅註解宣稱)。
距離超過 3 之後不再變化 —— `≥3` 是一整段,不是遞增門檻。

**R4. 疊加:所有關係線全部生效,逐條加總。**🔴【本次裁決 —— 推翻既有行為】
一個單位身上的每一條關係線,無論正負、無論幾條,**各自依 R3 算出數值後全部相加**。
**不存在任何抑制機制。**

> *裁決理由*:本作的結局結構建立在「大家聚在一起」與「疏離一個人」兩極之上
> (見 `endings.md` 第 3 節「對立的敘事機制:拒絕互相掩護」,2026-08-26 管理者裁決 D22)。
> 唯有逐條加總,**走向某一極的程度才會轉化為戰力差異**,那條軸線才是真的。
> 若只取最近一條,走一步與走到底同樣強,選擇就是假的。

**R5. 「犧牲一人」是 R4 的直接後果,不需另外設計。**【本次裁決】
被多人負向連著的角色,留在隊伍中會讓每個關係人各自承擔 −1;
排除他則團隊解除負擔,而他自己失去所有正向鄰接、每條正向線承擔 −1。
**這個取捨是機制自動長出來的,不是腳本事件。**

**R6. 兩條路線的戰力不對等,而且是刻意的。**【本次裁決】
合作(相鄰正向)每條 **+3**;對立(遠離負向)每條 **+2**。
合作路線的戰力上限比對立路線高約 50%。

> *裁決理由*:「拒絕互相掩護」換得的是不必背負他人,**戰術上付出代價是這個選擇的重量本身**。
> ⚠️ 此差距為**明文設計,非抄襲原型的巧合**。調整 R3 任一數值前必須連同本條一起重新裁決。
>
> ✅ **實機量測結果(2026-08-31,OQ-7 已結案):對立路線的代價「只有」這 50%,沒有第二層。**
> 本輪稍早曾推測「距離你 ≥3 的夥伴自己也遠離戰場、那回合沒在打仗」構成隱藏的第二層代價 ——
> **量測後證明該推測是錯的,已撤回。**
>
> 以 vs01 真實棋盤(13×6,4 格阻擋)窮舉:在**最嚴苛**的情況下(近戰單位須貼著敵人),
> 全部 15 個可攻擊站位中,最少仍有 **57 格**可供夥伴站在距離 ≥3 處
> (最多只需 4 格),**可行性從來不是限制**。更關鍵的是,那 57 格裡
> **9～12 格可讓近戰夥伴同時攻擊、26～33 格可讓遠程夥伴同時攻擊** ——
> **夥伴可以同時「離你 ≥3」與「正在打仗」。**
>
> 🔴 **因此調數值時要知道:兩條路線的差距就是帳面那 50%,不多也不少。**
> 先前那句「還有一層隱藏代價」若留著,會讓人誤以為 +2 其實比帳面更弱而過度補償。

**R7. `Φ` 的上下界**完全**由硬性夾限決定,沒有第二道機制。**🔴【本次裁決 —— 2026-08-31 管理者裁決修訂】

單一角色的關係線數量**不設上限**(管理者裁決:不限制敘事)。因此
**線數不約束 `Φ`,夾限是唯一的邊界,而且會在正常遊玩中真的作用** ——
它不是「資料寫錯時才生效的保險絲」,一旦有角色擁有第 5 條線,合法佈局就可能觸發它。
推導與反例見公式二。**修改夾限值即等同修改戰力上下限本身。**

> ⚠️ **本條 2026-08-31 前寫「結構層 + 保險絲層兩道互補」,那是錯的** ——
> 「結構層」`N=4` 被誤認為由棋盤拓樸推導而來。棋盤只約束「最多幾人能**相鄰**」,
> 不約束「一個角色能有**幾條線**」;後者是資料基數,不是拓樸。

**R8. 每條關係線對其兩端同時生效,且雙方看到的狀態必然一致。**🔴【本次裁決】
不存在「這條線對甲生效、對乙不生效」的情形。

> *此條的存在理由*:R4 取消抑制後,視角不對稱在數學上已不可能發生。
> 本條把該性質**明文化為契約**,防止日後有人以任何形式重新引入單邊抑制,
> 而破壞 Player Fantasy 的「線不會騙人」承諾。

**R9. 陣亡或離場的單位,連同其所有關係線一併失效。**【既有行為,已驗證】
實作為「從位置表整條移除」而非設旗標;任何一條關係線只要有一端不在場,該線不計。
**位置快照必須每次重新讀取,不得快取** —— 陳舊快照會讓屍體以生前位置繼續提供加成。

**R10. 本系統無狀態、無隨機、不寫入任何資料。**【既有行為】
它是一個純查詢:給定「誰在哪裡」,回覆一個帶號整數。
符合專案級禁令 `rng_in_combat_settlement`(戰鬥結算路徑絕不使用隨機)。

#### ⚠️ R4/R7/R8 對現有實作的影響(誠實記錄 —— 2026-08-31 依實測數字更正)

**本節 2026-08-31 前的版本低估了變更面**(原寫「3 條測試、方向是刪除」)。實測:
`active_positive_link()` 有 **2 個呼叫點**;受影響正式程式檔 **2 份**
(含舊版從未點名的 `affinity_line_status.gd` —— 須改 `create()` 簽章、移除 `SUPPRESSED`);
受影響測試 **至少 4 條、橫跨 2 個目錄**。

🔴 **第 4 條測試在 `tests/unit/ui/` 下**,任何把範圍框在「好感度測試」的人都看不見它。
🔴 **是刪除重寫,不是就地更新** —— 兩條測試的**名字**就是 R4 要推翻的行為,
保留名字改斷言值,測試名就是謊。**替代品即 AC-R4a / AC-R4b。**
⚠️ **「方向是刪除」只適用於 R4;R7 是百分之百的新增程式碼**(今日四個好感度檔案裡
沒有任何 `clamp`/`min`/`max` 呼叫,也沒有任何 logging 樣式)。
⚠️ **待實作階段裁決一次**:`SUPPRESSED` 列舉成員保留為死 API 還是刪除(決定第 4 條測試存廢)。

📌 逐檔逐測試的完整清單見 `design/gdd/reviews/affinity-position-chain-review-log.md` 第一輪條目。

### States and Transitions

**本系統本身沒有狀態**(R10)。可觀察的狀態屬於**每一條關係線**,完全由當下位置推導,
不儲存、不記憶。線共有六種可觀察狀態:**正向/負向 × 緊鄰/死區/疏遠**,
外加「失效」(任一端不在場,整條不計入 —— 見 R9)。

📌 **各狀態的數值一律以 R3 的曲線表為準,本節刻意不複製一份** ——
2026-08-31 前本節有一張與 R3 逐格重複的表,那是純粹的漂移面(改一處忘另一處就會不一致),
依專案「同一事實只定義一次」規則移除。

**轉換沒有事件,只有重新求值。** 任何移動、陣亡或極性變化發生後,下一次查詢自然反映新狀態。
**不存在「轉換中」的中間狀態,也沒有延遲生效。**

⚠️ **極性本身可在戰役中改變**(來源見下節):同一對角色可能在某一關是正向、之後翻成負向。
本系統**不記憶歷史**,永遠只反映查詢當下的極性。

### Interactions with Other Systems

**① 好感度數值池(#1,Accepted)—— 上游,本系統的資料來源**

配對的**極性與強度**來自呼叫該系統的**戰鬥強度讀取函數**(`combat_strength_read`),
**不是本系統自有的資料。本節是極性/強度來源的唯一權威,與 R1、公式一衝突時以本節為準。**

- 🔴 **【本次裁決】現行實作讀取靜態檔 `assets/data/affinity/vs01_affinity_links.txt`,
  屬垂直切片權宜做法,與上游已核准設計不符。正式實作必須改為即時查詢。**
- 🔴 **【本次裁決 —— 逐次查詢,禁止建構時快取】** 配對資料(極性、強度)**必須每次查詢
  重新取得**,與位置快照同級。**遷移時真正要改的是這一點,不只是把靜態檔換成函式呼叫。**
- 🔴 **尚未裁決**:上游回傳無上限實數、本公式吃 int `amp`,換算方式未定。見 OQ-3。
- ⚠️ **陣亡通知不得經由本系統轉發。** 上游明訂由戰棋系統直接通知;
  經本系統轉接會在 Foundation 與 Gameplay 兩層之間製造循環依賴。

> 📌 **本節三項義務的完整理由、實測證據與執行條件,登記於
> `docs/registry/architecture.yaml` 的 `interfaces` 節**,條目
> `affinity_pairing_data_per_query_refetch`(逐次重取、`amp` 量化未決)與
> `phi_curve_enemy_advantage_cross_calibration`(跨文件數值耦合)。
> **登記表是權威來源,本節是摘要。**(2026-08-31 `creative-director` 裁決:
> 這兩項缺的是「被記錄下來的義務」而非「要在數個方案間做選擇」,故**不開新 ADR**。)

**② 戰棋移動與交戰系統(#4)—— 雙向硬依賴**

- 本系統**輸出** `Φ` 進入其傷害公式 `max(0, ATK − DEF + Φ)`。
- 本系統**輸入**「誰站在哪一格」與「誰還活著」。
- ⚠️ **`Φ` 在結算中為快照**:該系統於結算步驟①取值後,即使中途有寫入改變好感度,
  步驟③仍用原快照。**本系統不負責維持快照,但不得提供任何會就地變動的回傳值** ——
  回傳純量即可滿足此契約。
- ⚠️ **呼叫頻率為輸入事件級,非每回合一次**(游標移動即觸發預覽)。查詢須以此為效能前提。

**③ 單一游標/高亮狀態系統(#3)** —— 提供「假設站在某格」的預覽查詢。
預覽與實際結算**必須走同一個函式**,否則預判會與結果不符,違反零隨機承諾。

**④ 好感度視覺呈現 UI(#9,Not Started)** —— 下游。本系統須能**逐條**回報關係線狀態
(而非只給總和),否則玩家看不出數字由哪些關係構成(見 Player Fantasy 反面感受)。

## Formulas

### 公式一:`Φ` 的計算

`Φ(u, o) = clamp( Σᵢ ampᵢ × B(polarityᵢ, dᵢ) , Φ_min, Φ_max )`

其中 `dᵢ = manhattan(o, pos(partnerᵢ))`,`B` 為 R3 的距離曲線。

| 變數 | 符號 | 型別 | 值域 | 說明 |
|---|---|---|---|---|
| 受查詢單位 | `u` | int | 名冊 id | 可為假設站位的預判查詢 |
| 查詢座標 | `o` | Vector2i | 棋盤合法格 | 「假如站在這裡」 |
| 生效線數 | `k(u)` | int | **0 ～ 無上限**(見公式二) | **雙端皆在場**的線數;任一端離場即不計(R9) |
| 強度 | `ampᵢ` | int | **目前恆為 1** | 保留給對話卡牌加乘;**權威來源見 Interactions ①,不是本表** |
| 距離 | `dᵢ` | int | 1 ～ 棋盤最大值 | 每次查詢即時算,**不快取** |
| 基礎貢獻 | `B` | int | `{−1, 0, +2, +3}` | R3 曲線 |

**夾限前的原始和:無界(兩個方向皆是,見公式二)。夾限後的輸出:`−4` ～ `+12`。**

### 公式二:關係線數量**不設上限**,而 `Φ` 因此無界 —— 這是夾限存在的理由

🔴 **2026-08-31 管理者裁決:每角色關係線數量不設限**(理由:不讓數值設計限制敘事)。
本節取代舊版「`N = 4`,由棋盤形狀決定」的推導 —— **舊推導是錯的**,錯法記錄於下方。

**幾何只約束「相鄰」這一項**:距離恰為 1 的格子確實恰有 4 個
(`|dx|+|dy|=1` 只有 4 組整數解),故正向緊鄰貢獻**飽和於 4×(+3) = +12**。
但**距離 ≥3 的格子不稀缺** —— 負向疏遠每條 **+2**(向上無界)、
正向疏遠每條 **−1**(向下無界)。

**具體反例(本輪審查發現,已複算)**:四條正向緊鄰(+12)+ 一條**負向**距 ≥3(+2)
= 原始和 **+14**,超出 `Φ_max`。這是**合法佈局**,不是資料錯誤;再多幾條可以任意大。

> ⚠️ **舊推導錯在哪(記錄下來,避免重犯)**:舊版寫「第 5 條線起最好情況只能停在
> 距離 2 的死區」——這只有在第 5 條與前 4 條**同極性**時才成立,而 R4 明文禁止該假設。
> 舊版並引用 `board.gd` 的 `_get_orthogonal_neighbors()` 為證,但 `affinity_rules.gd`
> **對 `Board` 零引用**,「距離 1 恰有四格」是 L1 度量本身的性質,與該函式無關。
> **日後想「重新驗證線數上限」而去看 `board.gd`,是在查錯的東西。**

### 公式三:夾限值 `Φ_min = −4`、`Φ_max = +12` —— 唯一的邊界,且會真的作用

承公式二:線數不設限後,夾限**不再是永不觸發的保險絲,而是唯一擋住數值的東西**,
一旦有角色擁有第 5 條線就可能在**合法佈局**下觸發。

這兩個數字取自**相鄰四格的飽和點**(`+12` = 4×(+3);`−4` = 4×(−1)),
意義為「純粹靠緊鄰站位所能達到的最好與最壞結果」——超出者一律壓回該邊界。
依管理者既有裁決「先補資訊再談數值」,**本次不調整這兩個值**。

🔴 **調整這兩個值 = 直接調整戰力上下限本身**,不再只是調整一道安全網。
⚠️ **本夾限假設 `amp = 1`。對話卡牌加乘系統定案後必須回頭重算。**
此處刻意**不**預留猜測性的寬鬆值 —— 猜一個涵蓋未來的數字等於拍腦袋。

### 算例(vs01 起始佈陣真實座標)

甲(0,2)、乙(0,3) 正向相鄰,距離 1 → `Φ(甲) = +3`。
甲攻擊 E1(`ATK=16`、`DEF=10`):`max(0, 16−10+3) = 9`;若未相鄰(`Φ=0`)則為 `6`。
**站位差了 3 點,佔基礎傷害的 50%。**
丙(1,3)、丁(0,4) 負向、距離 2 → 死區 → `Φ = 0`;丙移至距丁 ≥3 → `Φ = +2`。

### 交叉校準結果(對 `enemy_advantage_pct = 0.20`)

🔴 **敵方數值優勢分為兩條通道,`Φ` 只覆蓋其中一條 ——【本次裁決:維持如此】**

| 通道 | `Φ` 是否覆蓋 | 實測最大差距 |
|---|---|---|
| **輸出**(敵方 DEF 虛高) | ✅ 覆蓋 | 2 ～ 3 點 |
| **承傷**(敵方 ATK 虛高) | ❌ **不覆蓋** | 最高 6 點 |

`Φ` 為**攻擊方單向加成**,不影響防禦方算式。**玩家可以打得更痛,但無法挨得更少。**

**不等式驗算(僅輸出通道,數字來自 `vs01_roster.txt` 實測)**:

- 合作路線:單條 `+3` ≥ 最大差距 3 → **成立,有餘裕**
- 對立路線:單條 `+2`,對丁/E4 該組差距 2 → **剛好打平、零餘裕**
  (與 R6「對立付代價」一致,非缺陷)

⚠️ **本節對上游文件產生三項修訂義務,見 OQ-5**(不在此複述,避免兩處各自漂移)。
📌 **本節與 `enemy_advantage_pct` 的耦合義務登記於 `docs/registry/architecture.yaml`
的 `phi_curve_enemy_advantage_cross_calibration` 條目** —— 那是唯一會讓
「日後調 `enemy_advantage_pct` 的人」發現本義務存在的機制,散文告誡不是。

## Edge Cases

- **兩單位距離為 0**:不會發生(棋盤禁止同格,有落地機制)。曲線仍定義為 `0` 作為防禦性預設。
- **關係線任一端不在場**(陣亡/離場):該線**整條不計**,兩端皆然 —— 不是計為 0,是不參與加總。
- **兩端同時陣亡**:同上,無需特殊處理。
- **角色沒有任何關係線**(如戊):`Φ` **恆為 0,不論站哪**。
  ⚠️ 本系統對該角色**完全不提供抵銷敵方數值優勢的能力**(見 Open Questions)。
- **極性在戰役中翻轉**:本系統不記憶歷史,下次查詢即反映新極性,不保留舊加成。
- **某角色關係線達 5 條以上**:🔴 **合法,不是資料錯誤**(2026-08-31 管理者裁決不設上限)。
  依 R4 逐條加總,超出夾限即壓回 —— 這是**預期中的正常行為**,不觸發診斷記錄。
- **原始和超出夾限**:壓回 `−4` / `+12`。⚠️ **這會讓畫面上逐條顯示的數字加起來
  不等於總和**,而 Player Fantasy 明令「線不會騙人」。
  🔴 **因此 UI 必須明示「已達上限/下限」,不得靜默截斷** —— 見 UI Requirements。
- **`amp` 超出合法值域**(關係表寫錯,如 `amp=10`):**這才是資料錯誤**。
  夾限仍會壓回邊界,**且必須留下可觀測的診斷記錄** —— 靜默壓回會讓資料錯誤永遠不被發現。
  ⚠️ 現行 `affinity_link.gd` 以裸 `int(fields[3])` 解析,**無任何值域檢查**,須補。
- **`amp ≠ 1`**:公式仍成立,但公式三的夾限失效,必須重算。
- **同一對角色有多條重複線**:視為資料錯誤,**逐條加總不去重**(本系統不猜測意圖),診斷記錄同上。
- **查詢的單位本身不在棋盤上**(預覽用途):允許,回傳依 `o` 計算的假設值。
  ⚠️ **本系統不檢查該單位是否存活** —— 存活檢查是呼叫端義務。
  目前僅單一呼叫路徑保證此點,**此邊界尚無測試守著**(見 Open Questions)。
- **單次結算中好感度被寫入而改變極性**:本系統照實回報當下值;
  **維持快照是上游戰棋系統的義務**。本系統只保證回傳純量(不可就地變動)。

## Dependencies

| 系統 | 方向 | 硬/軟 | 介面 |
|---|---|---|---|
| 好感度數值池(#1,Accepted) | 上游 | **硬** | 呼叫戰鬥強度讀取函數取得**極性與強度**;另取互動次數以區分「久未互動」與「從未互動」 |
| 戰棋移動與交戰(#4,Designed) | **雙向** | **硬** | 輸出 `Φ` 進傷害公式;輸入位置與存活狀態 |
| 單一游標/高亮狀態(#3,Approved) | 下游 | 軟 | 提供「假如站這裡」的預覽查詢 |
| 好感度視覺呈現 UI(#9,Not Started) | 下游 | 軟 | 須能**逐條**回報線的狀態,非只給總和 |

⚠️ **陣亡通知不經本系統轉發**(上游明訂,避免循環依賴)。
✅ **雙向一致性已確認(2026-08-31 實查)**:`systems-index.md` 的戰棋移動與交戰系統該列
**已登記**「#5 的 `Φ` 輸出」為反向依賴(2026-08-17 補登)。
本文件原記「尚未登記」為**過時的假警報**,已更正。

## Tuning Knobs

### 🔴 校準目標(管理者 2026-08-31 裁決 —— **優先於下表任何數字**)

- **基礎(無好感度加成)**:一個普通敵人應**打 4 下**才死 → 對應一場 4～5 回合,
  落在既定的 4～6 回合對局長度目標內。
- **好感度加成生效時**:應**省下 1 下**(4 下 → 3 下)。

**由此反推的硬性約束**:要讓加成省下一下,需 `Φ ≥ 敵人血量 ÷ 12`。
因此在 `Φ=+3`(合作)下,**敵人血量不得超過 36** —— 超過即感覺不到加成。
⚠️ **E5 現有 41 血,已在此線之外。**

⚠️ **實測落差(2026-08-31)**:現況多數配對需 **6 下**(戊/E5 需 9 下),未達 4 下目標。
🔴 **校準時應優先調整基礎數值(角色攻擊力/敵人血量),而非加大 `Φ`** ——
加大 `Φ` 會讓「無關係線者」(戊)與「加成無感者」(丙)的落差**反而擴大**。
依管理者既有裁決「先補資訊再談數值」,本文件不調整任何數值,僅記錄目標與落差。

| 旋鈕 | 目前值 | 安全範圍 | 調太高 / 太低 |
|---|---|---|---|
| 正向·緊鄰 | **+3** | 2 ～ 5 | 太高:合作輾壓,無人走對立。太低:低於敵方優勢差距,以弱勝強不成立 |
| 負向·疏遠 | **+2** | 1 ～ 正向值 | ⚠️ **不得 ≥ 正向緊鄰值**,否則 R6 的路線不對等反轉 |
| 正向·疏遠 | **−1** | −3 ～ 0 | 太重會使「暫時分開」代價過高,壓縮戰術空間 |
| 負向·緊鄰 | **−1** | −3 ～ 0 | 同上 |
| 死區距離 | **2** | 固定 | ⚠️ 改動會使「靠近/遠離必須是明確選擇」失效 |
| 每角色線數上限 | **無上限** | — | 🔴 **2026-08-31 管理者裁決:不設限**,理由為不讓數值設計限制敘事。**這不是旋鈕,是已移除的約束** |
| `Φ` 夾限 | **−4 / +12** | 見下 | 🔴 **線數不設限後,這是唯一擋住數值的東西,且會在合法佈局下真的作用**。調它 = 直接調戰力上下限,不是調安全網 |

🔴 **跨系統校準義務**:前四項與 `enemy_advantage_pct`(0.20)**不得各自獨立調整**。
調整任一方後須重新驗算「輸出通道的 `Φ` ≥ 敵方優勢差距」,並在雙方文件記錄結果。

## Visual/Audio Requirements

關係線以**綠線(正向)/紅線(負向)**畫在棋盤上,**兩端一致**(R8)。

**上游已裁決、本節不重述細節、但必須遵守的兩條**(見 `tactical-combat-system.md`):

1. **`Φ` 的正負方向與量值須單獨可讀**,不得只呈現合併後的傷害數字 ——
   否則玩家看不出因果,Player Fantasy 的「我不知道為什麼變強了」反面感受成立。
2. **死亡回饋與受影響單位的加成變化回饋須為兩個先後可辨識的視覺事件**,
   不得疊在同一瞬間 —— 讓玩家能按時序讀出因果鏈。
   🔴 **此條為單數情況而寫,複數情況未定義** —— 見 UI Requirements。

⚠️ **`art-director` 未諮詢**(lean 模式 + 流程劑量上限)。**美術製作階段須補做** ——
本節僅約束「必須表達什麼」,不定義「長什麼樣」。

## UI Requirements

- **游標移到任一格時**,須同時顯示該格的 `Φ` 與**逐條**關係貢獻,而非只給總和。
- **預覽與實際結算必須走同一個查詢函式** —— 兩者不符即違反零隨機承諾。
- **無關係線的角色**(戊)須明確呈現「本系統對他不適用」,而非顯示一個空的 0 ——
  兩者對玩家的意義完全不同。
- 🔴 **原始和被夾限壓回時,必須明示「已達上限/下限」。** 否則逐條數字加起來不等於總和,
  玩家會直接看到「線在騙人」。線數不設限後這是**正常可達狀態**,不是例外。
**以下三項為本輪新登記的義務,機制一律交 `/ux-design`,本節只負責讓它們有人認領:**

- 🔴 **手把對等**:本節全部需求以「游標移到某格」描述,那是**滑鼠懸停**模型(可無限期停留),
  而 d-pad/類比搖桿是**步進**的。專案技術偏好強制手把對等、禁 hover-only(主機無游標)。
  **逐條可讀性必須塞得進日後選定的步進節流窗**,而該節流率在 `cursor-highlight-state.md`
  的 Open Questions 裡明文未決。**本系統是那條未經測試路徑的第一個高風險消費者。**
- 🔴 **一次陣亡可同時改變多個單位的 `Φ`**(R5 + R4),而 Visual/Audio 那條「兩個先後事件」
  繼承自上游、**只為單數情況而寫**,未定義批次或串列呈現、串列有無長度上限。
- 🔴 **死前值無人擁有**:呈現「變化了多少」需要死前/死後差值,但 R10 使本系統無狀態
  (**這是對的,不要改**),目前無任何系統被指派快照。見 OQ-6。

📌 **UX Flag**:本系統有實質 UI 需求。Pre-Production 須先跑 `/ux-design` 產出 UX spec,
**再**寫 epic;實作故事應引用 `design/ux/[screen].md`,不得直接引用本 GDD。

## Acceptance Criteria

### BLOCKING(自動單元測試)

- **AC-R1**:GIVEN 注入兩組強度不同的假資料(程式碼不變),WHEN 查詢 `Φ`,THEN 輸出隨資料改變 —— 證明極性/強度來自外部而非硬編碼。
- **AC-R2**(2026-08-31 改寫):GIVEN **至少 20 組**涵蓋邊界距離(0、1、2、3、棋盤最大值)的座標對,WHEN 逐組比對 `AffinityRules.manhattan_distance` 與 `CombatRules` 射程判定所隱含的距離,THEN **全數一致**。⚠️ **這是「兩份實作測到一致」,不是「同一個函式」** —— 後者是黑箱測試在結構上無法驗證的事(見 R2)。
- **AC-R3**:GIVEN 四種(極性×距離)組合各自單獨存在,WHEN 查詢單線 `Φ`,THEN 依序回傳 +3/−1(正向距1/距≥3)與 −1/+2(負向距1/距≥3),距離 2 恆為 0。
- 🔴 **AC-R4a**:GIVEN 單位有兩條正向線(一條距1、一條距≥3),WHEN 查詢 `Φ`,THEN 回傳 **+2**(=3−1)。**若回傳 +3 代表仍在跑舊「只取最近正向」邏輯,測試須失敗。**
- **AC-R4b**:GIVEN 同一單位再加一條負向距1線,WHEN 查詢 `Φ`,THEN 回傳 +1(=3−1−1)。
- **AC-R5**:GIVEN X 與 Y、Z 各有負向距1線,WHEN X 從名冊移除,THEN Y、Z 的 `Φ` 各自 +1,且不經任何額外腳本事件。
- **AC-R6**:GIVEN 單條正向距1與單條負向距≥3各自單獨存在,WHEN 查詢,THEN 前者 +3、後者 +2,兩者不相等。
- **AC-R7**(改寫:6 條線現為**合法**,非資料錯誤):GIVEN 6 條正向距1線(理論和 +18),WHEN 查詢,THEN 回傳恰為 **+12** —— 證明夾限在合法佈局下確實作用。
- 🔴 **AC-R7b**(新增 —— 夾限獨立於線數):GIVEN **單一**正向距1線、但 `amp=10`(線數僅 1,理論和 +30),WHEN 查詢,THEN 回傳恰為 **+12**。**證明夾限不依賴線數多寡即可生效**;`amp` 由未受驗證的 CSV 欄位解析而來,此路徑現在就活著。
- 🔴 **AC-MIX**(新增 —— 混合極性溢位):GIVEN 4 條正向距1(+12)加 1 條**負向**距≥3(+2),理論和 **+14**,WHEN 查詢,THEN 回傳恰為 **+12**。**這是合法佈局,不是資料錯誤**;若實作把 `clamp()` 當裝飾省略,本條是唯一會抓到的測試。
- 🔴 **AC-R8**(改寫:點名存取器):GIVEN A—B 一條線(另可有任意無關線),WHEN 呼叫 `lines_for(A,…)` 取 `partner_id == B` 的項、並呼叫 `lines_for(B,…)` 取 `partner_id == A` 的項,THEN 兩項的 `.delta` 與 `.polarity` **相等**。⚠️ **不得改用 `bonus_for(A)` 對比 `bonus_for(B)`** —— 只要 A、B 各自還有別的關係線,兩者本來就該不相等,那樣測根本沒測到 R8。
- **AC-R9**(改寫:移除 tick 概念):GIVEN Y 與 partner 有正向距1線,WHEN 把 Y 自位置表移除後,**在同一個測試函式內、中間不呼叫任何重置或清快取方法**,再查 partner 的 `Φ`,THEN 立即減少 3。⚠️ 原文的「同一 tick 內」借用了本系統沒有的概念(R10 明訂無更新迴圈),會誘導測試作者去 `await` 畫格、平白替 headless CI 引入不穩定。
- **AC-R10a**(改寫:今日可測):GIVEN 相同輸入連查兩次,WHEN 比對兩次輸出、並深度比對傳入的 `positions`/`links` 於呼叫前後的內容,THEN 輸出相等**且輸入未被改動** —— 純查詢契約不引用任何尚不存在的介面。
- **AC-R10b**(延後,非本文件範圍):池整合故事**自己的** DoD 須包含「好感度池寫入介面呼叫次數為 0」。⚠️ 原 AC-R10 計數的那個介面**在 `src/` 下不存在**(已 grep 確認),寫在這裡等於一條永遠無法執行的驗收條件。
- ~~**AC-SNAP**~~(2026-08-31 刪除):`bonus_for` 靜態標註 `-> int`,**GDScript 編譯器已保證**回傳值型別,執行期再斷言一次是**一條不可能失敗的測試**。本專案 CI 標準明文:「一條不可能失敗的測試比一條會失敗的更糟」。改以 `bonus_for_at()` 簽章上的程式碼註解表達意圖(「回傳型別刻意為 `int` 而非 `Variant`,不得放寬」)。
- **AC-F1**:GIVEN 甲(0,2)、乙(0,3) 正向相鄰,WHEN 查詢 `Φ(甲)`,THEN 回傳 +3。
- **AC-F2**:GIVEN 5 條正向線、其中 4 條距1、第 5 條落於距2死區,WHEN 查詢,THEN 仍為 +12(第 5 條淨貢獻 0)。⚠️ **本條只是一個具體佈局抽查,不證明「第 5 條只能落在死區」** —— 那個宣稱已被 AC-MIX 推翻(見公式二)。
- **AC-F3**:GIVEN 5 條負向距1線(理論和 −5),WHEN 查詢,THEN 回傳恰為 **−4**(下界夾限,與 AC-R7 對稱)。

### ADVISORY

- **AC-PERF**:GIVEN vs01 同規模合成資料,WHEN 連續 1000 次查詢取平均,THEN 較上一版基準劣化 ≤ 20%(相對回歸門檻,非絕對毫秒 —— 避免 CI 環境雜訊誤判)。
- **AC-FEEL**:GIVEN 校準完成後的正式數值,WHEN 人工試玩,THEN 無加成約 4 下擊殺、有加成約 3 下(校準目標,見 Tuning Knobs)。

⚠️ **無法自動涵蓋的兩項**:①R5「不需另外設計」這句話本身(屬 code review 範圍);
②R2 的「兩份曼哈頓實作是否仍只有兩份」—— 那是結構性質,黑箱測試看不到,
須以 grep/程式碼審查守著(見 R2)。
✅ **原列的第①項「公式二 `N=4` 幾何論證」已於 2026-08-31 移除** —— 該論證本身已被推翻
(見公式二),不再有需要涵蓋的宣稱。

## Open Questions

| # | 問題 | 阻擋什麼 | 誰決定 |
|---|---|---|---|
| ~~OQ-1~~ | ✅ **已裁決,自待決清單移除(2026-08-31)**。戊沒有任何關係線是**角色設定的既定裁決**,非本系統的待決問題 —— `design/narrative/characters.md` 第三節,Canon level: Established,經管理者三輪裁決定案,並已明確接受「麥子健站哪都一樣」這個玩起來的後果。⚠️ **本輪有專家提議重新檢討,經 `creative-director` 判定為把定案送回重審而擋下。** 要改先改 `characters.md`。殘餘的「戊需 9 下才殺得掉 E5」屬**基礎數值**問題,已併入 OQ-2 | — | 已結案 |
| OQ-2 | 基礎數值未達 4 下擊殺目標(現況 6 下,戊/E5 為 9 下)。何時校準? | 不阻擋本系統實作 | 管理者(既有裁決:先玩過再談數值) |
| OQ-3 | 🔴 **上游回傳無上限實數,本公式吃 int `amp`,換算方式未定**(四捨五入/分桶/只取符號)。現況恆為 1 = 只取符號、丟棄全部量值。**本輪刻意不決**:`amp` 值域須等卡牌系統定案,現在決定就是猜 | 不阻擋本系統實作;**阻擋 Interactions ① 的即時查詢遷移** | 技能卡牌系統(#6) |
| OQ-6 | 🔴 **陣亡前的 `Φ` 值無人擁有。** 呈現「因為某人陣亡而變化了多少」需要死前/死後差值,但 R10 使本系統無狀態(此為正確設計,不改)。誰負責快照? | 不阻擋本系統;**阻擋陣亡回饋的呈現** | `/ux-design` + `lead-programmer` |
| ~~OQ-7~~ | ✅ **已於 2026-08-31 驗證結案**(管理者當日裁決「現在就排時間驗證」,同日執行)。對立佈局**完全可達**(最嚴苛情況仍有 57 格可用,最多只需 4 格),且夥伴**可同時保持距離 ≥3 與正在攻擊**(近戰 9～12 格、遠程 26～33 格)。**結論:對立路線沒有隱藏的第二層代價,差距就是帳面的 50%。** 詳見 R6 裁決理由 | — | 已結案 |
| OQ-4 | 「單位不在棋盤上仍可查詢」的邊界目前僅靠單一呼叫路徑保證,無測試守著 | 不阻擋;第二個呼叫端出現時會成為風險 | `lead-programmer` |
| ~~OQ-5~~ | ✅ **已於 2026-08-31 執行完畢**(管理者當日裁決「現在一併修掉」)。上游 `tactical-combat-system.md` 三處已修:①Core Rules #5 步驟①與 Formulas 變數表的「對攻擊雙方」改為**攻擊方單向**(2 處);②Core Rules #7 與 Tuning Knobs 交叉校準段的「**唯一**機制」限定為**主要**機制,並載明 `Φ` 只覆蓋輸出通道、不覆蓋承傷通道,另註戊的角色層級例外;③該文件 OQ-6 的 HUD 面板由 3 個增為 **4 個**(補入好感度逐條貢獻面板) | — | 已結案 |

✅ **OQ-7 驗證方法留存(2026-08-31 執行完畢,未動 `src/`,純分析)**:
以 `vs01_terrain.txt`(13×6,4 格阻擋)與 `vs01_roster.txt` 敵方起始座標窮舉全部站位,
對每個「可攻擊到敵人」的站位,計算距離 ≥3 的合法夥伴格數,以及其中有多少格
夥伴自己也能攻擊。⚠️ 因現行關係表每人至多 1 條線,本驗證以**幾何可行性**為對象
(不綁定特定角色),故結論對未來任何 2～4 條線的配置皆成立。完整數字見審查紀錄檔。

