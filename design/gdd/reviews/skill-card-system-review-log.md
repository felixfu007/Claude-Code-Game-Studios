# `skill-card-system.md` 審查紀錄

> **紀錄壓縮協定**:一輪的發現被後續輪次取代後,其 `Summary` 散文收斂為修訂表的表列,
> 段落級敘述刪除 —— 完整歷史留在 git,活檔只承載**當前**輪次所需的篇幅。

## Review — 2026-09-08 — Verdict: MAJOR REVISION NEEDED

Review target: **GDD body(full 模式)** —— 計入兩輪收斂連續數
Scope signal: **L**(依賴 5 系統 + 3 份 ADR;公式 4 條;需**修訂**一份既有 ADR,**不需新 ADR**)
Specialists: `game-designer`、`systems-designer`、`qa-lead`、`ux-designer`、`narrative-director`、`godot-specialist` + `creative-director`(資深綜整)
Blocking-now: **17 叢集**(design-content 17 / propagation 0) | Deferred: 4 | Advisory: 約 15
Findings by location: **native-design 17 / review-added 0 —— accretion signal: 未觸發**(第一輪,定義上不可能觸發)
Prior verdict resolved: First review

### 管理者四項裁決(全部同輪落地)

| # | 裁決 | 連帶結果 |
|---|---|---|
| 1 | **丙類留在 MVP** | 推翻協調者與 `creative-director` 的「移出」建議。連帶必須登記第三份行數例外 |
| 2 | **允許改動戰棋系統的程式碼與設計文件** | 甲類的 `ATK_eff`/`DEF_eff` 存取層得以存在 |
| 3 | **打牌算「改變盤面」,已算好的查詢結果一律作廢重算** | 觸發 ADR-0001 修訂(見 OQ-2) |
| 4 | **維持每回合出牌數不限**,改寫失真的 Player Fantasy 場景 | 零行數成本;與「稀缺是時間上的」既有裁決一致 |
| — | **丙類不得翻轉 canon 極性**(裁決 3 的同批) | 🔴 **導出未預期結果:丙類對傷害的影響變成零,而那是設計。** 甲乙本為正、丙丁禁翻正 → 所有合法丙類寫入皆不改極性 → 不改變任何傷害。**分工收斂為「甲類負責戰鬥、丙類純粹負責劇情」,連帶消滅 4 項發現。** ⚠️ 管理者原文只點名丙丁,文件按對稱延伸至甲乙,已明文標示待確認 |

### 修訂表(叢集 → 處置)

| 叢集 | 處置 |
|---|---|
| Overview 整段停在裁決反轉前(4 位專家獨立命中) | 整段重寫 |
| Player Fantasy 關鍵時刻賣的兩難無規則支撐 | 重寫為時間軸取捨 |
| **「上游一字不改」為假**(協調者自身錯誤,`godot-specialist` 以檔案證據推翻) | 更正為三項判定表:傷害公式不變 ✅、無需結算步掛鉤 ✅、「一字不改」❌ |
| **ADR-0001 `board_version` 自我矛盾**(寬 1 處 vs 窄 4 處,窄的一處在 `control-manifest.md`) | 新增 Core Rules 六 + 登記為**第二把鎖**;修訂 ADR 列 OQ-2 |
| **`Φ` 夾限被上游四處宣稱為「唯一擋住數值的東西」,甲類讓它失真** | 要求 `Σ Δ` 累計夾限 + 尺度校準;通知上游列 OQ-4 |
| 甲類缺合法作用對象規則、能否打敵方未定義(疊加順序差 3 點防禦) | **僅限我方單位**(由檔頭「具名角色互動」判準直接導出),連帶三問不必回答 |
| 退回卡池時機兩處互斥 | 收斂為「本場已用區」—— 唯一同時滿足四處既有承諾的讀法 |
| **丙類保護寫錯位置**(協調者自身錯誤) | 釐清:戰鬥端是**結構性**保護(`Φ` 只加總 canon 線);**敘事端無防護**(池對 canon 零認知、append-only) |
| **形狀特徵不套折扣 → 丙類約 3:1 壓過支援對話**(支援對話全戰役 35 次 / 10 對 = 每對 3.5 筆) | 以**丙類張數上限**解決(裁決 1 同批)—— 不需動任何已核准文件 |
| AC-8 為「永遠會通過」的測試 | 拆為 AC-8a(負面)+ AC-8b(正面控制組);註明**無第二道防線可驗**,與 AC-7 不同 |
| AC-2/AC-5/AC-14 不可獨立測試 | 三條就地改寫(鎖定參數、鎖定驗證管道、給可觀測判準);新增 AC-7b |
| 七條規則零 AC 覆蓋 | 明文登記為實作期必補 |
| 敏感度證明方法遺漏「改名移出收集範圍」機制 | 補上,並要求把 AC 拆到多個測試檔 |
| **缺 UX Flag** | 補上;`ux-designer` 三項改列 OQ-7 交 `/ux-design` |
| **卡面「沿用 480×270 既有規格」為假宣稱**(該檔對卡面零命中) | 刪除 + 記下方向性風險(手牌屬介面層,而 128×128 立繪是為世界層整數縮放設計的) |
| 游標系統早已為卡牌備好表面類型而文件未引用 | 列 OQ-9(含「卡牌選取區」奪權門檻值無人填、手牌動態抽換的登記/取消登記未定) |
| 回合開始三件事未定序 | **先遞減與移除,再補牌** + 實作提醒(須同一函式內依序,不得寫成兩個獨立訊號監聽者) |

### 本輪的結構性結論(`creative-director`)

**17 叢集裡有超過三分之一,成因是同一件事:撰寫期發生了一次 MVP 範圍反轉,而流程沒有任何一步要求回頭走一遍全文。**
Overview、Player Fantasy 關鍵時刻、公式一的「一字不改」全部停在反轉前,而文件其餘部分寫得相當嚴謹。
**這份文件不是寫壞了,是流程少了一步。** 建議寫進 `/design-system`:

> 撰寫期若發生範圍裁決反轉,該文件不得直接進 `/design-review` —— 須先執行一次**以該裁決為錨**的全文回掃。

### 遺留(不阻擋,依劑量規則②降級為實作期處理)

- 行數超出 400 上限 → **已登記第三份明文例外**(管理者裁決;代價與替代方案已於裁決前明文告知,記於計畫檔第六節②)
- **只剩一輪審查額度。** 第二輪的殘留項一律降級為實作期處理
- 開工前三把鎖:OQ-1(基準表,已指派未執行)、OQ-2(ADR-0001 修訂)、OQ-3(戰棋存取層)
