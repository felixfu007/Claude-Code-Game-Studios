# 專案現況 —— 一頁式

**更新日期**:2026-09-04(第四段工作收尾)
**寫給誰看**:專案管理者。**這一頁刻意不使用任何內部代號。**
**更新時機**:每次工作階段收尾時重新生成。開會前看這一頁,不必讀別的。

---

# 🟢 今天開了你選的那一張,做完了,而且它做完解鎖了後面一張

游標系統 **14 份工作單完成 6 份**,自動測試 **345 → 356 條**,今天沒有退步。

---

## 一、開工前先花十分鐘證明「這件事驗得到」,才動工

那張工作單的價值幾乎全在一條自動測試上:確認游標的顯示層沒有被畫面縮放弄歪。
**弄歪的後果不是歪一點,是游標畫在離滑鼠一千到三千多像素外 —— 系統實質失效。**

工作單自己寫了警告:這條測試在我們的測試環境裡不見得做得到,**做不到就要誠實登記,
不要用一個「測得到但什麼都沒驗到」的替代寫法**。所以第一步不是寫程式,是回答這個問題。

結論是**做得到**,但過程中撞到一個陷阱:

🔴 **要測不同螢幕尺寸,最直覺的那個指令,在我們的測試模式下什麼都不會做,而且不會報錯。**
照直覺寫的測試會**四種尺寸全部跑在同一個尺寸上,然後四項檢查全部通過** —— 綠燈、一種尺寸都沒測到。

**這個陷阱如果沒先量到,今天會做出本專案第四個「看起來有測、其實什麼都沒測」的東西。**
正確寫法已量出來、寫進規範。

---

## 二、專家做完之後,我覆核抓到一件事

專家做了正確的事:**他故意把正式程式碼弄壞,來證明那條測試真的會失敗** ——
這是防「假警報器」最有效的手段,他主動做了。

**但他的證明只涵蓋 11 條測試裡的 1 條。**

原因是測試工具的一個行為(**一條測試失敗,同一組後面的測試就不跑了**)。
他那次跑到第 2 條就停了,而**真正扛驗收標準的是第 3 到第 6 條 —— 那四條當時根本沒被執行。**

⚠️ **他的回報沒有一個字是假的**(「弄壞之後測試紅了」完全屬實),
**但會讓讀的人以為那四條驗過了。** 這種「字面為真、方向誤導」沒有任何自動檢查抓得到。

**我逐條重做:四種螢幕尺寸的檢查,全部親眼看到它們在程式被弄壞時變紅**,不是推論。
做完全部還原,再跑一次全套確認乾淨。

📌 **通則已寫進規範**:一次「弄壞測試」只能證明跑到第一個失敗為止的那些。
**要證明整支測試檔每條都有效,需要一條一次。** 以後承諾這種證明前要先把成本算進去。

---

## 三、順手抓到的第三個假綠燈陷阱

我試著單獨跑一條測試、字串打錯了。工具印出「找不到任何測試,中止」——**然後回報「執行成功」**。

**亦即:篩選字串打錯 = 一條測試都沒跑 = 自動檢查判定通過。** 目前我們沒用到篩選功能,
所以現在不受影響 —— **但任何人在測試指令加上篩選的那一刻,這個陷阱就活了。** 已寫進規範。

---

## 四、你今天裁決的兩件事,都已經落地

| | 你的裁決 | 落到哪裡 |
|---|---|---|
| 1 | **接受不開視窗的測試**,但把「真的開視窗用眼睛看」記到下一張 | ✅ **已寫進下一張工作單**,不是寫在今天這張的結案紀錄裡 |
| 2 | **顯示層的疊放順序數字維持現狀**,不寫進架構文件 | ✅ 程式碼裡已標明「這是實作時選的,不是架構規定」,後人分得出來 |

🔴 **第 1 項我特別處理過**:義務如果寫在一張**已經結案**的工作單裡,等於沒寫 ——
沒有人會再打開一張標著「完成」的單子。**所以它寫在下一張的執行位置,不是今天這張的紀錄裡。**
(本專案的技術債登記表,當初就是因為這個問題才建立的。)

---

## 五、順手修掉的既有問題

**工作單總表的「狀態」欄 14 列全部寫著「待辦」**,實際上已有 5 張完成、1 張卡住。
我沒有只補今天這一列 —— **只補會看到的那一處,正是本專案登記在案的重複失誤形狀。**
整欄改成從工作單檔案自動重建,不手抄。同份文件的「下一步」還寫著「請執行拆解工作單的指令」,
而工作單兩天前就存在了 —— **照它做的人會重跑已完成的事**,一併更正。

---

## 六、需要你裁決的事:**三件**(皆為舊項;今天早上的第四件已在本次處理掉,見下一節)

| | 事情 | 為什麼要你決定 |
|---|---|---|
| 1 | **一次故障就永久靜默死機的隱患** | 專家判可延後,但後果是「開發時大聲報錯、玩家手上無聲卡死」,而這台機器**結構上驗證不了**發行版的行為 |
| 2 | **一個替代數值會讓後面某張工作單的測試假性通過** | 根因在一份**你先前凍結**的工作單,要你決定掛在哪、要不要現在動 |
| 3 | **游標圖層那兩項條件,要不要改成「不合格就根本註冊不進來」** | 現在只是一條寫在文件裡、沒有程式會檢查的紀律 |

**三件都不擋現在的進度。**

---

## 六之二、你 9/1 的畫面裁決 —— ✅ 今天有人擁有了,而且第一張已經做完

**早上還列在「需要你裁決」。你說「好」之後我建了工作單,下午第一張就完成了。**

**做完的是**:把畫面縮放從「引擎自動處理」切成「我們自己管」,並寫出決定棋盤放大幾倍、
擺在哪裡的程式。**設定與程式在同一次變更裡完成** —— 只改設定會讓畫面壞掉。

**驗證(我自己跑的,不是轉述)**:自動測試 356 → **371 條**,零退步。
在 2K 視窗實機擷圖,**畫面尺寸 2560×1440** —— 舊模式下是 2400×1350,那圈差額是引擎
**畫不進去**的範圍。現在整個視窗都能畫了,**這正是這次切換的核心理由**。

### 🔴 但畫面上的介面現在錯位了,而且這是可預見的

我打開擷圖用眼睛看,發現操作提示橫條**從螢幕底部跑到了左上角**。

**原因**:場景檔裡那些介面元件的座標是「棋盤畫布座標」。你 9/1 裁決介面改用**螢幕實際解析度**
當基準的那一刻,這些座標就已經失效了 —— **今天只是讓它顯現出來。**

受影響 **6 個元件**(逐一清點,非估計),兩種不同的壞法:
5 個跑到左上角;勝敗橫幅位置其實正確,但**尺寸小到不成比例**。

✅ **你已裁決:併進第二張,並把它的範圍從「只訂字級規則」擴成「訂規則 + 順手把這 6 個修好」。**
理由是不讓建置畫面在兩張工作單之間一直是錯的。

⚠️ **在第二張做完之前,任何擷圖都會帶著一個錯位的介面。** 不是壞掉,是還沒排。

### 值得記住的一件事

**機械檢查全部通過**:尺寸對、不是空白、顏色分布正常、像素格對齊。
**是打開圖用眼睛看,才看到提示列跑到左上角。**

專案規範裡那條「人要打開圖確認它畫的是它宣稱的東西」,今天付出了它的價值。

## 七、下一步

**你已裁決:下一段做「字級規則 + 把 6 個錯位的介面元件修好」。** 估 3–4 小時。
做完之後建置畫面會恢復正常,而且「介面在各種螢幕上該多大」這個問題有了確定答案。

**之後還可以開的有三份,都在游標系統**:

- **把系統內建的滑鼠游標關掉、改成我們自己畫的** —— 玩家第一眼會看到的東西。大(8–10 小時)。
  🔴 **今天有個新發現讓它更該排在畫面之後**:切換之後,游標系統讀到的滑鼠座標
  **尺度變了 4 到 8 倍**(從「棋盤格座標」變成「螢幕實際像素」)。
  滑鼠要移動多少距離才搶回控制權,那個手感數字是用像素定的 —— **先做游標會調錯。**
  今天已經先做畫面,所以這個坑避開了。
- **開機時的設定自檢** —— 小(2–3 小時),獨立、不擋別人。
- **換畫面時的游標交接** —— 中(4–6 小時)。

---

## 八、今天新增一件待決,但不急

**最小視窗尺寸(960×540)現在是由戰鬥畫面的腳本設定的。**

今天沒問題 —— 戰鬥畫面就是遊戲啟動時第一個載入的東西。
🔴 **但將來如果加了標題畫面或主選單放在戰鬥之前,最小尺寸在那些畫面會失效**,
玩家可以把視窗縮到很小。屆時需要有個「整個遊戲都管得到」的地方來設定它。

**現在不用決定,加標題畫面的時候會撞到。已經寫在交接文件裡。**
