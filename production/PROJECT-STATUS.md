# 專案現況 —— 一頁式

**更新日期**:2026-08-31(設計審查與核准)
**寫給誰看**:專案管理者。**這一頁刻意不使用任何內部代號。**
**更新時機**:每次工作階段收尾時重新生成。開會前看這一頁,不必讀別的。

---

# 🟢 四個系統的設計今天全部定案,可以開始寫程式了。

**但還開不了工,卡在一份技術文件上** —— 它缺六項要在真引擎上跑一次的驗證。

---

## 今天你決定了什麼

| | 你的決定 | 結果 |
|---|---|---|
| 1 | 好感度—位置系統:可以開始寫程式 | ✅ 設計定案 |
| 2 | 戰棋移動與交戰系統:可以開始寫程式 | ✅ 它卡了 13 天,擋著它的規則今天被你推翻 |
| 3 | 兩份太長的設計文件:記成例外,不拆 | ✅ 省下一次不小的重工 |
| 4 | 「數字必須是引擎跑出來的」規則:要附上跑過的檔案 | ✅ 從「相信我」變成「你可以自己打開來看」 |
| 5 | 已知的程式缺陷:寫一條會失敗的測試釘住它 | ✅ 測試會一直紅,直到缺陷被修 |
| 6 | 「敵人數量上限」:**你問了「這很重要嗎」** | ✅ 查完發現不重要,取消 |

---

## 🔴 你那一問,是今天最有價值的一次介入

我說有「三件事擋著開工」,其中一件是敵人數量上限沒人負責。你問:**「這很重要嗎?」**

查完:**不重要。** 全專案三個地方提到那個數字,全是規劃用的,**沒有一行程式、一條測試在等它**;
而且它擋的那件事本來就被別的擋著。

在你問之前,已經有兩個人在算「這數字應該是多少」——我填了一個(越權,已撤回)、
專家填了另一個(理由自相矛盾,已退回)。**兩層都在算數字,沒有一層問這數字要不要存在。**
**專家不會問,因為前提寫在我給他的任務裡了。這一層的下一層只有你。**

---

## 🔴 一件跟你的賣點直接相關的發現

垂直切片要證明的是**「好感度真的會改變戰鬥結果」**:不用好感度打 5 回合,用了打 4 回合。

今天用引擎跑真實資料後,這句話要改成:**差不差那一回合,要看你有沒有把人挪開。**

- 丙和丁在中後段拉開 → **4 回合**,比文件寫的還輕鬆。
- 兩人從頭到尾不動 → **5 回合,跟不用好感度打平。**
- 缺口**只有 1 點傷害**,丙或丁**其中一人**、從第 3 回合起拉開就夠。

⚠️ **這不是壞消息,但它改變了你試玩時該看什麼。** 原本說法是「每回合白拿」,
實際是「**你得動手才拿得到**」——那其實更貼近你要的體驗。
🔵 **請留意:你會不會自然而然把丙丁分開?** 會的話,這個設計就成立了。

---

## ✅ 2026-09-01:那兩份技術文件都核准了

**你今天按了核准。擋著開發的技術文件問題結束了。**

| 換到什麼 | |
|---|---|
| ✅ **游標/高亮系統可以開工** | 四個已核准系統裡**第一個真正解鎖的** |
| 🔴 **戰棋系統仍然不行** | 它還卡著**「我方基準數值表沒有人負責」** —— 不是在等誰,是沒指派給任何人。那份文件的公式二沒有它就寫不出來 |

**核准的意思**(門檻文件寫的):可以安全地開始照這兩份文件寫程式了。**不是**這兩份完美無缺。

**過程摘要**:一道從專案成立以來從沒對這兩份跑過的覆核,今天跑了,兩份都判「有疑慮」。修完之後又各跑一次窄範圍複驗 —— **戰棋那份的複驗判不通過**,抓到三處我寫錯的事實,改完才放行。引擎測試:戰棋四支全過,游標六項過五項、一項因為這台機器沒有 Steam 環境測不了(已登記,不影響設計)。

🔴 **兩條寫程式時必須遵守的規矩,隨核准生效**:
①游標那一層必須自己獨佔一塊,不能跟介面共用(做錯的話游標會偏差一千到三千像素,等於整個系統失效);
②按住方向鍵的重複訊號,程式要自己過濾掉(引擎不會幫你過濾,已實測)。

---

## 【以下為核准前的紀錄,保留供追溯】現在真正卡在哪:是兩件,不是一件

規則規定「引用草案文件的工作一律不准開始」。**還是草案的技術文件有兩份,不是一份。**

| | 哪一份 | 缺什麼 |
|---|---|---|
| **一** | 戰棋查詢那份 | ①它自列六項驗證未做 —— **其中四項要在引擎上跑,另兩項不是**(一項是要寫的測試、一項是程式碼審查,而那部分程式還沒寫,現在無從做);②一道「架構總監覆核」對這份沒跑過 |
| **二** | 游標架構那份 | 它管的是四個已核准系統裡的一個。**它那條「整個畫面只有一套座標系」的假設,2026-09-01 已經兩半都查完、你設的前置條件達成了**(見下)。**但它還是草案** —— 缺的跟第一份一樣是那道從沒對它跑過的「架構總監覆核」 |

🔴 **2026-09-01 更正了三句會害你做錯決定的話:**

1. 這一節原本標題是「**只剩一件**」,正文也說那份文件是「唯一還擋著開工的東西」。
   **不對** —— 拆掉它,游標那份還是擋著,你還是開不了工。
2. 原寫「一道架構總監覆核從沒跑過,而且**五份文件全都沒跑過**」。**不對** ——
   **8/24 對存檔那份跑過了**,結果是「有疑慮 → 窄範圍複驗 → 通過」,留有完整紀錄。
   **差別**:不是要從零建一道流程,是有現成的可以照抄。
3. 原寫那份文件「從未驗證」。**不對** —— 引擎專家查過它,那次還產出三條新規矩。
   缺的是「在真引擎上跑」那一層。**前者要從零審一遍,後者跑四支測試。**

---

## 這些東西可靠嗎

| 檢查 | 結果 |
|---|---|
| 自動測試 | **241 條,240 過,1 條紅的** |
| 那條紅的 | **故意的。** 它釘住一個真實存在、現在就在出貨的缺陷:關係資料開場讀一次就凍住,對話卡牌中途讓兩人翻臉,戰鬥計算看不到 |
| 會不會變成沒人看的紅 | 不會。哪一條、預期幾條、什麼情況代表真的壞了,都寫進自動檢查的設定檔 |
| 我有沒有自己重跑 | **有,收尾前跑過,與紀錄完全一致** |
| 今天的數字怎麼來的 | **引擎實際跑專案自己的程式**,不是算出來的 |

⚠️ 修好那條紅的需要「好感度帳本系統」,還沒開始。目前沒有小改動能讓它變綠。

---

## 🔴 今天最該記住的一件事

**每一層都出過錯,而每一層的錯都是被下一層抓到的** —— 我心算錯一次、越權填數字一次、
派了一個沒有執行能力的專家去跑引擎;專家甲算式得 6.5 卻寫 10;專家乙**引用了一條不存在的規則**,
而它正好推翻你親自的裁決;專家丙誤判「上限被觸發了」。三個專家的錯都是廉價檢查抓到的
(算一次除法、跑一次搜尋、讀一次程式碼)。

**只有一件事沒有下一層能抓** —— 「這件事到底重不重要」。那一層是你。

📌 而且上面六個錯,**沒有一個是自動測試抓得到的**。測試檢查「程式做的事對不對」,
不檢查「這件事該不該做」。

---

## 還沒解決、已經登記的

| | 問題 |
|---|---|
| 🔴 **一份技術文件擋著全部開工** | 六項驗證要跑、一道覆核從沒跑過(五份文件皆然) |
| 🔴 **關係資料開場凍住的缺陷** | 已有紅色測試釘住,修它要等好感度帳本系統 |
| 🟡 **下次架構檢查很可能判「不合格」** | **這是判定基準改變,不是退步。** 六輪來擋住不合格的唯一理由是「戰棋文件還沒核准」,今天你核准了。缺口數字一直沒變 |
| 🟡 **敵人強度的來源** | 公式與係數是設計好的、有測試覆蓋;缺的是遊戲執行時沒走這條路,數值是手打進資料檔的。⚠️ 這一頁上一版寫「五個裡只有兩個符合公式」,**那是錯的**——五個全部符合,只是用三種強弱係數 |
| 🟡 **兩份文件超過長度上限** | 上限 400 行,這兩份你已裁決記成例外。**⚠️ 長度上限沒有自動檢查** —— 較長那份超限 2.5 倍,是六天後被順手量到才發現;而它在你批准例外的**同一天又長了 39 行**,同樣沒人發現。所以這裡不再寫行數,只記「這兩份是例外」 |
| 🟡 **兩個必需系統不在計畫內** | 「營火存檔」與「跨線告知」,負責的系統都被降級 |
| 🟡 其餘 | 好感度數字寫在程式裡;棋盤沒格線;心靈對話約 80 段(原承諾 3 段);腐蝕變體立繪變成必畫 |

---

## 🔵 下一步:兩條路

你的一年計畫說現在該做第 2 段「**能動**」—— 讓棋子真的能走能打,而且好感度真的改變結果。
**那三個系統今天全部核准了,計畫要你做的下一件事就是把它們寫出來。**

| | 做什麼 | 換到什麼 |
|---|---|---|
| **甲** | 把那**兩份**技術文件都推到定案 | 戰棋那份跑四項引擎驗證;游標那份要你重新裁決一條假設(見下)。**兩份都拆掉才能真的開始寫** —— 只拆一份,還是開不了工 |
| **乙** | 往前設計技能卡牌系統 | 那是第 3 段的事,第 2 段會繼續空著 |

✅ **停損點在第 8 個月 —— 2026-09-01 你已經同意了。**

你同意的是**做法**:第 6-8 個月那階段結束時必須拿出實測數據;判定看「用好感度佈局的人打起來
是不是真的比較好」(回合數與重傷數);**沒過就停止投入第 4、5 階段**,改成重新設計核心機制
或結束專案。

你**沒有**同意那個數字(現在寫的是「回合數減少 ≥20% 或零重傷」)。理由是專案自己的設計文件
早就寫明:**那個數字是照「公平對戰」算出來的,而這款遊戲設計成「以弱勝強」,將來一定要重算。**
簽一個文件自己說會變的數字,只會在第 8 個月變成「你同意的到底是哪個數字」的爭議。

🔴 **配套的一條期限,是這個做法能成立的前提**:
**數字必須在第 6 個月結束前定死,而且必須在看到任何一筆測試數據之前定死。**
允許數字晚點定,不允許看完數據再定 —— 後者會讓整條約定變成裝飾:表面上還在,
實際上門檻是照著結果挑的,而且從外面看不出來。

⚠️ **同時登記三件連帶的事**(不影響上面的同意生效,但要處理):
① **這份計畫從來沒寫過「第 1 個月」是哪一天** —— 停損約定沒有起算日;
② 計畫表還在說專案處於第 0 個月,但**第 1 階段其實已經做完了**;
③ 判定要跑,得**先請人故意用錯的方法打同一張關卡**拿基準來對照 —— 那是一筆要排進去的人力。

**目前規模**:`src/` 底下的遊戲程式共 **3,791 行**、測試 **241 條**,
但**四個已核准系統的正式實作都還沒開始**(現有程式是驗證用的骨架與棋盤/戰鬥雛形)。
> 🔴 上一版寫「約 1,650 行」。**那個數字對不上任何一種算法**(只算玩法層 2,081、只算介面層
> 1,573、扣掉最大的檔 2,880),已改為明確定義的「`src/` 底下的 GDScript 總行數」。
> **這件事會影響你的判斷**:這句話的作用是告訴你「投入還很淺、轉向代價小」,而它低報了一倍多。

---

## 之後會來問你的(都不急)

**🔴 這兩件不「不急」,它們正擋著開工:**

- ~~**第 8 個月停損點,要不要同意**~~ —— ✅ **2026-09-01 已同意**(同意做法,數字另定,
  且數字須在第 6 個月前、看到數據之前定死)。詳見上方。
  **新接上來的一件**:那個門檻數字本身要在第 6 個月前來問你一次。
- ~~**游標架構的一條假設要重新裁決**~~ —— ✅ **2026-09-01 已解決,你設的前置條件達成了。**
  白話說:那份文件寫著「整個畫面只有一套座標系」,但你 8/25 要求畫面分兩層,分完就有兩套。
  **兩半都查完了:**
  **棋盤那層** —— 8/27 就量過了,只是沒人把結果寫回文件(這是這次才發現的)。開真視窗、
  真顯示卡、四種螢幕尺寸,換算來回對了 20 次錯 0 次,另有人用四個最小場景獨立再驗一次。
  **文字那層** —— 這次量的,而且答案跟我們問的問題不一樣:**決定游標會不會壞的,不是
  介面用什麼尺寸排版,而是「游標圖層有沒有自己獨佔一塊地方」** —— 而那份文件本來就是
  這樣設計的。四種螢幕實測全部正常。
  🔴 **但它留下一件必須做的事**:如果將來有人把游標和介面塞在同一塊地方,實測誤差是
  1440~3304 像素 —— 不是歪一點,是整個游標系統失效。**所以要寫一條會自動跑的測試盯著它**,
  不能靠人記得。
  ⚠️ **前置達成不等於那份文件就通過了。** 它還是草案,還要走正式的核准流程
  (其中一道覆核從沒對它跑過)。**這件事從「擋著開工」變成「排得進流程」,不是消失了。**

**其餘的可以慢慢來:**

隊友擋不擋路｜好感度強度怎麼換算(要等卡牌系統)｜腐蝕門檻與敵人強度的實際數字(要等你玩過)
｜咒印彩蛋｜兩個結局共不共用最後一關
