# 專案現況 —— 一頁式

> 寫給管理者看的。**沒有代號、沒有縮寫、技術名詞第一次出現就翻成後果。**
> 給接手工作的人看的那份在 `production/session-state/active.md`(很長、很精確)。
>
> **最後更新:2026-09-16 收工(第三批)**

## 今天下午電腦閃退了,原因查清楚了 —— 不是這個專案

下午 2 點 26 分,Windows 自己記錄了「記憶體不足」,一分鐘後桌面的檔案總管當掉
(你看到的閃退)。**機器沒有重開,是程式崩潰後自動重啟。**

**真兇是你另一個視窗開著的那個 Java 專案**(`D:\fcm2`)。它的一個 Java 擴充功能
壞掉了:每 1~2 秒重開一次語言分析程式,舊的從不關掉,29 分鐘內失敗了 407 次,
記憶體就這樣一路堆到滿。Windows 點名的前三名是兩個 VS Code 視窗和一個 Java 程式,
**沒有 Claude。**

你關掉那個視窗後,**釋放了約 14.6 GB**,機器從「只剩 3.8 GB」回到「剩 18.4 GB」。

> ⚠️ **下次再開那個 Java 專案之前要先處理,否則同一件事會再發生一次。**
> 這不是第一次了 —— 9 月 8 日發生過完全一樣的一次,連肇事的程式都相同。
> 最小的處理方式:那個專案同時裝了兩套互相打架的 Java 分析工具,關掉其中一套。

---

## 崩潰時正在做的三份工作,全部救回來並收尾了

當時有三份工作做到一半就斷了。檔案都還在硬碟上,但沒有備份、也沒有人確認過還能不能用。

| 那份工作在做什麼 | 現在 |
|---|---|
| 好感度的「加權讀取」——把歷史互動按時間遠近折算成一個分數 | ✅ 完成並存檔 |
| 戰鬥畫面的五個按鍵設定 | ✅ 完成並存檔 |
| 把卡牌牌組接上戰鬥畫面 | ✅ 程式完成、驗收通過,**但要你裁決算不算完成**(見下) |

我先跑了全套測試確認沒壞,才存檔並上傳。**四次存檔全部已上傳到雲端,本機沒有孤本。**

---

## 🔴 今天最值得你知道的一件事:一半的測試證明不了自己有用

**背景**:一條測試如果不管程式壞不壞都顯示綠燈,它就是裝飾品 —— 而且比沒有更糟,
因為它讓人以為有在把關。這個專案上個月才真的踩到:有 6 條測試全綠,結果一查,
**它們根本沒讀過要檢查的那段內容**。

所以今天的工作是:**逐條證明每條測試在程式被弄壞時真的會變紅。**

**結果**:47 條裡 **23 條證明成功**,**24 條證明不了** —— 而且證明不了的原因不是偷懶,
是結構上就做不到。它們檢查的東西(設定檔的內容、資料檔的內容、隨機數的重現性)
沒有可以動手腳的地方;硬要做,只能證明「電腦分得出兩個不同的數字」,
卻要冒讓其他 700 多條測試一起壞掉的風險。

> **這份清單的真正價值,是標出了哪些地方電腦幫不上忙、只能靠人去看。**
> 三位專家各自獨立走到同一個結論,理由已逐條寫進存檔紀錄,任何人都能自己查。

**目前測試總數 670 → 748 條**,我親自跑了全套:61 個套件全跑、零錯誤、零殘留,
唯一失敗仍是那條刻意留紅、你已核准的。

---

## 過程中抓到的三件事(都是「看起來正常、實際有問題」那一類)

1. **有人為了測試而故意把程式弄壞,清理時只清了一半。** 被弄壞的地方還留在檔案裡,
   而被暫停的測試恢復了 —— 結果會是「測試全部在跑,但測的是一份被動過手腳的程式」。
   **是那位專家自己發現並補救的,我當時只查到另外一半。**
2. **有一條測試明明出錯了,卻回報「通過」。** 原因是那類錯誤在畫面上很大聲,
   但不算在測試結果裡。這是這個專案記錄到的第三種同類型陷阱。
3. **有一個排序假設是錯的**,新寫的測試第一次跑就被抓出來 —— 正好證明了
   「新測試自己也要先跑過才能信」。

---

## ⚠️ 我自己犯的一個錯,已更正

有一則存檔說明的**標題和內文數字互相矛盾**(標題 24 是對的,內文寫成 25 和 22)。
存檔說明一旦上傳就改不了(專案禁止強制覆寫歷史),**正確數字已寫進接手紀錄**,
並在那裡註明哪一處有誤。

這正是專案守則反覆警告的「同一份文件出現兩個互斥數字」—— 而它就發生在
記錄這件事的那一批上。

---

## ✅ 你剛裁決的兩件,都已經做完了

### 一、「把卡牌接上戰鬥畫面」算完成 —— 你改了門檻

**你裁的**:完成的標準改成「**每條測試要嘛證明得了自己有用,要嘛寫下來為什麼證明不了**」。

**結果**:那份工作已標記完成。而且**這個標準不只用在這一次** —— 我把它寫成了
專案的正式規則,以後每份工作都照這個標準,不必每次重新問你。

> **你否決的那個選項會造成什麼**,這裡記一下供日後參考:原本的標準是
> 「全部證明完才算完成」。但那 15 條在結構上就證明不了,所以那份工作會
> **永遠停在未完成**,並且一路擋住後面所有依賴它的工作。

規則裡我也一併寫明了一件事:**這條規則沒有自動檢查。** 它換到的是
「做不到的地方必須寫在檔案裡、可以被查證」,**不是「不會有人敷衍」**。

### 二、那條站錯地方的測試,搬走了

**你裁的**:保留測試、搬離、另開一份工作單。

**結果**:已搬到卡牌資料的測試資料夾,並建立了新的工作單讓它有自己的負責人。

我要求專家驗證這是**純搬家**(不是搬的過程中弄丟或弄重複)。
驗證方式是:總測試數必須一模一樣,只有檔案數多一個。
**實測:總數維持 748 條、檔案從 61 個變 62 個** —— 純搬家成立,我自己也跑了一次確認。

專家順手做了一個判斷並寫進檔案:新檔案**不再向畫面程式借用資料檔路徑**,
改為自己寫。理由是這是一個資料層的測試,不該認識畫面程式;而且同資料夾裡
其他測試本來就都是自己寫的。**已知代價也一併寫進去了**(路徑若日後搬家,兩邊要各改一次)
—— 沒有藏起來。
---

## 下一步

兩件都裁完了,沒有待你決定的事項。下一份工作是**戰鬥選單畫面**(卡牌介面 17 份裡的第 7 份)。

工作隨時可以接續 —— 所有成果都已存檔上傳,本機沒有孤本。
