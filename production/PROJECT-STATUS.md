# 專案現況 —— 一頁式

**更新日期**:2026-09-04(整日收尾,含當日補跑的架構覆核)
**寫給誰看**:專案管理者。**這一頁刻意不使用任何內部代號。**
**更新時機**:每次工作階段收尾時重新生成。開會前看這一頁,不必讀別的。

---

# 🟢 今天完成四張工作單,自動測試 345 → 391 條,零退步

| | 做完的事 |
|---|---|
| 1 | **游標專屬顯示層** + 一條防它跑位的自動測試 |
| 2 | **畫面縮放改為我們自己管**(執行你 9/1 的裁決) |
| 3 | **字級規則 + 修好 6 個錯位的介面元件** |
| 4 | **換畫面時的游標交接** —— 讀檔、取消讀檔、開新局三種情況 |

**畫面縮放那一區兩張全部完成。** 游標系統 14 張已完成 7 張。

**第四張在做什麼**:玩家戰鬥中途讀取另一份存檔時舊戰場會被拆掉,**如果游標還指著一個
已經不存在的格子,畫面上的高亮框就會指向虛空。** 這張確保三種情況都不會發生。
**沒有寫任何新的遊戲程式** —— 功能上一張就做好了,這張是把「該怎麼用它」釘死成測試。

---

## 一、你今天裁決的六件事,全部已落地

| | 裁決 | 落到哪裡 |
|---|---|---|
| 1 | 接受不開視窗的測試,但把「開視窗用眼睛看」記到下一張 | ✅ 寫進下一張工作單的執行位置 |
| 2 | 顯示層疊放順序維持現狀,不寫進架構文件 | ✅ 程式碼註明「這是選的,不是規定」 |
| 3 | 補一張工作單讓 9/1 的畫面裁決有人擁有 | ✅ 建了,而且當天做完 |
| 4 | 先把字型裝進專案 | ✅ 已裝,含來源、版本、授權 |
| 5 | 字級跟著棋盤一起放大 | ✅ 已實作,規則寫進設計文件 |
| 6 | 開新局與讀檔走同一個入口 | ✅ 寫進架構文件,並**依你的追加要求補跑了覆核**(見第三節) |

⚠️ **第 5 項的代價我再說一次,因為它會被人當成 bug**:某些筆電解析度(1366×768)
的字會**跟最小視窗一樣大**,相對螢幕偏小。**你是看過三個方案完整對照表後選擇接受的。**

---

## 二、今天擋下的兩件事,形狀一模一樣:看起來成功,實際什麼都沒做

**(一)派工前的查核,這次擋下的是整整兩張工作單。**

| 下一波候選 | 結果 |
|---|---|
| 換畫面交接(今天做的這張) | ✅ 要驗的東西完整存在 —— **所以選了它** |
| 開機時的設定自檢 | 🔴 3 項檢查裡 **2 項**在檢查一張**不存在的表** |
| 把系統游標關掉、改成自己畫 | 🔴 8 項檢查裡 **4 項**驗不到(詳見第四節) |

**沒查就照順序做下去,那兩張都會做完、全綠、然後什麼都沒驗到。** 專案裡已經有三次這種情形。

**(二)本機那條跑測試的指令,從來沒掃過整合測試那一層。**

今天這張是**第一張把測試放進那個資料夾**的工作單 —— 位置放對了(專案規範明訂那類測試
就放那裡,而且列為最高等級的把關),但指令寫死只掃另一個資料夾。

結果:5 條測試寫好之後,指令回報**跟寫之前一模一樣的數字**,那 5 條的名字一次都沒出現,
**沒有任何錯誤訊息。** ⚠️ **推上雲端的自動測試沒有這個問題** —— 壞的方向是
**「本機比雲端寬鬆」**,而本機是大多數人唯一會跑的地方。
✅ 已修好並實測(386 → 391),並加註:日後新增任何測試種類都要同步這條指令。

---

## 三、你要求補跑的那道覆核:**裁決成立,但我的理由被換掉兩項**

那條「開新局與讀檔走同一入口」的規定是我寫進架構文件的,**而我沒跑專案規定的覆核關卡。
你要我補跑。**

| | |
|---|---|
| **你的裁決** | ✅ **成立,沒有反轉** |
| 我給的三項理由 | **兩項被換掉** —— 一項判為過度推論,一項方向對但論證寫錯 |
| 我當時最擔心的那一點 | **前提根本是假的**,一條早就存在的測試可以直接排除 |
| 覆核另外找到 | **兩個我沒看到的缺口**,都已補上 |

**最值得記的**:覆核找到的決定性依據,是設計文件裡**白紙黑字的一句話** ——
「與『新開局進入戰鬥』**走完全相同**」。**我繞了三層推理去論證它,而答案本來就寫在那裡。**

📌 **這回答了「補跑覆核值不值得」**:結論沒變,但不跑的話,架構文件會留下一條
**結論正確、依據脆弱**的規定 —— 下一個檢視的人會先撞到那兩條弱理由,然後合理地懷疑整條。

⚠️ 同一句話**還有半句已經過期**(「不需要新介面」,而後來的修訂真的加了一個),
**從 8/19 到今天沒有任何一輪審查點名過。** 已登記為不阻擋的待清項,並明文警告:
**將來要改只能改過期那半,不能碰「走完全相同」那半** —— 後者是你這條裁決的支柱。

---

## 四、🔴 需要你裁決的事:**五件**,其中兩件擋住下一步

| | 事情 | 為什麼要你決定 |
|---|---|---|
| **1** | **開機自檢那張:2 項檢查的對象不存在** | 那張表屬於**你先前凍結的**那個子功能。要嘛連帶把表做出來(等於解凍一部分),要嘛只做剩下 1 項 |
| **2** | **自己畫的游標那張:主角畫出來永遠是透明的** | 濃淡取決於一個**永遠是 0 的數值**(同樣來自你凍結的那個子功能)。而這張綁著「必須開視窗用眼睛看」的義務 —— **開了視窗,主角不會出現** |
| 3 | 一次故障就永久靜默死機的隱患(舊項) | 後果是「開發時大聲報錯、玩家手上無聲卡死」,而這台機器**驗證不了**發行版行為 |
| 4 | 一個替代數值會讓後面某張的測試假性通過(舊項) | 根因同樣在**你凍結的**那個子功能 |
| 5 | 游標圖層那兩項條件要不要改成「不合格就註冊不進來」(舊項) | 現在只是一條沒有程式會檢查的紀律 |

🔴 **第 1、2、4 三件的根都是同一個**:你 8/11 凍結的「滑鼠奪回控制權」那個子功能。
**它不是擋著不能做,而是「沒做,所以有三張工作單各缺一塊」。**

---

## 五、待決但不急的三件(已登記,無人被指派)

1. **場景檔裡的舊座標** —— 畫面是對的,但在編輯器打開會看到壞掉的版面。**屬技術慣例問題,不用你決定。**
2. **最小視窗尺寸的設定位置** —— **加標題畫面時才會撞到。**
3. **對話正文字型還沒選** —— **排對話畫面前必須選,而且要另訂一套字級規則。**

---

## 六、下一步

**下一步不是「開哪一張」,是「先回答第四節第 1、2 件」。**

剩下兩張都需要你的決定才能開工,而兩張的問題**指向同一個根**(第四節下方紅字)。
建議下次開工先花幾分鐘談那件事,**一次解掉三張工作單的阻塞**,而不是逐張裁決三次。
