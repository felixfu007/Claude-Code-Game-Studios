# 專案現況 —— 一頁式

**更新日期**:2026-09-04(整日收尾)
**寫給誰看**:專案管理者。**這一頁刻意不使用任何內部代號。**
**更新時機**:每次工作階段收尾時重新生成。開會前看這一頁,不必讀別的。

---

# 🟢 今天完成三張工作單,自動測試 345 → 386 條,零退步

| | 做完的事 |
|---|---|
| 1 | **游標專屬顯示層** + 一條防它跑位的自動測試 |
| 2 | **畫面縮放改為我們自己管**(執行你 9/1 的裁決) |
| 3 | **字級規則 + 修好 6 個錯位的介面元件** |

**畫面縮放那一區兩張全部完成。** 你早上裁決建立它,下午就做完了。

---

## 一、你今天看得到的改變

**操作提示橫條回到螢幕底部了** —— 早上它縮在左上角。回合狀態、勝敗橫幅也各就各位。
**遊戲文字現在用我們選定的像素字型**,不再是引擎預設字型。

**我親自打開 1080p 與 2K 兩張擷圖確認過**,不是只看機械檢查。

---

## 二、你今天裁決的五件事,全部已落地

| | 裁決 | 落到哪裡 |
|---|---|---|
| 1 | 接受不開視窗的測試,但把「開視窗用眼睛看」記到下一張 | ✅ 寫進下一張工作單的執行位置 |
| 2 | 顯示層疊放順序維持現狀,不寫進架構文件 | ✅ 程式碼註明「這是選的,不是規定」 |
| 3 | 補一張工作單讓 9/1 的畫面裁決有人擁有 | ✅ 建了,而且當天做完 |
| 4 | 先把字型裝進專案 | ✅ 已裝,含來源、版本、授權 |
| 5 | 字級跟著棋盤一起放大 | ✅ 已實作,規則寫進設計文件 |

⚠️ **第 5 項的代價我再說一次,因為它會被人當成 bug**:某些筆電解析度(1366×768)
的字會**跟最小視窗一樣大**,相對螢幕偏小。**你是看過三個方案完整對照表後選擇接受的。**
我已在設計文件與程式碼註解兩處寫明「這是裁決接受的,不要當缺陷修掉」。

---

## 三、今天三次「開工前查核」各擋下一件事

1. **游標那張**:要測四種螢幕尺寸,最直覺的指令在測試環境下什麼都不做**而且不報錯** ——
   照直覺寫會四種尺寸跑在同一個尺寸上然後全部通過。
2. **字級那張**:那個像素字型**根本不在專案裡**,驗收標準的檢查對象是空的。
3. **裝字型時**:第一次下載抓到 **0 bytes**,因為伺服器對大檔案不回傳內容**而且不報錯**。
   是驗檔案內容才發現的。

**三件的共同形狀:安靜地失敗,看起來像成功。**

---

## 四、我覆核抓到的兩件事

**一、專家的測試證明只涵蓋 11 條裡的 1 條。** 他的回報沒有一個字是假的,
但會讓人以為全驗過了。原因是工具行為(一條失敗,同組後面就不跑)。我逐條重做。

**二、介面錯位是打開圖用眼睛看才發現的。** 機械檢查四項全過。
規範裡那條「人要打開圖確認」,今天付出了它的價值。

---

## 五、需要你裁決的事:**三件**(皆為舊項,都不擋進度)

| | 事情 | 為什麼要你決定 |
|---|---|---|
| 1 | 一次故障就永久靜默死機的隱患 | 後果是「開發時大聲報錯、玩家手上無聲卡死」,而這台機器**驗證不了**發行版行為 |
| 2 | 一個替代數值會讓後面某張工作單的測試假性通過 | 根因在一份**你先前凍結**的工作單 |
| 3 | 游標圖層那兩項條件要不要改成「不合格就註冊不進來」 | 現在只是一條沒有程式會檢查的紀律 |

---

## 六、今天新增三件待決,都不急、都已登記

1. **場景檔裡的舊座標** —— 畫面是對的,但在編輯器打開會看到壞掉的版面。
   這已是第三個同型情況,**屬技術慣例問題,不用你決定**,但需要有人定調。
2. **最小視窗尺寸的設定位置** —— **加標題畫面時才會撞到。**
3. **對話正文字型還沒選** —— 只寫了「一般繁體中文字型」這個類別,沒指名。
   **排對話畫面前必須選,而且要另訂一套字級規則**(今天訂的是像素字專用)。

---

## 七、下一步

**畫面這一區沒有後續工作單了。** 下一波回到游標系統,三張都可以開:

- **把系統游標關掉、改成我們自己畫的** —— 玩家第一眼看到的東西。大(8–10 小時)。
  📌 **今天先做畫面是對的**:它吃的滑鼠座標尺度已經定案,先做它會把手感數字調錯。
- **開機時的設定自檢** —— 小(2–3 小時)。
- **換畫面時的游標交接** —— 中(4–6 小時)。

🔴 **今天還解鎖了一件事**:「棋盤旁邊有沒有空位」現在有確定答案 ——
**最常見的兩種螢幕邊區為零**。介面必須疊在棋盤上方。
**所有介面版面工作現在可以開始,而且不會白做。**
