# Story 007:寫入與讀取介面(七個公開入口 + 重入閘門)

> **Epic**:單一游標/高亮狀態系統
> **Status**:Complete(2026-09-03,兩方覆核後修正完畢)
> **Layer**:Core
> **Type**:Logic
> **Estimate**:L(約 8–10 小時)
> **Manifest Version**:2026-09-02
> **Last Updated**:[由 /dev-story 於實作開始時設定]

## Context

**GDD**:`design/gdd/cursor-highlight-state.md`(2026-08-13 第十六輪 Approved)
**Requirement**:TR-cursor-012, TR-cursor-013, TR-cursor-014
*(需求原文在 `docs/architecture/tr-registry.yaml`,審查時請當場讀最新版)*

**Governing ADR**:ADR-0005 單一游標/高亮狀態系統:裝置權威輸入架構(**Accepted** 2026-09-01)
**本 story 對應的機制**:機制十(寫入與讀取介面)

**Engine**:Godot 4.7.1 | **Risk**:HIGH
**引擎注意事項**:本系統的兩個核心領域(Input、UI)在 4.6 與 4.7 **各有一項直接相關的破壞性變更**。
🔴 **引擎判斷一律以 `docs/engine-reference/godot/breaking-changes.md` 與 `current-best-practices.md` 為準**
—— `modules/input.md` 與 `modules/ui.md` 停在 4.6,對這兩項變更**各自零命中**。

**控制清單規則(本 story 適用)**:
- 🔴 **必須**:公開寫入入口**絕不互相呼叫** —— GDScript 無 `try`/`finally`,這是唯一能保證閘門正確釋放的形狀。
- **必須**:拒絕一律回傳明確結果碼(如 `STALE_NOT_APPLIED`),**絕不靜默忽略、絕不回傳 `void`**。
- **必須**:兩種拒絕回饋在**感知上可區分** —— 兩者的正確補救動作相反。
- **必須**:兩條 reset 路徑寫成 `if` / `elif`,**不得**寫成兩個獨立 `if`。
- **禁止**:`public_cursor_write_entry_calling_another`
- **禁止**:`cursor_state_write_from_own_signal_handler`
- **禁止**:`independent_ifs_for_cursor_target_reset_policy`
- **禁止**:`call_deferred_for_cursor_retarget_deferral`

---

## Implementation Notes

*出自 ADR-0005 機制十:*

1. **七個公開入口掛重入閘門**:`arbitrate_device_authority()`、`apply_buffered_navigation()`、`set_target()`、`mark_pending_reresolve()`、`handoff_before_unload()`、`handoff_after_mount()`、`reseed_reclaim_on_focus_regained()`。
2. **六條私有路徑不掛閘門** —— 公開入口不得互相呼叫,共用邏輯一律下放私有方法由兩邊各自呼叫。
3. **`set_target(target, from_ui_action) -> SetTargetResult`** —— 雙輸入簽章,**不含碰撞箱幾何**。
4. **`mark_pending_reresolve(expected) -> MarkResult`** —— `STALE_NOT_APPLIED` 是明確回傳值;競態判定依賴 `CursorTarget.equals()` 的值語意。
   ⚠️ **呼叫端的順序義務(有陷阱)**:必須「**先存舊值 → 再寫新值 → 才呼叫本方法**」。
5. 🔴 **讀取介面刻意分成兩個獨立查詢,不是一個合併的「可否確認」布林** —— 合併會讓呼叫方**結構上無法**產生可區分的回饋,而兩種拒絕的正確補救動作相反(等待重新解析 vs 移動滑鼠取回權威)。
6. **回傳 `void` 的三個入口如何表達拒絕**:見 ADR 機制十對應段落,不得自行發明。

⚠️ **本 story 的 S-1 防禦論證依賴一項本機無法查證的引擎行為**(層 B:release 建置下 VM 是否中止所在函式)。debug 下已實測會中止。**取得匯出範本後應回頭複驗。**

---

## Out of Scope

- Story 009:三分支交接的呼叫慣例(本 story 只出入口本體)
- Story 014:`_reclaim` 的具體策略行為
- 🔴 **Story 005:`arbitrate_device_authority()` 與 `apply_buffered_navigation()` 的裁決主體**
  (2026-09-03 引擎覆核 N-7 補列 —— 原本漏列,而它正是本張最容易被誤讀的範圍界線)。
  本張產出兩者的**閘門、診斷計數、provider 前置檢查、drain 義務**(機制十);
  **「依權威決定套用什麼」屬機制六,是 Story 005 的範圍。**
  ⚠️ 連帶:**AC-32 無法由本張滿足**,且該缺口**沒有可靠的自動防線**
  (測試涵蓋覆核查過三種寫法皆不可靠),只能寫進 Story 005 自己的完成定義。

---

## Acceptance Criteria

*以下為 `design/gdd/cursor-highlight-state.md` 的條文**原文轉錄**,未改寫:*

- **AC-3**: **GIVEN** 滑鼠懸停/移動與 d-pad/類比搖桿導覽兩種輸入來源,**WHEN** 分別觸發有效輸入,**THEN** 兩者皆呼叫同一個寫入介面更新同一個狀態物件——不存在任一輸入來源繞過寫入介面、直接改變某表面已渲染高亮的路徑。**(驗證方式:程式碼審查/靜態分析為主;執行期測試僅能驗證「有」繞過路徑的反例存在與否,無法窮舉證明「不存在」。)**
- **AC-24**(2026-08-04 修訂,涵蓋新增的「標記失效」介面): **GIVEN** 游標目前指向的目標本身失效,且沒有任何呼叫方系統呼叫「標記目前目標為待重新解析」或「設定新目標」介面,**WHEN** 查詢游標狀態,**THEN** 目前游標目標的座標值與有效性旗標皆維持失效前的原始值不變——系統不自動清空、不拋出例外、不自動重新解析成其他目標。
- **AC-25**(2026-08-04 修訂,移除主觀措辭,回應 qa-lead 審查發現): **GIVEN** 游標目前指向的目標已失效(AC-24 情境),**WHEN** 呼叫方系統(例如戰棋移動與交戰系統)主動呼叫本系統的「設定新目標」介面,**THEN** 游標目標欄位的座標值立即等於呼叫方傳入的新目標值、有效性旗標立即翻回有效,且此後任何時間點查詢游標狀態、或任一掛載的 UI 表面渲染,皆讀取到此新值與有效旗標,不殘留失效前的舊座標值或無效旗標——確認委派給呼叫方的解決路徑本身可被驗證地正常運作,而非僅止於假設可運作。

- **AC-29**(2026-08-04 新增,回應 game-designer + systems-designer 審查發現的失效目標視覺一致性缺口): **GIVEN** 呼叫方系統呼叫本系統的「標記目前目標為待重新解析」介面,**WHEN** 檢視下一個渲染影格,**THEN** 所有掛載的 UI 表面皆將該目標呈現為統一的「待重新解析」視覺(而非原本的一般高亮樣式),且此呈現在呼叫方呼叫「設定新目標」介面前持續維持,不因下游系統各自的實作差異而不一致。
- **AC-32**(2026-08-04 新增,回應 ux-designer 審查發現的無限期停滯風險,見 Core Rules #2「玩家導覽自動解析」): **GIVEN** 游標目標的有效性旗標為無效(待重新解析),且沒有任何呼叫方系統呼叫「設定新目標」介面,**WHEN** 玩家透過任一裝置產生一個帶有新座標目標的有效 `ui_*` action,**THEN** 該影格結束時,游標目標的座標值等於該次輸入指定的新值、有效性旗標翻回有效、待重新解析視覺解除——不需要任何呼叫方系統介入即完成解析,證明玩家自身的正常導覽操作本身即足以脫離停滯狀態。
- **AC-33**(2026-08-04 新增,回應 ux-designer 審查發現,見 Core Rules #2「確認鍵行為」): **GIVEN** 游標目標的有效性旗標為無效(待重新解析),**WHEN** 呼叫方系統(例如戰棋移動與交戰系統)查詢本系統的有效性旗標作為確認/選定操作的前置檢查,**THEN** 查詢結果明確回傳「無效」,呼叫方依此拒絕該次確認/選定操作生效於此目標——本 AC 驗證的範圍限於本系統的有效性旗標查詢介面本身回傳正確結果,呼叫方是否真的遵守查詢結果屬於呼叫方系統自己的驗收範圍(見 Dependencies 章節登記的對應義務)。
- **AC-37**(回應 ui-programmer 審查發現的競態條件,見 Core Rules #2「呼叫參數與競態防呆」;**2026-08-06 局部修訂,非重開對抗性審查,擴充回傳值斷言,回應 `/review-all-gdds` Scenario Walkthrough 發現、使用者裁決**): **GIVEN** 呼叫方呼叫「標記目前目標為待重新解析」介面時傳入的目標識別,與系統當下實際持有的目前游標目標**不相符**(例如玩家已在呼叫方偵測失效與實際呼叫之間導覽至另一個目標),**WHEN** 該次呼叫被處理,**THEN** 系統不執行任何有效性旗標翻轉,目前游標目標的座標值與有效性旗標維持呼叫前的狀態完全不變,**且該次呼叫回傳明確的「已過期,未套用」結構化結果(不是靜默無回傳)**——確認過期的標記呼叫不會誤傷玩家當下持有的合法目標,且呼叫方能透過回傳值得知標記未被套用。**GIVEN** 呼叫方呼叫該介面時傳入的目標識別與系統當下持有的目前游標目標**相符**,**WHEN** 該次呼叫被處理,**THEN** 有效性旗標翻轉為無效,且回傳明確的「已套用」結構化結果——與過期情境的回傳值可程式化區分。
- **AC-39**(回應 qa-lead 審查發現的 AC 覆蓋缺口,升級為阻擋項,見 Core Rules #2「系統主動改標與裝置權威的關係」): **GIVEN** 呼叫方呼叫「設定新目標」或「標記目前目標為待重新解析」介面,且此次呼叫並非由任何裝置的 `ui_*` action 觸發,**WHEN** 檢視呼叫完成後的狀態,**THEN** 目前裝置權威欄位與呼叫前完全相同,不因這兩個寫入介面的呼叫而改變——確認游標目標與裝置權威是可獨立變動的正交欄位,寫入目標值不隱含裝置權威的轉移。
- **AC-50(下游預覽渲染義務,回應帳本 L1 自動升級、game-designer/ux-designer/ui-programmer 三方收斂發現,見 Core Rules #2「下游預覽渲染義務」)**: **GIVEN** 游標目標的有效性旗標為無效(待重新解析),且該目標所在座標同時是下游某預覽渲染邏輯(例如移動範圍/攻擊射程預覽、好感度連線預覽)的輸入,**WHEN** 檢視該預覽的畫面呈現,**THEN** 該預覽已被抑制(不渲染)或以明確的不確定佔位樣式呈現,不呈現為與有效性旗標為有效時相同的確信樣式——確認下游預覽消費者確實查詢並遵守有效性旗標,而非只有本系統自身的高亮視覺與確認攔截路徑遵守。**(驗證方式:本系統僅負責正確回傳有效性旗標查詢結果;下游預覽是否遵守屬於各下游系統自身的驗收範圍,比照 AC-33 的既有分工模式,由該下游系統的 GDD 落實對應 AC。)**
- **AC-54(拒絕原因可感知區分,回應 ux-designer 審查發現,見 Core Rules #3「拒絕生效須伴隨主動回饋」(iv))**: **GIVEN** 一次滑鼠點擊分別因(a)裝置權威此刻不是滑鼠、與(b)裝置權威是滑鼠但目標有效性旗標為無效,兩種不同原因被拒絕,**WHEN** 比較兩次拒絕各自觸發的主動回饋,**THEN** 兩者在感知上可區分(例如不同音效音色、不同震動節奏),不使用完全相同的回饋覆蓋兩種情境。**(驗證方式:Visual/Feel 類證據,screenshot/錄音 + lead sign-off,依 coding-standards.md 的 ADVISORY 證據等級,比照 AC-36 的驗證慣例;具體回饋形式由 `/art-bible`/`/sound-designer` 落實後執行。)**

---

## QA Test Cases

🔴 **本批未經 qa-lead 產生測試規格**(管理者 2026-09-02 裁決:精簡模式,覆核關卡不跑;
且本工作環境未經授權不得動用 Agent)。

> 🔴 **2026-09-03 更正:括號裡「不得動用 Agent」那半句已不成立。**
> 管理者於 2026-09-03 開工時明文授權派工,同日稍後再次確認「由你負責派工與監督」。
> 原文保留供追溯,**但不要照它跳過覆核** —— Story 004 正是在這句話仍寫在檔案裡的當天,
> 實際跑完了引擎覆核與測試涵蓋覆核兩關。前半句(qa-lead 精簡模式)是否仍適用,
> 依當次派工指示為準,不由本檔決定。

**替代做法**:上方驗收標準**本身就寫成 GIVEN / WHEN / THEN 形式**,直接作為測試規格使用。
這不是省略 —— 該 GDD 的 AC 是 qa-lead 於 2026-08-03 諮詢草擬、並經十六輪審查修訂的成果,
明文要求「所有標準以可觀測不變式書寫,避免『感覺清楚』等無法驗證的措辭」。

⚠️ **但有一項它不能替代**:AC 沒有列邊界值與失敗態。實作時若發現某條 AC 的邊界不明確,
**停下來問,不要自己選一個** —— 本專案已有「假設錯誤的腳本順利跑完、輸出漂亮數字」的前例。

---

## 效能影響

**無效能影響預期** —— 七個公開入口皆為 O(1) 欄位讀寫加驗證,重入閘門是一次 `bool` 檢查。這些入口由呼叫端事件驅動,不是每幀輪詢。

*權威來源:ADR-0005 `Performance Implications` 節。*

---

## Test Evidence

**Story Type**:Logic
**必要證據**:`tests/unit/cursor/write_read_interface_test.gd`

**Status**:[x] 已建立 —— `tests/unit/cursor/write_read_interface_test.gd`(38 條、0 失敗、0 orphans)
外加 `tests/unit/cursor/state_host_test.gd` 的 AC-1 排除名單擴充與存在性斷言。
**全套實測(主 session 獨立執行,非轉述):345 條 / 0 errors / 1 failures / 0 flaky / 0 skipped /
0 orphans / 26 suites / exit 100。** 唯一失敗為既有已核准的 `affinity_phi_provider` 紅燈,與本張無關。

### 🔴 完成度:7/7 結構層,但只有 5/7 行為主體

**不得寫成「七個公開入口完成」** —— 字面為真、實質誤導(引擎覆核 B-1 阻擋項)。正確敘述:

| | 狀態 |
|---|---|
| 七個公開入口的**結構層**(重入閘門、診斷計數、provider 前置檢查、drain 義務) | **7/7 完成** |
| **行為主體** | **5/7** —— `arbitrate_device_authority()` 與 `apply_buffered_navigation()` 的裁決邏輯屬機制六,是 **Story 005 的接縫** |
| 六條私有路徑、四個讀取查詢、三個 signal、三個 enum | 全部完成 |
| `force_redraw_current_authority()` / `reapply_native_cursor_visibility()` | **刻意未實作**(分屬 Story 008 / 011,見相依稽核報告末節) |

### 驗收標準涵蓋:完整 5 / 部分 4 / 未涵蓋 1

**以測試涵蓋覆核者的獨立重算為準**(他與撰寫者在 AC-25 上有分歧,採他的,依據是工作單第 66 行原文):

| AC | 判定 | 界線 |
|---|---|---|
| AC-24 / AC-33 / AC-37 / AC-39 / AC-50 | **完整** | AC-33、AC-50 的 AC 原文自帶分工條款 |
| AC-3 | **部分** | 只驗公開方法清單;AC 原文自己寫「無法窮舉證明『不存在』」 |
| AC-25 | **部分** | THEN 含「或任一掛載的 UI 表面渲染」,本張無任何掛載表面 → 零涵蓋。與 AC-29 同一句界線 |
| AC-29 | **部分** | 只驗訊號側;表面渲染屬 Story 010/011 |
| AC-54 | **部分(ADVISORY)** | 只驗「兩個獨立查詢可程式化區分兩種拒絕」;**感知上可區分無法自動化**,且回饋形式尚未設計 |
| **AC-32** | 🔴 **未涵蓋** | 阻擋於 Story 005(導覽入口是接縫)。**已登記進 `story-005` 的驗收條件** |

🔴 **AC-32 沒有可靠的自動防線。** `write_read_interface_test.gd` 裡那條 `test_ac32_blocked_...`
**不是警報器** —— 測試涵蓋覆核第 4.5 節證明它在 Story 005 落地當天照樣是綠的(它斷言的
「仍為無效」有第二個成因,由測試自己的輸入所保證),另查三種替代寫法皆不可靠,故不修。
**唯一會被執行的防線是 `story-005-frame-buffer-ordering.md` 的那條登記。**

### 兩方覆核結果

- **引擎/ADR(`godot-specialist`)**:**CONCERNS**,報告 `docs/reviews/story-007-engine-review-2026-09-03.md`(1401 行)。
  17 項全判,阻擋項 1 項(本節措辭,已處理),必修 N-1~N-7,建議 S-1~S-5。
- **測試涵蓋(`qa-lead`)**:**INCOMPLETE**,報告 `docs/reviews/story-007-test-evidence-review-2026-09-03.md`(797 行)。
  8 項全判,最小修正集 T-1/T-2/T-3/B-1/B-2 **已全數執行**。

**已關閉的必修**:N-1(接縫縮排,會讓 Story 005 誤把鍵盤手把仲裁綁在滑鼠管道上)、
N-4(兩句已為假的註解)、N-5(未定義邊界摘到公開 doc comment)、N-7(本檔 Out of Scope 補 Story 005)、
T-1~T-3、B-1(null 組態七入口降級,**含證明閘門未卡死的最後一步**)、B-2(甲分支無條件重播種)。

**未關閉、已登記**:N-2/N-3/N-6(須改已核准的 ADR,呈管理者)、S-1~S-5、G-3~G-7。

---

## Dependencies

- **Depends on**:002, 003
- **Unlocks**:**005**, **008**, 009, **011**

> 🔴 **`Unlocks` 於 2026-09-03 相依稽核補上 005、008、011。** 三張皆呼叫本張產出的
> `CursorState` 公開入口或私有寫入路徑。**本張因此是關鍵路徑上最壅塞的一張** ——
> 四張下游全部等它。全文見 `docs/reviews/story-dependency-audit-2026-09-03.md`。
