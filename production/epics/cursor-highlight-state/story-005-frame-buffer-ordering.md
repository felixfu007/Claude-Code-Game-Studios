# Story 005:整幀緩衝 + `_process` 裁決 + 六行為者定序

> **Epic**:單一游標/高亮狀態系統
> **Status**:Ready
> **Layer**:Core
> **Type**:Integration
> **Estimate**:L(約 8–10 小時)
> **Manifest Version**:2026-09-02
> **Last Updated**:[由 /dev-story 於實作開始時設定]

## Context

**GDD**:`design/gdd/cursor-highlight-state.md`(2026-08-13 第十六輪 Approved)
**Requirement**:TR-cursor-006, TR-cursor-008
*(需求原文在 `docs/architecture/tr-registry.yaml`,審查時請當場讀最新版)*

**Governing ADR**:ADR-0005 單一游標/高亮狀態系統:裝置權威輸入架構(**Accepted** 2026-09-01)
**本 story 對應的機制**:機制五(`_input()` 只緩衝、`_process()` 才裁決)、機制六(六行為者的 `process_priority` 定序)

**Engine**:Godot 4.7.1 | **Risk**:HIGH
**引擎注意事項**:本系統的兩個核心領域(Input、UI)在 4.6 與 4.7 **各有一項直接相關的破壞性變更**。
🔴 **引擎判斷一律以 `docs/engine-reference/godot/breaking-changes.md` 與 `current-best-practices.md` 為準**
—— `modules/input.md` 與 `modules/ui.md` 停在 4.6,對這兩項變更**各自零命中**。

**控制清單規則(本 story 適用)**:
- **必須**:`_input()` 只緩衝整幀,裁決一律在 `_process()`。
- **必須**:六個行為者的 `process_priority` **兩兩相異** —— 這是定序保證成立的前提,不是建議。同值之間 ADR **不假設任何 tie-break**。
- 🔴 **必須**:`process_priority` 的賦值語句在**原始碼順序上早於** `add_child()`(全部 5 處呼叫點)。
- **禁止**:`single_node_for_nonadjacent_cursor_actor_roles`
- **禁止**:`call_deferred_for_cursor_retarget_deferral`

---

## Implementation Notes

*出自 ADR-0005 機制五、機制六:*

**六行為者的架構強制優先序**:

| # | 行為者 | `process_priority` |
|---|---|---|
| ① | 裝置權威判定(`CursorStateHost`) | **−100** |
| ② | 呼叫方主動改標 | **開區間 −100 < ② < −25,兩端點皆不可取**(建議 −60) |
| ③ | 緩衝內導覽寫入(`CursorNavigationApplier`,專屬子節點) | **−25** |
| ④ | 已註冊表面 | **0** |
| ⑤ | 全域視覺層 | **50** |
| ⑥ | 下游讀取方(含確認動作判讀) | **100** |

1. 🔴 **為何必須是兩個節點而不是一個節點內的兩段程式碼**:`process_priority` 是**逐節點**屬性,同一節點的 `_process()` 只會在優先序梯上出現一次。步驟一必須在 −100、步驟三必須在 −25。
2. 🔴 **同時身兼 ② 與 ⑥ 的系統必須拆兩個節點** —— 兩者之間隔著 ③④。
3. 🔴 **`process_priority` 只排序同一種回呼各自的鏈,不跨鏈** —— `_process()` 與 `_physics_process()` 兩鏈之間**無排序保證**。把結算或裁決放進 `_physics_process` 會讓**全部定序保證靜默失效且不報錯**。
4. **`process_priority`(更新順序)與 `CanvasLayer.layer`(繪製疊放順序)是兩個獨立概念**,不得混用。
5. **緩衝區清空點在 −25(③)**,不在 −100。同一影格內因此存在一個中間狀態:「步驟一已裁定、步驟三尚未套用」。
6. **拒絕 `_unhandled_input()` 的理由**:被 `accept_event()` 消費的事件**永不抵達緩衝區**。

✅ **定序基礎已實測成立**(2026-09-01):該影格全部 `_input()` 派發完畢**才**進入 `_process()` 鏈,6 次獨立注入 6/6 一致,跨 `process_priority` −100~100 全部一致。
⚠️ **執行者自陳樣本不足以宣稱「任何負載下皆成立」** —— 僅在空場景、headless、無負載下測。
證據:`prototypes/adr0005-engine-probes-2026-09-01/logs/probe2_headless.txt`

---

## Out of Scope

- Story 008:暫停/焦點對裁決的閘控
- Story 013:AC-12 的 1 幀上限**量測**(本 story 負責讓它成立,013 負責證明)

---

## Acceptance Criteria

*以下為 `design/gdd/cursor-highlight-state.md` 的條文**原文轉錄**,未改寫:*

- **AC-10**(2026-08-04,`/design-review` 完整模式審查後重寫,原版本未區分 B 裝置類型,與 Core Rules #4 的滑鼠暫態繼承邏輯矛盾,由 game-designer 審查發現): **GIVEN** 裝置權威從 A 轉移到 B 的觸發影格,且 **B 為鍵盤/手把**,**WHEN** 檢視該影格結束時的游標目標值,**THEN** 其與轉移前最後一次由 A 寫入的值完全相同——不重置為任何預設格或空值。**範圍排除(B 為滑鼠的情境)**:若 B 為滑鼠,依 Core Rules #4 的一般化說明,滑鼠取得權威的同一個 `ui_*` action 本身帶有螢幕座標推導出的目標位置,交接觸發影格結束時的游標目標值應等於滑鼠當下懸停目標,而非 A 最後寫入的值——此情境不受本 AC 約束,見 AC-10b。
- **AC-10b**(2026-08-04 新增,涵蓋 B=滑鼠情境): **GIVEN** 裝置權威從 A 轉移到滑鼠(B=滑鼠)的觸發影格,**WHEN** 檢視該影格結束時的游標目標值,**THEN** 其等於滑鼠取得權威的同一個 `ui_*` action 所推導出的懸停目標——不殘留 A 的舊值超過該觸發影格本身。
- **AC-13**(2026-08-04 修訂,移除主觀措辭,回應 qa-lead 審查發現): **GIVEN** 裝置權威剛轉移到 B 且游標目標已依 AC-10 承接舊值,**WHEN** B 隨後產生一個帶有明確新目標的 `ui_*` action(例如手把方向鍵移動),**THEN** 游標目標欄位的值於該次輸入的同一影格內,立即等於 B 該次 `ui_*` action 所指定的新座標值,不停留在繼承自 A 的舊值、不延遲到下一影格才更新——繼承規則只作用於交接觸發影格本身,不會持續阻擋新權威裝置後續影格的操作。
- **AC-14**(2026-08-04 第五輪補充「有效」定義的裝置別差異,回應 qa-lead 審查發現的隱性歧義): **GIVEN** 裝置 A 失去權威且其視覺被抑制(AC-11),**WHEN** A 之後再次產生一個有效的 `ui_*` action,**THEN** A 依 Group B 的一般規則重新取得權威且視覺恢復顯示——先前的視覺抑制不會遺留任何狀態,阻止 A 之後正常取回權威。**「有效」的定義依 A 的裝置類別而異,不是單一標準**:若 A 為滑鼠,「有效」指累積淨位移已達 Core Rules #3 空間門檻(見 AC-28、AC-28b);若 A 為鍵盤/手把,「有效」指任一 `ui_*` 導覽動作本身,不需額外累積門檻(見 Core Rules #3 反方向段落、AC-34、AC-34b)——本 AC 驗證兩種裝置類別重新取得權威後視覺恢復的共同行為,門檻是否達標的判定由對應的 AC 群組驗證,不在本 AC 重複驗證。

- **AC-17**: **GIVEN** 手把持有權威並持續產生方向輸入,滑鼠同時完全靜止(無移動、無點擊),**WHEN** 檢視連續多個影格的裝置權威欄位,**THEN** 該欄位維持為手把——滑鼠僅「停留」在畫面某處不觸發權威轉移或游標目標變更。
- **AC-20**(2026-08-04 第二次修訂,統一驗證標準,回應 qa-lead 審查發現的內部不一致;2026-08-04 第八輪修訂 THEN 子句;**2026-08-05 第九輪修訂適用範圍,回應 systems-designer 審查發現的真矛盾——原版本的 GIVEN 未限定鍵盤/手把該次事件是否具備主張權威的資格,若鍵盤/手把該次僅產生確認類動作,依 Core Rules #3「確認類動作與裝置權威的關係」的範圍澄清,母規則根本不適用於該情境,本 AC 與 Core Rules #3、AC-35 曾各自斷言相反結果,已修正為本 AC 僅適用於雙方事件皆具權威資格的情境**): **GIVEN** 滑鼠與手把在同一影格內皆產生對應的 `ui_*` action,**且鍵盤/手把該次動作為導覽類(具備主張裝置權威的資格,見 Edge Cases 母規則「適用範圍」)**,**WHEN** 該影格處理完畢,**THEN** 最終的裝置權威為**鍵盤/手把**,不論滑鼠事件是否在引擎內部被視為同幀內較晚處理,游標目標同時等於該導覽動作所指定的新值。**範圍排除(鍵盤/手把該次僅產生確認類動作的情境)**:若鍵盤/手把該次動作為確認類或其他非導覽類動作,該動作不具備主張裝置權威的資格,本 AC 不適用於此情境——裝置權威判定改依 AC-35 驗證(確認類動作不轉移裝置權威)。**滑鼠奪權累積量的重置行為(不限動作類型皆須重置)由 AC-35b 驗證,不在本 AC 重複驗證。**驗證方式:以程式碼審查為主**——確認判定邏輯是否真的無條件優先鍵盤/手把、且不依賴任何引擎內部事件處理順序或到達順序(此為規則本身的確定性保證,不隨執行次數改變),另須確認判定邏輯正確排除確認類動作不落入本規則仲裁範圍。**執行期測試作為輔助佐證(2026-08-05 第九輪修訂,回應 qa-lead 審查發現——原版本以相同輸入序列重複執行 20 次,對決定性實作沒有額外資訊量,因為固定優先序判定不依賴到達順序,重複相同序列無法驗證「不依賴到達順序」這件事本身)**:以**模擬測試框架控制、逐次刻意變動滑鼠與鍵盤/手把事件到達緩衝區順序**的輸入注入序列,重複執行至少 20 次,確認每次判定結果皆為鍵盤/手把勝出、100% 一致——變動到達順序才能實際佐證「判定不依賴到達順序」的宣稱,單純重複相同序列的佐證力極低,僅作為程式碼審查結論的交叉驗證,不取代程式碼審查。**延伸(連續多影格情境)**:**GIVEN** 此雙裝置同幀觸發情境於連續 10 個以上影格重複發生,**WHEN** 檢視逐影格的裝置權威判定結果序列,**THEN** 每一影格皆為鍵盤/手把勝出,不因連續發生而產生任何滑鼠意外奪權的影格——與上方單幀情境採用相同的驗證標準(程式碼審查為主,執行期測試為輔佐證),不再是兩套不同標準。
- **AC-20b**(2026-08-05 第九輪新增,回應 ui-programmer 審查發現的相鄰缺口,見 Core Rules #3「同一裝置類別內多個 `ui_*` 動作同影格並存的優先序」): **GIVEN** 同一影格內鍵盤/手把產生兩個 `ui_*` 動作——一個導覽類(改變游標目標)、一個確認類(讀取目前游標目標作為確認/選定對象),**WHEN** 緩衝後統一裁定,**THEN** 確認類動作讀取到的游標目標,是導覽類動作套用後的最新值,不是該影格開始前的舊值——確認同一裝置類別內的寫入類事件先於讀取類事件生效,避免確認動作誤讀到本影格已過期的目標。
- **AC-52(同影格四方完整定序,回應 ui-programmer 審查發現,見 Core Rules #2「四方完整定序」)**: **GIVEN** 同一影格內,呼叫方系統的主動改標呼叫(例如單位死亡觸發「標記目前目標為待重新解析」)與玩家對同一目標的緩衝確認類 `ui_*` 動作同時發生,**WHEN** 該影格處理完畢,**THEN** 呼叫方的主動改標呼叫已先生效(有效性旗標已翻為無效),緩衝內的確認讀取讀到的是改標後的最新有效性旗標,依 Core Rules #2「確認鍵行為」正確拒絕該次確認——不存在因排序巧合讓確認動作作用於一個本影格已被判定失效的目標上。**(驗證方式:程式碼審查為主——確認實作的節點執行順序符合「裝置權威判定 → 呼叫方主動改標 → 緩衝內導覽寫入 → 緩衝內確認讀取」四步序列;執行期測試以刻意變動事件到達順序的模擬框架交叉驗證,比照 AC-20 的既有驗證慣例。)**

---

## QA Test Cases

🔴 **本批未經 qa-lead 產生測試規格**(管理者 2026-09-02 裁決:精簡模式,覆核關卡不跑;
且本工作環境未經授權不得動用 Agent)。

> 🔴 **2026-09-03 更正:括號裡「不得動用 Agent」那半句已不成立。**
> 管理者於 2026-09-03 開工時明文授權派工,同日稍後再次確認「由你負責派工與監督」。
> 原文保留供追溯,**但不要照它跳過覆核** —— Story 004 正是在這句話仍寫在檔案裡的當天,
> 實際跑完了引擎覆核與測試涵蓋覆核兩關。前半句(qa-lead 精簡模式)是否仍適用,
> 依當次派工指示為準,不由本檔決定。

**替代做法**:上方驗收標準**本身就寫成 GIVEN / WHEN / THEN 形式**,直接作為測試規格使用。
這不是省略 —— 該 GDD 的 AC 是 qa-lead 於 2026-08-03 諮詢草擬、並經十六輪審查修訂的成果,
明文要求「所有標準以可觀測不變式書寫,避免『感覺清楚』等無法驗證的措辭」。

⚠️ **但有一項它不能替代**:AC 沒有列邊界值與失敗態。實作時若發現某條 AC 的邊界不明確,
**停下來問,不要自己選一個** —— 本專案已有「假設錯誤的腳本順利跑完、輸出漂亮數字」的前例。

---

## 效能影響

🔴 **本 story 是逐幀路徑本體。**

ADR 已量化:`_input()` 每事件僅一次 `Array.append`(O(1));裁決與套用兩者對「該影格事件數」線性,而**一影格內的輸入事件數是個位數量級**(滑鼠移動事件最密,約每影格 1–2 個)。`_frame_events` 每影格清空,峰值與單影格事件數成正比。六個行為者各一次 `_process` 呼叫,**總量遠低於 16.6 ms 預算的任何可觀比例**。

⚠️ **緩衝區沒有硬性容量上限** —— 依據是「事件數天然是個位數」這個**前提**,不是一道閘門。**若實測發現某情境下單影格事件數異常暴增,那是這個前提被打破,不是效能調校問題。**

*權威來源:ADR-0005 `Performance Implications` 節。*

---

## Test Evidence

**Story Type**:Integration
**必要證據**:`tests/integration/cursor/frame_buffer_ordering_test.gd`

**Status**:[ ] 尚未建立

---

## Dependencies

- **Depends on**:002, 004, **007**
- **Unlocks**:008, 013

> 🔴 **本欄於 2026-09-03 相依稽核更正。** 這條相依只存在於 ADR-0005 的簽章裡,本工作單內文
> 從未出現該型別名稱 —— `/story-readiness` 檢查的是「工作單本身完不完整」與「ADR 核准了沒」,
> **結構上不會檢查「ADR 契約是否需要別張工作單的產出」**,故抓不到。全文見
> `docs/reviews/story-dependency-audit-2026-09-03.md`。
>
> **依據**:ADR-0005 L.577-581 —— `apply_buffered_navigation()` 的目標寫入一律走
> `_write_target_internal()`,而該私有路徑由機制十(L.811-819)定義,EPIC.md 指派給 Story 007。
>
> 🔴 **後果**:原排程寫「002、004 完成即可動工」,照做會在開工後才發現要呼叫的方法不存在。
