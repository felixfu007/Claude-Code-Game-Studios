# Story 002:螢幕自適應字級規則

> **Epic**:畫面縮放與定位(手動管理)
> **Status**:Ready
> **Layer**:Presentation
> **Type**:UI
> **Estimate**:S(約 2–3 小時)
> **Last Updated**:[由 /dev-story 於實作開始時設定]

## Context

**權威文件**:`design/art/screen-architecture.md` / `design/art/art-direction.md`
**Engine**:Godot 4.7.1 | **Risk**:LOW(技術上)/ **MEDIUM**(設計上 —— 它會決定文字好不好讀)

---

## 🔴 這張的存在,是 2026-09-01 裁決的一項**已知連帶後果**,不是新發現

裁決把介面設計基準畫布定為**當下螢幕實際解析度**,不設固定基準、不做二次縮放。

**那個裁決是對的,理由是實測**:HUD 用的 Cubic 11 是 11×11 點陣、放大 2 倍使用;
若採固定 1920×1080 基準,在 2K 上合併縮放為 **2.667 倍**,實測像素塊 **2/3px 交錯、
肉眼可見鋸齒**。

⚠️ **但它換來一個必須自己處理的問題,而且當時就寫下來了**:

> 基準等於實際解析度,代表 **20px 的字在 4K 上仍然是 20px**,相對螢幕顯著變小。

📌 **`ux-designer` 在同日評估中把這一項列為最擔心的一件事。**
**該裁決沒有消除它,是把它從「引擎自動處理」換成「我們自己處理」。**

🔴 **權威文件明文寫著「排介面前必須先訂這套規則」。** 這張就是那件事。

---

## 要交付什麼

**一套寫下來的規則**,回答:在不同螢幕解析度下,各類文字要用多大的字級。

**至少要涵蓋兩類,因為它們的限制不同**:

| 類別 | 字型 | 限制 |
|---|---|---|
| HUD / 標籤 | Cubic 11(11×11 點陣,放大 2 倍) | 🔴 **點陣字只能整數倍放大**,非整數倍會產生上述 2/3px 交錯鋸齒 |
| 對話正文 | 一般繁體中文字型(**刻意不用像素字型**,見 `art-direction.md`) | 向量字型可任意縮放,但要顧慮可讀性下限與行長 |

⚠️ **兩類的答案很可能不同**,不要用一條規則套兩邊。

---

## Implementation Notes

1. **這張的產出主要是「決定 + 文件」,不是大量程式碼。** 規則寫進
   `design/art/screen-architecture.md`(或它指定的位置),程式面可能只需要一個小的換算函式。
2. 🔴 **點陣字的整數倍限制是硬性的**,不是偏好 —— 它有實測支撐(2K 上 2.667 倍的鋸齒)。
   **任何規則若讓 Cubic 11 落在非整數倍上,就是錯的。**
3. **主機安全區已有定案數字可用**:`screen-architecture.md` 第六節「文字絕對安全 432×242 置中」。
   字級規則不得讓文字跑出該範圍。
4. 與 Story 001 **無依賴關係**,可平行進行。但**兩張都完成之前,不要開始排任何介面版面。**

---

## Out of Scope

- 實際的介面版面配置(要等本張與 001 都完成)
- 字型檔本身的選用(已定案:HUD 用 Cubic 11、正文用一般繁體中文字型)

---

## Acceptance Criteria

- **AC-S002-a(規則存在且可套用)**:**GIVEN** 1920×1080 / 2560×1440 / 3840×2160 /
  3440×1440 四種螢幕,**WHEN** 依規則推導 HUD 與對話正文的字級,**THEN** 四種螢幕各得到
  一個明確數值,**沒有任何一種需要現場判斷**。

- **AC-S002-b(點陣字整數倍)**:**GIVEN** 上述四種螢幕,**WHEN** 檢視 HUD 字級對應的
  Cubic 11 放大倍率,**THEN** 四者皆為整數倍。

- **AC-S002-c(不超出安全區)**:**GIVEN** 依規則設定的字級,**WHEN** 檢視文字在
  `screen-architecture.md` 第六節主機安全區(432×242 置中)內的排布,**THEN** 未超出該範圍。

⚠️ **邊界值未定義處請停下來問,不要自己選一個。** 已知未定義:
四種列舉螢幕**之外**的解析度(例如 1600×900、2560×1080)要套哪一級。

---

## Test Evidence

**Story Type**:UI(閘門等級 ADVISORY)
**必要證據**:`production/qa/evidence/adaptive-font-scale-evidence.md`
**Status**:[ ] 尚未建立

🔴 **這張的證據必須包含真的把字畫出來看。** 字級規則的失敗態是「算得出數字但讀起來很糟」,
**那不是自動測試抓得到的東西** —— `coding-standards.md` 的「What NOT to Automate」
明文把「感覺」類品質排除在自動化之外。
AC-S002-b(整數倍)可以自動驗;AC-S002-a / AC-S002-c 要人看。

---

## Dependencies

- **Depends on**:無
- **Unlocks**:所有介面版面工作
