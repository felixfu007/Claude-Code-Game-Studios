# Epic:畫面縮放與定位(手動管理)

> **層**:Presentation
> **權威文件**:`design/art/screen-architecture.md`(2026-09-01 管理者裁決,非 GDD)
> **狀態**:Ready
> **Stories**:2 張
> **建立日期**:2026-09-04

## 為什麼這個 epic 存在

**2026-09-01 管理者裁決把畫面縮放從「引擎自動處理」改為「我們自己管」。**
該裁決被記載在 8 個檔案裡,但**沒有任何一張工作單擁有它**,因此三天內沒有進入任何排程。

🔴 **本 epic 的存在理由就是把它從「文件裡的決定」變成「排程裡的工作」。**
建立於 2026-09-04,起因是協調者在盤點下一步時發現 `project.godot` 的實際值
與 `.claude/docs/technical-preferences.md`(**每次對話開場載入**)寫的值不一致,
而不一致的那一邊沒有人負責收斂。

## 🔴 這個 epic 沒有 GDD,也不會有

它不是遊戲系統,是呈現層基礎設施。權威來源是 `design/art/screen-architecture.md`
(該節原在 `art-direction.md` 第七節,2026-09-01 搬出為獨立文件,搬出理由是
「內容是引擎設定與縮放規則,不在美術文件自述的權威範圍內」)。

⚠️ **依 `.claude/docs/coding-standards.md`,ADR 只在跨系統契約時才寫。**
本 epic 目前不需要新 ADR —— 決策已經做完,缺的是執行。

## 裁決內容(摘要,權威在 `screen-architecture.md`,本節不複述細節)

| 項 | 裁決 |
|---|---|
| `window/stretch/mode` | 由 `"canvas_items"` 改為 **`"disabled"`** |
| 介面設計基準畫布 | **當下螢幕實際解析度**,不設固定基準、不做二次縮放 |
| 世界層縮放與定位 | **手動管理** —— 取塞得下的最大整數倍,**永遠置中** |

**倍率結果**(2K 與超寬為 spike 實測,1080p 與 4K 為算術推導):

| 螢幕 | 倍率 | 棋盤尺寸 | 剩餘邊區 |
|---|---|---|---|
| 1920×1080 | 4× | 1920×1080 | **零** |
| 2560×1440 | 5× | 2400×1350 | 四周 80 / 45 px |
| 3840×2160 | 8× | 3840×2160 | **零** |
| 3440×1440 | 5× | 2400×1350 | 左右各 520 px |

🔴 **由上表導出、約束所有介面工作的一條硬性規則**:
**介面不得依賴「棋盤旁邊有空位」** —— 兩種最常見的螢幕邊區為零。
對話框、HUD、好感度關係圖**必須疊在棋盤上方**,不得佔用側邊固定欄位。

## Stories

| # | Story | 型別 | 狀態 | 估時 | 依賴 |
|---|---|---|---|---|---|
| 001 | [切換縮放模式 + 世界層手動縮放定位](story-001-manual-world-scaling.md) | Integration | Ready | M(約 4–6 小時) | — |
| 002 | [螢幕自適應字級規則](story-002-adaptive-font-scale.md) | UI | Ready | S(約 2–3 小時) | — |

**兩張互不依賴,可平行,也可只做一張。**
🔴 **但 001 必須整批完成,不可只改設定** —— 引擎不再自動縮放之後,若手動縮放程式不存在,
棋盤會以 480×270 原始大小畫在角落。**設定與程式是同一個原子變更。**

## 🔴 已知會被 001 弄紅的既有測試(不是缺陷,是預期後果)

`tests/unit/cursor/cursor_layer_transform_test.gd` 的
`test_environment_sanity_root_transform_is_non_identity_when_resized` **會失敗**。

它斷言根視窗變換在 2K 下為**非**恆等、倍率 5×、偏移 `(80, 45)` ——
**這三項全部是 `canvas_items` 模式的性質**,切成 `disabled` 之後三項皆不成立。

⚠️ **這不是把測試改綠就好。** 該測試存在的目的是證明「游標圖層的恆等斷言不是廢話」——
它要先證明環境是非恆等的,圖層的恆等才有資訊量。**切換之後那個論證失效,
必須重新回答「AC-S010-a 現在證明了什麼」。**

✅ **ADR-0005 Validation Criteria #20 已經預先寫過這段推理**(2026-09-01 窄範圍複驗補),
結論是該驗證條件**仍然必需**,理由有二:①畫面設定會再改;②HUD 字型放大 2 倍使用,
介面層將來一旦為此掛上縮放,共用節點立刻壞掉。**動手前先讀該段,不要重新推導。**

## 驗收得了嗎(開工前查核,2026-09-04)

✅ **驗得到,不是空集合。** `src/ui/battle/BattleScreen.tscn` 是目前的主場景,
內含 `SubViewportContainer` + `BoardView`(世界層)與 `UILayer` 的四個文字標籤(介面層),
兩層都真的會被畫出來。**改設定會立刻在這個畫面上顯現,不是純理論工作。**

📌 **本節是刻意寫的。** 本專案已有三次「工作單做完全綠、而它要驗的東西不存在」
(Story 003 AC-51、Story 007 AC-32、Story 012 全案),故新 epic 一律先回答這一題。

## Next Step

兩張皆可開工。**沒有前置阻擋。**
