# Epic:技能卡牌系統 —— 邏輯層(好感度對話卡牌)

> **層**:Gameplay
> **權威文件**:`design/gdd/skill-card-system.md`(In Design,第一輪 `/design-review` 已修訂)
> **介面權威**:`design/ux/skill-card-play.md` —— 🔴 **介面故事一律引用該檔,不得引用 GDD**
> **治理 ADR**:ADR-0001(戰棋查詢原子性,Accepted · 2026-09-09 第一次修訂)、
> ADR-0002(好感度數值池,Accepted)、ADR-0005(游標/高亮,Accepted)
> **狀態**:✅ **邏輯層 5 張 + 接線 3 張全部完成(2026-09-10)**;介面層未建立
> **建立日期**:2026-09-10

## 這個 epic 涵蓋什麼、不涵蓋什麼

**涵蓋:邏輯層。** 甲類修正的資料模型與存續、卡池/手牌/棄牌、丙類的合法配對與驗證、
打牌四步流程的資料層。**全部 headless 可測,不碰任何節點、不碰畫面。**

**不涵蓋:介面層。** 手牌帶、卡面、選取視覺、按鍵綁定、棋子上的剩餘回合標記,
一律待後續 epic,權威為 `design/ux/skill-card-play.md`。

🔴 **不涵蓋的理由不是「還沒排到」,是一條硬性同批義務**:
`design/ux/skill-card-play.md` AC-U12 與 `design/ux/battle-menu.md` AC-M1 要求
**取消鍵重綁與戰鬥選單必須同批上線** —— `Esc` / 手把 `B`(`battle_end_phase`)
目前是玩家唯一能把回合交給敵方的手段,只做前半會讓戰鬥永遠停在玩家回合。
**邏輯層完全不碰按鍵,故不受該義務約束;介面層受它約束。**

## 🔴 開工前查到的一件事:丙類的下游在程式碼裡不存在

GDD「硬依賴」節寫「好感度數值池的寫入介面(ADR-0002,Accepted)—— 丙類唯一的出口」。
**2026-09-10 實測:該數值池零實作。**

```
grep -rn "append_record\|combat_card\|AffinityPool\|data_pool" src/ --include=*.gd
→ 零命中
```

`src/gameplay/affinity/` 現有四檔(`affinity_link` / `affinity_rules` /
`affinity_phi_provider` / `affinity_line_status`)**全部是關係線極性的讀取路徑**,
餵給 `Φ`;**沒有任何 append-only 記錄結構。**

⚠️ **「ADR 已 Accepted」是設計層通過,不是程式已存在。** 兩者在文件裡長得一樣,
而本專案已記載過同形狀的誤讀(`docs/consistency-failures.md`)。

**處置(不新增裁決,套用既有規範)**:依 `.claude/docs/coding-standards.md`
「dependency injection over singletons」,#6 定義一個**窄寫入埠**並注入,
丙類自己那一側的規則(合法配對集合、陣亡排除、`m ≠ 0`、取消零寫入)完整實作並有測試;
真正的池由 #1 未來實作後接上。

🔴 **必須誠實登記的限制**:AC-6 今天只能驗到**埠**這一層。
「記錄真的落在池裡、`source` 欄真的是 `combat_card`」的端到端驗證**待 #1 實作**。
**這一項不得計入本 epic 的完成度。**

## 數值錨點(全部已存在,本 epic 不自行決定任何數值)

| 項目 | 值 | 來源 |
|---|---|---|
| 單卡 `\|Δatk\|` / `\|Δdef\|` | 1 ~ 3 | `design/quick-specs/unit-stats-provisional.md` 7-4 |
| 累計夾限 `ΣΔatk` | **[−4, +6]** | 同上 |
| 累計夾限 `ΣΔdef` | **[−3, +4]** | 同上(該節註明對現行 DEF 偏保守、放寬有依據) |
| 有效值下限 | `max(0, …)` | GDD 公式一 —— **硬性,不是保守寫法** |
| 甲類持續回合 `r` | {1, 2} | GDD 公式二 |
| 開局手牌 / 每回合補牌 / 手牌上限 | 5 / 1 / 5 | GDD Core Rules 二 |
| 丙類幅度 `m` | {−3,−2,−1,+1,+2,+3},**不得為 0** | GDD 公式三 |

## Stories

| # | Story | 型別 | 狀態 | 估時 | 依賴 |
|---|---|---|---|---|---|
| 001 | [甲類修正的資料模型與有效值計算](story-001-modifier-model.md) | Logic | ✅ **Complete**(2026-09-10) | M | — |
| 002 | [甲類的回合存續、到期與清除](story-002-modifier-lifecycle.md) | Logic | ✅ **Complete**(2026-09-10) | M | 001 |
| 003 | [卡池、手牌與棄牌](story-003-deck-and-hand.md) | Logic | ✅ **Complete**(2026-09-10) | M | — |
| 004 | [丙類的合法配對集合與寫入埠](story-004-permanent-write-port.md) | Logic | ✅ **Complete**(2026-09-10) | M | — |
| 005 | [打牌四步流程的資料層(取消零寫入 + 預覽串接)](story-005-play-session.md) | Integration | ✅ **Complete**(2026-09-10) | M | 001, 003, 004 |
| 006 | [把卡牌接進戰鬥迴圈](story-006-battle-loop-wiring.md) | Integration | ✅ **Complete**(2026-09-10) | M | 001~005 |
| 007 | [強制棄牌的阻塞閘門](story-007-forced-discard-gate.md) | Integration | ✅ **Complete**(2026-09-10) | M | 006 |
| 008 | [把「打牌」這個動作接上](story-008-play-session-wiring.md) | Integration | ✅ **Complete**(2026-09-10) | M | 005~007 |

**001~004 彼此獨立,可任意順序。005 需要前三張。**

## 驗收條件對照(GDD `## Acceptance Criteria`,15 條)

| AC | 落在哪張 | 備註 |
|---|---|---|
| AC-1 逐條加總不取最大 | 001 | |
| AC-2 減益溢出不轉傷害(邊界) | 001 | 須鎖定 `Φ=0` 與攻擊方 ATK |
| AC-3 `r=1` 撐過緊接的敵方回合 | 002 | |
| AC-4 到期早於該回合任何查詢 | 002 | |
| AC-5 打牌後預覽 `+3` | 005 | 🔴 **必須呼叫 `BattleState.preview_damage()`,不得自行重算公式** |
| AC-6 丙類寫入 `source=combat_card` | 004 | ⚠️ **只驗到埠;端到端待 #1** |
| AC-7 陣亡配對不在合法集合 | 004 | |
| AC-7b 繞過介面直接寫陣亡配對被拒 | 004 | ⚠️ 第二道防線在池,**池不存在** → 由埠契約承擔,見該故事 |
| AC-8a 非 canon 配對不在合法集合 | 004 | |
| AC-8b 正面控制組(甲乙可寫、極性不變) | 004 | **無它則 AC-8a 訊號打折** |
| 🔴 **AC-8c** 繞過介面傳非 canon 配對被拒 | 004 | **2026-09-10 新增** —— 該日裁決「配對由玩家選」開了這條路徑。**無第二道防線**(池對關係線零認知) |
| AC-9 `m=0` 被拒且不靜默糾正 | 004 | |
| AC-10 滿手強制棄牌阻塞 | 003 | UI 型;**邏輯層只驗「阻塞旗標為真且系統未代選」** |
| AC-11 戰鬥結束收回 + 清修正 | 002 + 003 | 兩側各一半 |
| AC-12 牌永不銷毀(總量不變) | 003 | |
| AC-13 確認前取消 → 零寫入 | 005 | |
| AC-14 權威寫入進行中拒絕開手牌 | 005 | 🔴 **`authoritative_write_in_progress` 在程式碼裡尚不存在**,見該故事 |

🔴 **GDD 明文要求把這些 AC 拆到多個測試檔**,理由是本專案實測的陷阱:
**一條真實失敗會讓同檔案後面的測試全部不執行**,而 `failures` 計數會讓人誤讀成「只壞了一個」。
本 epic 的一張 story = 至少一個獨立測試檔,**不得合併。**

## 🔴 GDD 登記為「零 AC 覆蓋、實作期必補」的六條(本 epic 必須補上)

保底規則、打牌不消耗移動/攻擊旗標、**打牌介面開啟中不得發起攻擊**(AC-14 的另一半)、
每回合出牌數不限、卡池不足 5 張時開局發不滿、甲類修正隨宿主陣亡消失。
**逐條落點見各 story 的「補測」節。** 保底規則待卡表(GDD OQ-6),本 epic 不補。

## 本 epic 不做、且有明確理由的

| 不做 | 理由 |
|---|---|
| 乙類(暫時覆蓋關係極性) | GDD 定義不實作;且需要 #5 一個不存在的注入口,**不得單方面新增**(OQ-10) |
| 卡表本體、保底規則具體形式、丙類張數上限 | GDD OQ-6,待卡表設計階段 |
| `amp` 量化 | GDD Core Rules 五:MVP 答案是「不量化」,恆為 1 |
| 存檔區塊 | 戰鬥中不可手動存檔(2026-09-08 裁決),甲類與手牌活不過任何存檔點 |
| 移動力類效果 | 甲類收窄為 ATK/DEF;雙旗標語意未定(上游 OQ-4 未關的一半) |

## 🔴 2026-09-10 管理者裁決:丙類配對由玩家在棋盤上選

**權威依據**:`design/ux/skill-card-play.md` 的 S2p/S2q 兩個獨立選取步驟。
GDD 已同步更新兩處(牌面判準節、公式三界線節)並新增 **AC-8c**。

**它是怎麼被抓到的,值得記住**:Story 004 的專家在回報的「你沒做到或不確定的事」
一欄自陳「我假設每張丙類卡自己寫死配對」,協調者比對 UX 規格後發現兩份文件講的不一樣。
**派工單要求逐項回報不確定項,這是它第一次直接擋下一個實作錯誤** ——
若沒標,Story 005 接上去才會發現,那時要動的東西更多。

⚠️ **管理者知情接受的代價**:牌面可能寫「甲對乙說了句鼓勵的話」而玩家打在丙丁身上,
**牌面故事與實際效果對不上**。選它的理由是另一邊更糟(卡片寫死配對 → 抽到不相干的那對就是廢牌)。

## ✅ 2026-09-10 收工狀態

**測試 448 → 516 條**(新增 68:S001 18 / S002 9 / S003 16 / S004 7 / S005 18)。
516/516 全部執行、0 errors、0 orphans、唯一失敗是既有那條刻意留紅的
`affinity_phi_provider_test.gd`。**協調者獨立跑過覆驗,非照抄專家報告。**

### 🔴 三項必須傳下去的限制(全部不得計入完成度)

| # | 限制 | 誰能解 |
|---|---|---|
| 1 | **丙類端到端無法驗證** —— 好感度數值池零實作,只驗到埠 | #1 好感度數值池 |
| 2 | **AC-14 驗的是「閘門有接」不是「旗標會變真」** —— `authoritative_write_in_progress` 在 `src/` 只是一句註解 | #4 戰棋(ADR-0001 機制一) |
| 3 | 🔴 **整套卡牌邏輯尚未接進戰鬥迴圈** —— `CardDeck` / `CardPlaySession` 在 `src/` 皆零呼叫點 | 下一批 |

### 🔴 限制 3 帶著一條順序契約,接線的人必須遵守

**玩家回合開始:先 `BattleState.tick_all_modifiers()`,再 `CardDeck.draw_for_turn()`。**

理由不是實作偏好:若先補牌而觸發滿手強制棄牌,玩家會在一個**還掛著已到期修正**的盤面上,
做那個**不可逆**的棄牌決定。Story 002 已用測試證明「若遵守此序,棄牌決策點看到的盤面就是對的」,
**但那不等於證明 production 已正確接線 —— 目前沒有任何生產程式碼把兩者串起來。**

### 敏感度證明的實際覆蓋(誠實,非全綠)

| Story | 測試數 | 做過注入證明的 |
|---|---|---|
| 001 | 18 | 9 條(P1~P9) |
| 002 | 9 | **2 條**(AC-3、AC-4);其餘 7 條未逐一注入 |
| 003 | 16 | 6 條(含一個做失敗後捨棄重做的) |
| 004 | 7 | 3 條 |
| 005 | 18 | 見該 story 回報 |

📌 **成本說明**:一次注入只證明得了第一條被它撞到的斷言(套件會中止),
要證明第 N 條就得把前面的 `test_` 暫時改名移出收集範圍再改回。**逐條證明 = 每條一次執行。**

### ⚠️ 流程層面必須傳下去的兩件事

1. **專家報告本日截斷 10 次,每一次都是「報告沒送出」而非「工作沒做完」。**
   實際進度全部靠逐檔 `grep` 才知道。**不要把專家報告當成完成度來源。**
2. 🔴 **提交閘門只擋帶標記的注入。** Story 005 的專家把 `delta_atk` 換成字面 `0`
   來證明 AC-5,**沒加標記,閘門完全沒反應** —— 而那個改法的後果是
   「所有攻擊加成牌一律無效」,不崩潰、不編譯失敗。**擋下它的是逐行讀程式碼。**
   **綠燈不等於沒有注入。** 派工單一律要求專家為注入加標記。

## ✅ 2026-09-10 接線完成後的最終狀態

**測試 448 → 532 條**(新增 84)。532/532 全部執行、0 errors、0 orphans、
唯一失敗是既有那條刻意留紅的 `affinity_phi_provider_test.gd`。**協調者獨立覆驗。**

### 🔴 接線層學到的一件有普遍性的事

**AC-W3(順序契約)原本是一條驗不到東西的測試,而發現它的是專家自己。**

功能測試在 `begin_player_turn()` 返回之後查最終狀態,**兩種順序的最終狀態一模一樣** ——
因為 `tick_all_modifiers()` 改的是 `Unit._modifiers`,`draw_for_turn()` 改的是 `CardDeck`
自己的三個陣列,**兩者不讀對方的資料**。

> **判準:若兩種錯誤實作會留下相同的最終狀態,就不可能用最終狀態的斷言區分它們。**
> 要驗「順序」,取樣點必須在**過程中**,不能在結束後。

**解法是間諜**:覆寫 `CardDeck.draw_for_turn()`,在被呼叫的**當下**(委派給父類別之前)
取樣 `unit.active_modifiers().size()`。順序對讀到 0,對調讀到 1。

📌 **另有一條讀原始碼順序的測試並存**,各守一種失效:
一條守「兩行被對調」,一條守「行為真的錯了」。
⚠️ **讀原始碼那條的弱點要記住**:它綁在文字上,有人把 draw 包進 helper 或改名,
**它會照樣通過而契約已經沒人守著,且不留任何跡象。** 它是輔助,不是主防線。

### 📌 一件實測到的 GDScript 行為(考慮登記進引擎參考庫)

**lambda 捕捉外部區域變數是傳值,不是傳參照** —— 在閉包裡改一個捕捉來的 `int`,
改動不會傳出去。專家最初想用閉包記錄間諜資料,因此失敗,改用物件成員變數才成立。

### 待管理者確認的一項(不阻擋)

🔴 **第一回合手牌 5 張,不是 6 張。** GDD 寫「開局 5 張」+「每回合補 1 張」,
**兩句話對第一回合的合成結果沒有明說。** 選 5 的理由:6 張會立刻觸發滿手強制棄牌,
亦即玩家第一個動作就是被迫棄牌 —— 很可能不是設計意圖,但 GDD 沒說死。

### ✅ 那道靜默守衛已由 Story 007 移除(2026-09-10 同日結案)

**原文記錄的是**:`BattleState.begin_player_turn()` 的補牌帶著
`not _card_deck.has_pending_discard()` 條件,亦即上一回合的強制棄牌沒解決的話,
這一回合**靜默不補牌**。當時登記為「待確認」,因為 Story 006 的實作者自陳那是
**防禦性直覺、不是查證過的判斷**,並誠實寫下「我自己選的方案,犯了我讀過的規則要防的那種錯」。

🔴 **管理者的裁決推翻的是問法,不只是實作。** 協調者把它問成「棄牌沒解決該怎麼辦」,
給的三個選項全是在處理那個狀態。裁決逐字:

> **玩家因為獲得新手牌而造成手牌上限時,啟動棄牌流程,系統必須詢問玩家要捨棄哪一張牌,
> 玩家選擇後,系統棄掉該張牌,接著玩家才能動作。如果玩家沒有執行棄牌行為,
> 則一直等待玩家選擇好棄牌為止。**

→ **它不可能沒解決,因為回合會一直等。** 守衛因此被**移除**而非調整,見 Story 007。

📌 **這一則值得留著的理由**:兩件事單獨都不夠 ——
專家自陳「這是直覺不是判斷」讓它沒有被當成既定事實;管理者的裁決才真的修掉它。
**若專家當初沒有在「不確定的事」那一欄寫下來,它會以一道看起來合理的守衛留在程式裡。**
