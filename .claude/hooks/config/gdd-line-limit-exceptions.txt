# GDD files that are exempt from the 400-line hard limit
# (but still subject to the "no growth" ratchet rule)
# Do not remove entries without manager approval

affinity-position-chain.md
skill-card-system.md
tactical-combat-system.md

# ⚠️ 2026-09-14 未上線 —— 效能未過關,刻意不註冊
# 單次執行實測 50462 ms(對 11 份 GDD 各開 git cat-file + git show,共 22 個外部行程;
# 本機每個行程 0.5~1.5 秒)。而 2026-09-11 凍住兩個 session 的根因就是 hook 逾時。
# 修法方向:改用一次 git ls-tree 或 git diff --numstat 取得全部行數,1~2 個行程而非 22 個。
# 邏輯本身已驗證正確:排除清單有效(1337 行裁決紀錄 +1 未被列為違規)、邊界為 > 400。
