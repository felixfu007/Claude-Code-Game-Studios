@abstract
class_name SpikeBareString extends RefCounted
# ROUND7 Probe E — E2a。@abstract func 裸簽章 `-> String` 是否編譯。順帶測掉,
# 成本為零。獨立成檔,理由同 e1_abstract_variant_return.gd 檔頭。

@abstract
func get_label() -> String
